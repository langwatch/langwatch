{
  "skill_name": "feature-new",
  "evals": [
    {
      "id": 1,
      "prompt": "Add a new 'saved-view' feature: users can save a named set of trace filters per project, list them and delete them. Settings page under the project.",
      "expected_output": "Creates packages/features/saved-view/{contract,server,web} with feature.json, a spec file written first, a catalogue.json subject, zod schemas with infer, a callable <Name>Api interface with its featureApi token, an installer built with defineFeature, an app class over private services, repository interfaces with Prisma and memory backends selected by defineRepositories, a flat tRPC declaration via defineTransport, an apps/api composition that boots the installer with createApp, apps/ui catalogue and WebInstallation registrations, error codes registered in handled-error, and tests binding every scenario; runs scoped gates only.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Scaffold a feature package for webhook signing keys, server only, no UI yet.",
      "expected_output": "Creates contract and server only, no web package, names the absence of a UI, and still writes the spec, the API token, the installer, the app, the service, both repository backends, the transport declaration and the composition.",
      "files": []
    }
  ]
}
