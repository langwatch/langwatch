{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "d=$PWD; while [ \"$d\" != / ] && [ ! -f \"$d/dev/scripts/playwright-mcp.sh\" ]; do d=$(dirname \"$d\"); done; [ -f \"$d/dev/scripts/playwright-mcp.sh\" ] || { echo \"playwright-mcp.sh not found above $PWD\" >&2; exit 1; }; exec bash \"$d/dev/scripts/playwright-mcp.sh\" \"$@\"",
        "_"
      ],
      "env": {}
    },
    "playwright-headed": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "d=$PWD; while [ \"$d\" != / ] && [ ! -f \"$d/dev/scripts/playwright-mcp.sh\" ]; do d=$(dirname \"$d\"); done; [ -f \"$d/dev/scripts/playwright-mcp.sh\" ] || { echo \"playwright-mcp.sh not found above $PWD\" >&2; exit 1; }; exec bash \"$d/dev/scripts/playwright-mcp.sh\" \"$@\"",
        "_",
        "--headed"
      ],
      "env": {}
    }
  }
}
