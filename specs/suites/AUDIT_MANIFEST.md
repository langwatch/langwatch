# Suites domain — unimplemented scenario audit

**Issue**: #3458
**Branch**: `audit/unimpl-suites-2026-04-25`
**Scope**: every scenario tagged unimplemented under `specs/suites/` (112 total).

Classes:
- **KEEP** — scenario describes intended behavior, no test bound yet → write a test in Phase 3.
- **UPDATE** — behavior has changed since the scenario was written; rewrite before binding.
- **DELETE** — aspirational/stale; remove from the spec.
- **DUPLICATE** — already covered by another scenario or test; remove + cross-link.

| File | Scenario | Class | Rationale |
|------|----------|-------|-----------|
| specs/suites/archived-scenario-exclusion.feature | "Run a suite that contains archived scenarios and targets" | KEEP | E2E flow for #1828 (`feat(suites): exclude archived scenarios and targets from suite runs`). Behavior is implemented in `langwatch/src/server/suites/suite.service.ts`; no e2e test bound. Phase 3 should add a Playwright run or convert to integration. |
| specs/suites/archived-scenario-exclusion.feature | "Suite run excludes archived scenarios from job scheduling" | KEEP | Behavior covered by `suite.service.unit.test.ts` "given a suite with mixed active and archived scenarios". Bind via `@scenario` JSDoc rather than write new test. |
| specs/suites/archived-scenario-exclusion.feature | "Suite run fails when all scenarios are archived" | KEEP | Behavior covered by `suite.service.unit.test.ts` "given all scenarios in a suite are archived" (throws AllScenariosArchivedError). Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Deleted scenarios still cause validation errors" | KEEP | Behavior covered by `suite.service.unit.test.ts` "given a suite references a deleted scenario". Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Suite run excludes archived targets from job scheduling" | KEEP | Behavior covered by `suite.service.unit.test.ts` "given a suite with mixed active and archived targets". Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Suite run fails when all targets are archived" | KEEP | Behavior covered by `suite.service.unit.test.ts` "given all targets in a suite are archived" (throws AllTargetsArchivedError). Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Suite run reports skipped archived scenarios" | KEEP | `skippedArchived.scenarios` is returned by service (see #1828). Covered by `suite.service.unit.test.ts` "given 3 scenario refs, 2 target refs, 1 scenario archived…". Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Suite run reports skipped archived targets" | KEEP | `skippedArchived.targets` is returned by service. Same combined unit test as above. Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Filters out archived scenarios from a reference list" | KEEP | Unit-level wording of "given a suite with mixed active and archived scenarios" in `suite.service.unit.test.ts`. Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Returns empty list when all scenarios are archived" | KEEP | Unit-level wording of "given all scenarios in a suite are archived" in `suite.service.unit.test.ts`. Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Returns all scenarios when none are archived" | KEEP | Equivalent to `suite.service.unit.test.ts` "given a suite with 3 scenarios, 2 targets, and repeat count 1" (no archive activity). Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Filters out archived targets from a reference list" | KEEP | Unit-level wording of "given a suite with mixed active and archived targets" in `suite.service.unit.test.ts`. Bind via JSDoc. |
| specs/suites/archived-scenario-exclusion.feature | "Job count reflects only active scenarios and targets" | KEEP | Covered by `suite.service.unit.test.ts` "given 3 scenario refs, 2 target refs, 1 scenario archived, 1 target archived". Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "Sidebar shows \"Runs\" menu item without beta badge" | KEEP | Implemented: `MainMenu.tsx` uses `projectRoutes.simulation_runs.title` = "Runs" with `featureIcons.simulation_runs.icon` (`utils/routes.ts:131`, `utils/featureIcons.ts:81`). No beta badge. No test bound. Phase 3 = sidebar integration test. |
| specs/suites/simulation-runs-page.feature | "No separate \"Run History\" or \"Run Plans\" menu items exist" | KEEP | Negative-of-above. Sidebar wiring confirms absence; needs an integration test asserting these labels are not rendered. |
| specs/suites/simulation-runs-page.feature | "Page heading reads \"Simulations\"" | KEEP | Already covered by `SuiteUrlRouting.integration.test.tsx` "displays Simulations heading". Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "New Run Plan button text is unchanged" | KEEP | Already covered by `SuiteUrlRouting.integration.test.tsx` "displays New Run Plan button". Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "Navigating to /simulations shows All Runs view" | KEEP | Already covered by `SuiteUrlRouting.integration.test.tsx` "shows the all runs view" (under "navigating to /simulations (base path)"). Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "Navigating to /simulations/run-plans/:suiteSlug shows suite detail" | KEEP | Already covered by `SuiteUrlRouting.integration.test.tsx` "shows that suite details" (under "/simulations/run-plans/:suiteSlug"). Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "Navigating to /simulations/:externalSetSlug shows external set" | KEEP | Already covered by `SuiteUrlRouting.integration.test.tsx` "shows the external set panel". Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "Navigating to /simulations/run-plans/:suiteSlug/:batchId loads suite and highlights batch" | KEEP | `useSuiteRouting.unit.test.ts` covers parsing ("/simulations/run-plans/slug/batchId returns suite slug with batch highlight"). Visual flash + scroll behavior is unbound — Phase 3 integration test needed. |
| specs/suites/simulation-runs-page.feature | "No scroll when target batch is already the first row" | KEEP | Edge case for scroll-to-batch UX, no test bound. Phase 3 = scroll-stub integration test using viewport assertions on RunHistoryPanel. |
| specs/suites/simulation-runs-page.feature | "Navigating to /simulations/:externalSetSlug/:batchId highlights batch in external set" | KEEP | `useSuiteRouting.unit.test.ts` covers parsing ("/simulations/setId/batchId returns external set with batch highlight"). Visual highlight behavior unbound. Phase 3. |
| specs/suites/simulation-runs-page.feature | "Yellow flash fades after a short duration" | KEEP | UI flash timeout behavior; no test bound. Phase 3 = vitest with fake timers asserting class/style removal after delay. |
| specs/suites/simulation-runs-page.feature | "Clicking a suite in the sidebar navigates to /simulations/run-plans/:slug" | KEEP | Covered by `SuiteUrlRouting.integration.test.tsx` "when clicking a suite in the sidebar — navigates with shallow routing" + `useSuiteRouting.unit.test.ts` "navigateToSuite is called with a suite slug — uses shallow router.push". Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "Clicking an external set in the sidebar navigates to /simulations/:setSlug" | KEEP | Covered by `useSuiteRouting.unit.test.ts` "navigateToSuite is called with an external set — uses shallow router.push with setId path" and `ExternalSetsSidebar.integration.test.tsx` "when an external set is clicked — calls onSelectSuite". Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "Clicking \"All Runs\" navigates to /simulations" | KEEP | Covered by `SuiteUrlRouting.integration.test.tsx` "when clicking All Runs in the sidebar — navigates with shallow routing" and `useSuiteRouting.unit.test.ts` "navigateToSuite is called with all-runs". Bind via JSDoc. |
| specs/suites/simulation-runs-page.feature | "SDK-generated URL lands on unified page with external set selected" | KEEP | Composite of routing + highlight; routing pieces tested. End-to-end SDK→URL flow not bound. Phase 3 = e2e or SDK-level test. |
| specs/suites/simulation-runs-page.feature | "Old suites URL with suite param redirects to new path" | KEEP | Redirect implemented in `pages/[project]/simulations/[[...path]].tsx:33-44` (handles `pathSegments[0] === "suites"` with `?suite=`). No test bound. Phase 3 integration test. |
| specs/suites/simulation-runs-page.feature | "Old suites URL with externalSet param redirects to new path" | KEEP | Same redirect handler, `?externalSet=` branch. No test bound. Phase 3. |
| specs/suites/simulation-runs-page.feature | "Old suites URL without params redirects to simulations root" | KEEP | Same redirect handler, fallback branch (`router.replace`/`/${projectSlug}/simulations`). No test bound. Phase 3. |
| specs/suites/simulation-runs-page.feature | "Old individual run URL redirects to unified page with drawer" | KEEP | Redirect implemented at `[[...path]].tsx:46-52` (3-segment, non-`run-plans` path). `?openRun=` query then opens drawer. No test bound. Phase 3 integration test. |
| specs/suites/simulation-runs-page.feature | "Save and Run from scenario drawer redirects to simulations page" | KEEP | `ScenarioFormDrawer` exposes Save and Run; redirect destination behavior unbound. Phase 3 integration test (mock router.push, assert call after onSaveAndRun). |
| specs/suites/simulation-runs-page.feature | "Initializing placeholder shown while run starts from suite detail" | KEEP | `RunHistoryPanel.tsx` references `pendingBatch` placeholder logic. No test bound. Phase 3 integration test. |
| specs/suites/suite-archiving.feature | "Archive a suite via the context menu" | KEEP | E2E for archive flow, behavior implemented (#1808). `SuiteContextMenu.integration.test.tsx` covers menu wiring and `SuiteArchiveDialog.integration.test.tsx` covers dialog. Bind via JSDoc on context menu integration test or add Playwright e2e in Phase 3. |
| specs/suites/suite-archiving.feature | "Archived suite runs remain visible in All Runs" | KEEP | Run-data side: `SuiteRepository.findAll` excludes archived but runs are stored separately. No test exercises the full path. Phase 3 integration test against AllRunsPanel + archive. |
| specs/suites/suite-archiving.feature | "Archive confirmation modal displays suite name" | KEEP | Covered by `SuiteArchiveDialog.integration.test.tsx` "displays the suite name". Bind via JSDoc. |
| specs/suites/suite-archiving.feature | "Archive confirmation modal explains preservation" | UPDATE | Spec text says "Archived suites will no longer appear in the sidebar. Test runs are preserved." but code now reads "Archived run plans will no longer appear in the sidebar. Test runs are preserved." (suite→run plan rename). Rewrite scenario to match actual copy, then bind to existing `SuiteArchiveDialog.integration.test.tsx` "explains that archiving preserves test runs". |
| specs/suites/suite-archiving.feature | "Archive confirmation modal has Cancel and Archive buttons" | KEEP | Covered by `SuiteArchiveDialog.integration.test.tsx` "has Cancel and Archive buttons". Bind via JSDoc. |
| specs/suites/suite-archiving.feature | "Cancel archive dismisses modal without archiving" | KEEP | Covered by `SuiteArchiveDialog.integration.test.tsx` "when Cancel is clicked — calls onClose / does not call onConfirm". Bind via JSDoc. |
| specs/suites/suite-archiving.feature | "Archived suite does not appear in sidebar search results" | KEEP | `SuiteSidebar.integration.test.tsx` covers search filtering on suites the parent component passes; archive-side filtering is upstream in `SuiteRepository.findAll`. No combined test. Phase 3 integration test. |
| specs/suites/suite-archiving.feature | "Archived suite is hidden from the active list" | KEEP | Covered by `suite.repository.unit.test.ts` "given an archived suite — returns null because archivedAt filter excludes it" and `suites-api.integration.test.ts` "excludes archived suites". Bind via JSDoc. |
| specs/suites/suite-archiving.feature | "Archived suite preserves associated test runs" | KEEP | Behavior preserved by storing runs in ClickHouse (separate from Postgres `Suite` archive). No specific assertion test — Phase 3 integration test against AllRunsPanel post-archive. |
| specs/suites/suite-archiving.feature | "Archiving frees up the suite name for reuse" | KEEP | Behavior implied by `SuiteRepository.archive` appending `--archived` suffix. Covered indirectly by `suite.repository.unit.test.ts` "preserves the original archivedAt timestamp / does not stack --archived suffixes". Phase 3 integration test for create-after-archive flow. |
| specs/suites/suite-archiving.feature | "Archiving an already-archived suite succeeds without error" | KEEP | Covered by `suite.repository.unit.test.ts` "given an already-archived suite — preserves the original archivedAt timestamp". Bind via JSDoc. |
| specs/suites/suite-archiving.feature | "Cannot archive a suite from a different project" | KEEP | Cross-project isolation enforced by `projectId` filter in `SuiteRepository.archive`. No explicit test — Phase 3 integration test. |
| specs/suites/suite-archiving.feature | "Archiving a non-existent suite returns not found" | KEEP | Covered by `suite.repository.unit.test.ts` "archive() given a non-existent suite — returns null without attempting update" and `suites-api.integration.test.ts` "DELETE /api/suites/:id — returns 404 for non-existent suite". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run succeeds when all scenarios exist" | KEEP | Covered by `suite.service.unit.test.ts` "given a suite with 3 scenarios, 2 targets, and repeat count 1 — delegates to suiteRunService". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run fails when a scenario does not exist" | KEEP | Covered by `suite.service.unit.test.ts` "given a suite references a deleted scenario — throws InvalidScenarioReferencesError". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run succeeds when HTTP target agent exists" | KEEP | Covered by `suite.service.unit.test.ts` "given a suite with an HTTP target referencing an existing agent — resolves via agentRepository and delegates". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run fails when HTTP target agent does not exist" | KEEP | Covered by `suite.service.unit.test.ts` "given a suite references a removed HTTP target — throws InvalidTargetReferencesError". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run succeeds when prompt config exists in project" | KEEP | Covered by `suite.service.unit.test.ts` "given a suite with a prompt target referencing an existing config — resolves via llmConfigRepository and delegates". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run succeeds when prompt config is org-scoped" | KEEP | Service uses `llmConfigRepository.findManyIncludingArchived` which is org-scoped. No specific org-scope assertion test — Phase 3 unit test covering org vs project resolution. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run fails when prompt config is soft-deleted" | KEEP | Covered by `suite.service.unit.test.ts` "given a suite with a deleted prompt target — throws InvalidTargetReferencesError (not AllTargetsArchivedError)". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run fails when prompt config belongs to another organization" | KEEP | Org-scoped LlmConfig query naturally excludes other-org configs (treated as missing → InvalidTargetReferencesError). No specific cross-org test — Phase 3 unit test. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run fails for targets with unknown type" | KEEP | Covered by `suite.service.unit.test.ts` "given a suite with a target of unknown type — throws during target parsing". Bind via JSDoc. |
| specs/suites/suite-run-dependency-refactor.feature | "Organization ID is resolved from project for suite run" | KEEP | Implemented in `suite.router.ts` via `ProjectRepository.getOrganizationId`. No router test. Phase 3 router-layer unit/integration test. |
| specs/suites/suite-run-dependency-refactor.feature | "Suite run fails when organization cannot be resolved" | KEEP | `suite.router.ts` throws `TRPCError NOT_FOUND` when `getOrganizationId` returns null. No router test. Phase 3. |
| specs/suites/suite-stale-prompt-references.feature | "Run validation accepts org-scoped prompt from another project" | KEEP | `LlmConfigRepository.findManyIncludingArchived` queries with `OR: [{ projectId }, { organizationId, scope: "ORGANIZATION" }]` (see `llm-config.repository.ts`). No specific cross-project assertion test. Phase 3 unit/integration test. |
| specs/suites/suite-stale-prompt-references.feature | "Run validation accepts project-scoped prompt from same project" | DUPLICATE | Equivalent to `suite-run-dependency-refactor.feature` "Suite run succeeds when prompt config exists in project" — already classified KEEP and bound to `suite.service.unit.test.ts` "given a suite with a prompt target referencing an existing config". Remove one of the two; cross-link to dependency-refactor.feature. |
| specs/suites/suite-stale-prompt-references.feature | "Run validation rejects prompt from unrelated project without org scope" | KEEP | Behavior emerges naturally from the `OR` query missing the prompt → InvalidTargetReferencesError. Logically covered by `suite.service.unit.test.ts` "given a suite with a deleted prompt target" but the cause differs (missing vs deleted). Phase 3 add explicit cross-project unit test. |
| specs/suites/suite-stale-prompt-references.feature | "Run validation rejects soft-deleted prompts" | DUPLICATE | Equivalent to `suite-run-dependency-refactor.feature` "Suite run fails when prompt config is soft-deleted" already KEEP-bound to `suite.service.unit.test.ts` "given a suite with a deleted prompt target". Remove this one; cross-link to dependency-refactor.feature. |
| specs/suites/suite-stale-prompt-references.feature | "Edit drawer warns about deleted prompt targets" | KEEP | `useSuiteForm.unit.test.ts` covers stale-target detection ("given stale target detection — returns the archived target with type info"). Bind via JSDoc; alternatively Phase 3 SuiteFormDrawer integration test. |
| specs/suites/suite-stale-prompt-references.feature | "Edit drawer shows no warning when all references are valid" | KEEP | Negative case covered by `useSuiteForm.unit.test.ts` "given stale target detection — returns empty archivedTargets". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Navigate to simulations page" | DUPLICATE | Same as `simulation-runs-page.feature` "Navigating to /simulations shows All Runs view" + "Sidebar shows \"Runs\" menu item without beta badge". Both already KEEP-classified. Remove this and cross-link. |
| specs/suites/suite-workflow.feature | "Empty state when no suites exist" | UPDATE | Spec says "Select a suite or create one"; actual UI text is "Select a run plan from the sidebar or create a new one" (`SuiteDetailPanel.tsx`). Already covered by `SuiteDetailPanel.integration.test.tsx` "displays the empty state message". Rewrite the spec text, then bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Open new suite drawer" | UPDATE | Spec says drawer title "New Suite" and button "+ New Suite"; actual code is "New Run Plan" and "+ New Run Plan" (`SuiteFormDrawer.tsx`, `SimulationsPage.tsx`). Update wording, then bind to `SuiteFormDrawer.integration.test.tsx` "displays the 'New Run Plan' title". |
| specs/suites/suite-workflow.feature | "Create a suite with name, scenarios, and targets" | KEEP | E2E for the create flow. Behavior implemented in #1421 / #1775. No e2e bound. Phase 3 = Playwright e2e or component-level integration test wiring picker → save → sidebar refresh. |
| specs/suites/suite-workflow.feature | "Drawer validates required fields" | UPDATE | Spec "+ New Suite" button label is outdated (now "+ New Run Plan"). Validation behavior covered by `SuiteFormDrawer.integration.test.tsx` "shows a name validation error" / scenarios / targets. Update wording, then bind. |
| specs/suites/suite-workflow.feature | "Typing in the name field clears its validation error" | UPDATE | Same outdated "+ New Suite" wording. Behavior covered by `SuiteFormDrawer.integration.test.tsx` "clears the name error but keeps scenario and target errors". Update + bind. |
| specs/suites/suite-workflow.feature | "Drawer validates at least one scenario selected" | UPDATE | Same outdated "+ New Suite" wording. Behavior covered by `SuiteFormDrawer.integration.test.tsx` "shows a scenarios validation error". Update + bind. |
| specs/suites/suite-workflow.feature | "Drawer validates at least one target selected" | UPDATE | Same outdated "+ New Suite" wording. Behavior covered by `SuiteFormDrawer.integration.test.tsx` "shows a targets validation error". Update + bind. |
| specs/suites/suite-workflow.feature | "Search scenarios in the drawer" | KEEP | Covered by `SuiteFormDrawer.integration.test.tsx` "given the scenario search box — when a search query is typed — filters the visible scenarios" + `useSuiteForm.unit.test.ts` "filters scenarios by name (case-insensitive)". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Filter scenarios by label in the drawer" | KEEP | Covered by `useSuiteForm.unit.test.ts` "when activeLabelFilter is set — filters scenarios by label". Bind via JSDoc; integration coverage may be partial — Phase 3 may add ScenarioPicker integration test. |
| specs/suites/suite-workflow.feature | "Select All and Clear scenarios" | KEEP | Covered by `useSuiteForm.unit.test.ts` "when selectAllScenarios is called — selects all filtered scenario ids" and "when clearScenarios is called — empties selectedScenarioIds". Counter wording from `TargetPicker.unit.test.tsx` "displays the selection count". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Search targets in the drawer" | KEEP | Covered by `useSuiteForm.unit.test.ts` "filters targets by name (case-insensitive)". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Target list shows type indicator" | KEEP | Covered by `TargetPicker.unit.test.tsx` "given a code agent target — displays the Code type label" (and HTTP/Prompt sibling cases). Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Set repeat count in execution options" | UPDATE | Spec asserts "3× trials" string; actual UI is `"3x"` plus a separate "trials" label (`SuiteDetailPanel.tsx`, `SuiteDetailPanel.integration.test.tsx` "displays '3x' in the trials stat"). Update wording, then bind to existing test. |
| specs/suites/suite-workflow.feature | "Repeat count appears in suite stats bar" | UPDATE | Same "3× trials" → "3x" trials wording mismatch. Update + bind to `SuiteDetailPanel.integration.test.tsx` "displays '3x' in the trials stat". |
| specs/suites/suite-workflow.feature | "Edit suite via header button" | UPDATE | Spec says drawer opens with title "Edit Suite"; actual is "Edit Run Plan" (`SuiteFormDrawer.tsx`). Edit wiring covered partly by `SuiteFormDrawer.integration.test.tsx` "displays the Edit Run Plan title" + pre-population tests. Update + bind. |
| specs/suites/suite-workflow.feature | "Edit suite via context menu" | KEEP | Covered by `SuiteContextMenu.integration.test.tsx` "displays Edit, Duplicate, and Archive actions" + "when Edit is clicked — calls onEdit and onClose". Drawer-open behavior is integration. Bind via JSDoc; may need an e2e for full flow. |
| specs/suites/suite-workflow.feature | "Context menu actions on sidebar suite item" | KEEP | Covered by `SuiteContextMenu.integration.test.tsx` "displays Edit, Duplicate, and Archive actions" + "does not display a Delete action". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Duplicate suite" | KEEP | E2E. Backend covered by `suite.service.unit.test.ts` "duplicate() — creates a new suite with '(copy)' appended" + supporting copy tests. Sidebar reflection unbound — Phase 3 e2e test. |
| specs/suites/suite-workflow.feature | "Run suite from sidebar button" | KEEP | Sidebar `Run` button covered by `SuiteSidebar.integration.test.tsx` "when the Run button is clicked on a suite — calls onRunSuite with the suite id". Full e2e (run history reflects new run) unbound. Phase 3 e2e. |
| specs/suites/suite-workflow.feature | "Run suite from header button" | KEEP | Header Run button covered by `SuiteDetailPanel.integration.test.tsx` "when Run button is clicked — calls onRun". Full run-history reflection unbound. Phase 3 e2e. |
| specs/suites/suite-workflow.feature | "Save and run from drawer" | KEEP | `SuiteFormDrawer` exposes Save and Run Now; backend behavior covered by SuiteService.run unit tests. End-to-end run-history reflection unbound. Phase 3. |
| specs/suites/suite-workflow.feature | "Suite run generates correct number of jobs" | DUPLICATE | Equivalent to `archived-scenario-exclusion.feature` "Job count reflects only active scenarios and targets" (both trace to `suite.service.unit.test.ts` "calculateJobCount() given 3 scenarios, 2 targets…"). Cross-link and remove this duplicate. |
| specs/suites/suite-workflow.feature | "Suite run respects repeat count" | KEEP | Covered by `suite.service.unit.test.ts` "calculateJobCount() given 2 scenarios, 1 target, and repeat count 3 — returns 6 jobs" + run() "given a suite with 2 scenarios, 1 target, and repeat count 3 — delegates with correct repeat count". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Suite run uses suite ID as setId" | KEEP | Covered indirectly by `suite-set-id.unit.test.ts` (resolves suite-namespaced setId) + `suite.service.unit.test.ts` `run()` calls. Bind via JSDoc to `suite-set-id.unit.test.ts`. |
| specs/suites/suite-workflow.feature | "Suite run validates scenario references before scheduling" | DUPLICATE | Same as `archived-scenario-exclusion.feature` "Deleted scenarios still cause validation errors" / `suite-run-dependency-refactor.feature` "Suite run fails when a scenario does not exist" — already KEEP-classified and bound to `suite.service.unit.test.ts` "given a suite references a deleted scenario". Remove + cross-link. |
| specs/suites/suite-workflow.feature | "Suite run validates target references before scheduling" | DUPLICATE | Same as `suite-run-dependency-refactor.feature` "Suite run fails when HTTP target agent does not exist" — already KEEP-classified and bound to `suite.service.unit.test.ts` "given a suite references a removed HTTP target". Remove + cross-link. |
| specs/suites/suite-workflow.feature | "Queue status returns counts for pending and active jobs" | DELETE | No `getQueueStatus`/queue-status endpoint exists in current code; pending UI uses single `pendingBatchRunId` placeholder, not aggregate counts. Aspirational/never-implemented. |
| specs/suites/suite-workflow.feature | "Queue status returns zero counts when no jobs are queued" | DELETE | Same as above — no queue-status implementation exists. Delete. |
| specs/suites/suite-workflow.feature | "Queue status banner appears when jobs are pending" | DELETE | No "X pending, Y running" banner exists in `RunHistoryPanel.tsx`. Implementation took a different approach (single placeholder per submission via `pendingBatchRunId`). Aspirational. Delete. |
| specs/suites/suite-workflow.feature | "Queue status banner disappears when all jobs complete" | DELETE | Same as above — banner does not exist. Delete. |
| specs/suites/suite-workflow.feature | "View run history after suite run completes" | KEEP | E2E view of suite header + run list. Header covered by `SuiteDetailPanel.integration.test.tsx`; run list by `RunHistoryPanel`/`RunRow` integration tests. Full e2e unbound. Phase 3 Playwright or stitched integration test. |
| specs/suites/suite-workflow.feature | "Run row shows timestamp, pass rate, and trigger type" | UPDATE | Trigger-type ("Manual") column is not implemented (no `triggerType` rendered in `RunRow`). Timestamp + pass rate covered by `RunRow.integration.test.tsx` "displays pass rate in metrics summary pill". Rewrite spec to drop the "Manual" trigger column or add the feature, then bind. |
| specs/suites/suite-workflow.feature | "Expand run to see scenario × target breakdown" | KEEP | Covered by `RunRow.integration.test.tsx` "when expanded — displays scenario x target rows". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Expanded run shows repeat trial counts" | KEEP | Trial counts derived by `computeRunHistoryTotals`; per-pair display in expanded `RunRow`. Indirectly covered by `RunRow.integration.test.tsx` and `RunMetricsSummary.integration.test.tsx`. Bind via JSDoc; Phase 3 may add explicit "100% (3/3)" assertion. |
| specs/suites/suite-workflow.feature | "Most recent run is expanded by default" | KEEP | Behavior referenced in PR `feat(suites): suite workflow` (#1421) commit "RunHistoryList: most recent expanded by default". No direct unit test surfaced — Phase 3 integration test. |
| specs/suites/suite-workflow.feature | "Collapse and expand run rows" | KEEP | Covered by `RunRow.integration.test.tsx` "when the header is clicked — calls onToggle" plus collapsed/expanded render checks. Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Click run row navigates to run detail page" | KEEP | Covered by `RunRow.integration.test.tsx` "when a scenario x target row is clicked — calls onScenarioRunClick with the scenario run data". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Filter run history by scenario" | KEEP | Covered by `RunHistoryFilters.integration.test.tsx` "when a scenario is selected from the Scenario filter — calls onFiltersChange with the selected scenarioId". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Filter run history by target" | KEEP | `RunHistoryFilters` exposes scenario + pass/fail filters; target filter is also wired in `RunsFilterUrlSync.integration.test.tsx`. Bind via JSDoc; Phase 3 may add explicit Target filter test. |
| specs/suites/suite-workflow.feature | "Filter run history by pass/fail status" | KEEP | Covered by `RunHistoryFilters.integration.test.tsx` "when Fail is selected from the Pass/Fail filter — calls onFiltersChange with passFailStatus set to fail" (+ Pass sibling). Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Sidebar shows pass count and recency for each suite" | KEEP | Covered by `SuiteSidebar.integration.test.tsx` "when a suite has all passing results — displays pass count / displays compact recency text" + `/2h ago/` regex match. Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Sidebar shows failure indicator when some fail" | KEEP | Covered by `SuiteSidebar.integration.test.tsx` "when a suite has some failures — displays pass count / displays pass rate reflecting failures". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Run history footer shows totals" | UPDATE | Implementation moved totals from a footer to the stats bar (header). `RunRow.integration.test.tsx` explicitly asserts "does not render a RunSummaryFooter when expanded/collapsed". Spec must be rewritten to describe stats-bar totals, not a footer. |
| specs/suites/suite-workflow.feature | "Search suites in the sidebar" | KEEP | Covered by `SuiteSidebar.integration.test.tsx` "when typing 'billing' in the search box — filters to only show Billing Edge". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Simulations page uses standard PageLayout header" | KEEP | Covered by `SuitesPageLayout.integration.test.tsx` "renders PageLayout.Header with a 'Simulations' heading" and "does not render a 'SUITES' section header in the sidebar". Bind via JSDoc. |
| specs/suites/suite-workflow.feature | "Simulations page sidebar fills available height below header" | KEEP | Layout/scroll behavior; partly covered by `SuitesPageLayout.integration.test.tsx`. Independent-scroll assertion is not explicitly tested. Phase 3 integration test (could use computed-style + bounded-height assertions). |
| specs/suites/suite-workflow.feature | "DashboardLayout wraps the page exactly once" | KEEP | Covered by `SuitesPageLayout.integration.test.tsx` "does not render DashboardLayout twice". Bind via JSDoc. |
