# specs/agents — unimplemented-tag audit manifest

Total unimplemented-tagged scenarios: 76
Classified: 76

| File | Scenario | Class | Rationale |
|------|----------|-------|-----------|
| specs/agents/agents-rest-api.feature | "List agents returns paginated non-archived agents" | DUPLICATE | Already covered by `langwatch/src/app/api/agents/__tests__/agents-rest-api.integration.test.ts` ("returns paginated non-archived agents", "excludes archived agents") |
| specs/agents/agents-rest-api.feature | "List agents with page and limit parameters" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("paginates with page and limit parameters") |
| specs/agents/agents-rest-api.feature | "List agents returns empty array for project with no agents" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns empty paginated response") |
| specs/agents/agents-rest-api.feature | "Create an agent with name, type, and config" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("creates an agent with name, type, and config") |
| specs/agents/agents-rest-api.feature | "Create an agent enforces plan limits" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 403 Forbidden") under describe "when the project has reached its agent plan limit" |
| specs/agents/agents-rest-api.feature | "Create an agent validates config against type schema" | KEEP | `app.ts` has `createAgentSchema` and `validateConfig` in `agent.repository.ts` but no explicit integration test for invalid-config-against-type-schema; closest existing test is "returns 422 when type is invalid" which checks type enum, not config schema |
| specs/agents/agents-rest-api.feature | "Create an agent requires a name" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 422 when name is missing") |
| specs/agents/agents-rest-api.feature | "Create an agent requires a type" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 422 when type is missing") |
| specs/agents/agents-rest-api.feature | "Get an agent by id" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns agent details by id") |
| specs/agents/agents-rest-api.feature | "Get agent returns 404 for non-existent id" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 404 for non-existent agent") under GET /api/agents/:id |
| specs/agents/agents-rest-api.feature | "Update an agent name" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("updates agent name") |
| specs/agents/agents-rest-api.feature | "Update an agent config" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("updates agent config") |
| specs/agents/agents-rest-api.feature | "Update a non-existent agent returns 404" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 404 for non-existent agent") under PATCH /api/agents/:id |
| specs/agents/agents-rest-api.feature | "Delete an agent archives it" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("archives the agent and returns archivedAt", "makes the agent inaccessible via GET after deletion") |
| specs/agents/agents-rest-api.feature | "Delete a non-existent agent returns 404" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 404 for non-existent agent") under DELETE /api/agents/:id |
| specs/agents/agents-rest-api.feature | "Request without API key returns 401" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 401 without X-Auth-Token header") |
| specs/agents/agents-rest-api.feature | "Request with invalid API key returns 401" | DUPLICATE | Already covered by `agents-rest-api.integration.test.ts` ("returns 401 with invalid X-Auth-Token") |
| specs/agents/agent-management.feature | "Agent types available" | UPDATE | `AgentTypeSelectorDrawer.tsx` exposes three types (http, code, workflow), not the two listed in the table; scenario must be rewritten to reflect HTTP option |
| specs/agents/agent-management.feature | "Create code-based agent" | KEEP | `AgentCodeEditorDrawer` exists and integration test covers basic save (`AgentCodeEditorDrawer.integration.test.tsx`) but no end-to-end "from agents page click New Agent → choose Code Agent → save → appears in list" scenario binds |
| specs/agents/agent-management.feature | "Create workflow-based agent" | UPDATE | Workflow agents are now created via `WorkflowSelectorDrawer.tsx` which creates a NEW workflow from the Blank template, not selecting an existing one as the scenario describes |
| specs/agents/agent-management.feature | "View agents list" | KEEP | `pages/[project]/agents.tsx` renders agent list with name/type via `AgentCard`; no scenario test currently bound |
| specs/agents/agent-management.feature | "Empty state when no agents" | UPDATE | Empty state on `pages/[project]/agents.tsx` exists but copy is "No agents yet" not "Create your first agent" CTA exactly as worded; needs minor wording reconciliation before binding |
| specs/agents/agent-management.feature | "Agents are project-scoped" | KEEP | `agents.getAll` in `server/api/routers/agents.ts` filters by `projectId`; no scenario binding ties cross-project isolation to a test |
| specs/agents/agent-management.feature | "Edit code-based agent" | KEEP | `AgentCodeEditorDrawer` supports editing via `agentId` prop; `getAgentEditorDrawer.unit.test.ts` exists but no scenario JSDoc bound |
| specs/agents/agent-management.feature | "Edit workflow-based agent" | UPDATE | `AgentWorkflowEditorDrawer.tsx` opens for workflow agents but does not let user "select a different workflow" — the workflow link is fixed (created with the agent), so scenario premise is wrong |
| specs/agents/agent-management.feature | "Archive agent" | KEEP | `agents.softDelete` mutation in `routers/agents.ts` and `CascadeArchiveDialog` on agents page; no scenario test bound |
| specs/agents/agent-management.feature | "Archived agents are excluded from list" | DUPLICATE | Already covered by `agent.repository.unit.test.ts` ("queries with archivedAt: null") and the REST API archived-exclusion test |
| specs/agents/agent-management.feature | "Code agent config stored as JSON" | KEEP | `agent.repository.ts` stores config as JSON validated against `codeComponentSchema`; no scenario binding documents the storage shape |
| specs/agents/agent-management.feature | "Workflow agent has workflowId at top level" | KEEP | `agents.ts` router writes `workflowId` as a top-level Prisma column (see `data.workflowId` in create); no scenario binds this top-level join contract |
| specs/agents/agent-management.feature | "AgentListDrawer shows available agents" | DUPLICATE | Already covered by `AgentListDrawer.test.tsx` ("shows agent list with all agents", "shows New Agent button at top") |
| specs/agents/agent-management.feature | "AgentListDrawer empty state" | DUPLICATE | Already covered by `AgentListDrawer.test.tsx` ("shows empty message when no agents", "shows create first agent button in empty state") |
| specs/agents/agent-management.feature | "Select agent from drawer" | DUPLICATE | Already covered by `AgentListDrawer.test.tsx` ("calls onSelect when clicking an agent", "closes drawer after selection") |
| specs/agents/agent-management.feature | "Create new agent from drawer flow" | DUPLICATE | Already covered by `useAgentPickerFlow.unit.test.ts` ("opens agentTypeSelector", "wires onSave to update the pending node with agent data and select it") |
| specs/agents/agent-management.feature | "AgentTypeSelectorDrawer shows two options" | UPDATE | `AgentTypeSelectorDrawer.tsx` shows three options (http, code, workflow); scenario premise of "two options" is wrong and `AgentTypeSelectorDrawer.test.tsx` already asserts the actual behavior |
| specs/agents/agent-management.feature | "Selecting type navigates to appropriate editor" | DUPLICATE | Already covered by `AgentTypeSelectorDrawer.test.tsx` ("opens code editor when clicking Code Agent", "opens workflow selector when clicking Workflow Agent") |
| specs/agents/agent-management.feature | "WorkflowSelectorDrawer lists project workflows" | UPDATE | `WorkflowSelectorDrawer.tsx` no longer lists existing workflows — it is now a creation form that always builds a Blank template; scenario describes outdated selection behavior |
| specs/agents/agent-management.feature | "WorkflowSelectorDrawer empty state" | UPDATE | Same as above — drawer no longer has an empty/list state; scenario obsolete and needs rewrite to describe the create-form |
| specs/agents/agent-management.feature | "New Workflow button navigates to workflows page" | DELETE | `WorkflowSelectorDrawer.tsx` has no "+ New Workflow" button to a workflows page; the drawer itself IS the new-workflow creator now |
| specs/agents/create-workflow-agent.feature | "Workflow agent option description updated" | KEEP | `AgentTypeSelectorDrawer.tsx` shows Workflow Agent with "Create a new workflow for custom agent logic" description and `Workflow` lucide icon; describes existing UI but no scenario binding |
| specs/agents/create-workflow-agent.feature | "Workflow agent option opens workflow creator" | UPDATE | Selecting Workflow Agent opens `WorkflowSelectorDrawer` (a drawer) not a "New Workflow modal" with a Blank-template-pre-selected step 2; scenario describes a different UI |
| specs/agents/create-workflow-agent.feature | "After creating workflow, agent is auto-created" | KEEP | `WorkflowSelectorDrawer.tsx` creates workflow + agent linked together and routes to studio (`router.push` after success); no scenario test bound |
| specs/agents/create-workflow-agent.feature | "Agent inherits workflow name automatically" | KEEP | `WorkflowSelectorDrawer.tsx` form has a single `name` field used for BOTH workflow and agent on submission; no scenario binding |
| specs/agents/create-workflow-agent.feature | "Workflow is created with blank template structure" | KEEP | `WorkflowSelectorDrawer.tsx` imports `blankTemplate` from `~/optimization_studio/templates/blank` and uses it on create; behavior exists, no test bound |
| specs/agents/create-workflow-agent.feature | "Canceling workflow creation does not create agent" | KEEP | `WorkflowSelectorDrawer.tsx` has explicit close/back handlers that do not mutate; no scenario binding |
| specs/agents/create-workflow-agent.feature | "Workflow creation failure does not create orphan agent" | KEEP | `WorkflowSelectorDrawer.tsx` uses transactional mutation flow (workflow then agent); no scenario binding for the rollback path |
| specs/agents/create-workflow-agent.feature | "Code agent flow remains unchanged" | DUPLICATE | Already covered by `AgentTypeSelectorDrawer.test.tsx` ("opens code editor when clicking Code Agent") |
| specs/agents/create-workflow-agent.feature | "HTTP agent flow remains unchanged" | KEEP | `AgentTypeSelectorDrawer.tsx` includes HTTP option opening `agentHttpEditor` but `AgentTypeSelectorDrawer.test.tsx` does not assert HTTP flow specifically |
| specs/agents/create-workflow-agent.feature | "New agent appears in list after creation" | KEEP | `pages/[project]/agents.tsx` re-fetches via `agents.getAll.invalidate` after mutation; no scenario binding for the post-create list refresh |
| specs/agents/http-agent-tracing.feature | "Test execution creates a trace visible on the Traces page" | KEEP | `httpProxyTracing.ts` calls `scheduleTraceCollectionWithFallback` and `useHttpTest` triggers it from the drawer; integration covers trace creation but no e2e binding for "visible on the Traces page" UI assertion |
| specs/agents/http-agent-tracing.feature | "Trace includes agent_test type" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("creates a trace with type agent_test") |
| specs/agents/http-agent-tracing.feature | "Trace includes agent ID" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("includes agent ID in trace metadata") |
| specs/agents/http-agent-tracing.feature | "Trace includes project ID" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("includes project ID in trace") |
| specs/agents/http-agent-tracing.feature | "Trace includes user ID" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("includes user ID in trace metadata") |
| specs/agents/http-agent-tracing.feature | "Trace captures request URL and method" | KEEP | `httpProxyTracing.ts` `buildTraceTestContext` returns `{url, method}`; existing tests check trace creation but no specific assertion on URL+method in test_context |
| specs/agents/http-agent-tracing.feature | "Trace captures request body" | KEEP | `httpProxy.ts` passes body to trace flow; no integration test currently asserts the request body in the captured trace |
| specs/agents/http-agent-tracing.feature | "Trace captures output path when configured" | KEEP | `buildTraceTestContext` conditionally includes `output_path`; integration tests cover output extraction but not the test_context output_path field assertion |
| specs/agents/http-agent-tracing.feature | "Trace captures response status code" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("captures response status code") |
| specs/agents/http-agent-tracing.feature | "Trace captures response duration" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("captures request duration") |
| specs/agents/http-agent-tracing.feature | "Trace captures response body" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("captures response body") |
| specs/agents/http-agent-tracing.feature | "Trace captures extracted output" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("captures extracted output when output path configured") |
| specs/agents/http-agent-tracing.feature | "Trace captures HTTP error responses" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("captures the error response in the trace") under "when endpoint returns an error" |
| specs/agents/http-agent-tracing.feature | "Trace captures connection failures" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("captures the connection error in the trace") under "when endpoint is unreachable" |
| specs/agents/http-agent-tracing.feature | "Bearer token credentials are redacted from trace" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("redacts the bearer token from the trace") |
| specs/agents/http-agent-tracing.feature | "API key credentials are redacted from trace" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("redacts the API key value from the trace") |
| specs/agents/http-agent-tracing.feature | "Basic auth credentials are redacted from trace" | DUPLICATE | Already covered by `httpProxyTracing.integration.test.ts` ("redacts the username and password from the trace") |
| specs/agents/http-agent-tracing.feature | "Authorization headers are redacted in captured request headers" | KEEP | `httpProxyTracing.ts` `sanitizeHeadersForTrace` redacts Authorization header; logic is unit-testable but no current test binds the captured-headers assertion |
| specs/agents/http-agent-tracing.feature | "Custom auth headers are redacted in captured request headers" | KEEP | `sanitizeHeadersForTrace` accepts `customAuthHeaderName` and redacts; no test currently asserts custom-header redaction in captured headers |
| specs/agents/http-agent-tracing.feature | "Filter traces by agent_test type" | KEEP | Trace router (`server/api/routers/traces.ts`) has no filter for `customMetadata.type` = "agent_test"; the trace TYPE is created but UI/API filter does not exist yet, intended behavior per feature header |
| specs/agents/workflow-agent-editor.feature | "Configure button for workflow agent shows workflow link" | UPDATE | Agent card uses an "Edit" button (not "Configure") that opens `AgentWorkflowEditorDrawer.tsx`; the studio link is labeled "Open editor" not "Edit Workflow" — wording mismatch |
| specs/agents/workflow-agent-editor.feature | "Code agent does not show workflow link" | UPDATE | `AgentCard.tsx` uses "Edit" not "Configure"; behavior is correct (code editor has no workflow link) but scenario verb mismatches actual UI |
| specs/agents/workflow-agent-editor.feature | "HTTP agent does not show workflow link" | UPDATE | Same wording issue: `AgentCard.tsx` "Edit" not "Configure"; behavior holds (HTTP drawer has no studio link) but scenario needs verb fix |
| specs/agents/workflow-agent-editor.feature | "Click Edit Workflow navigates to studio" | UPDATE | The link in `AgentWorkflowEditorDrawer.tsx` is labeled "Open editor" with `data-testid="open-workflow-editor-link"`, target=_blank — not "Edit Workflow"; needs rename or scenario rewrite |
| specs/agents/workflow-agent-editor.feature | "Workflow link includes correct workflow ID" | KEEP | `editorHref` in `AgentWorkflowEditorDrawer.tsx` is `/${project.slug}/studio/${workflowId}`; behavior exists but no scenario binding asserts ID interpolation |
| specs/agents/workflow-agent-editor.feature | "Workflow agent shows name and workflow selection" | UPDATE | Drawer shows agent name field and linked workflow name as text plus "Open editor" link, not an "Edit Workflow" button; scenario needs rewording |
| specs/agents/workflow-agent-editor.feature | "Can rename workflow agent without changing workflow" | KEEP | `AgentWorkflowEditorDrawer.tsx` supports updating name via `agents.update` and the workflow remains linked; no scenario binding |
| specs/agents/workflow-agent-editor.feature | "Workflow agent with deleted workflow shows warning" | KEEP | `AgentWorkflowEditorDrawer.tsx` line 383 falls back to `"(workflow not found)"` when `workflowQuery.data?.name` is undefined but does not show an explicit warning or disable the link — partially implemented; classify KEEP as the described behavior is the spec target |
