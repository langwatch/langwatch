# specs/home — unimplemented audit manifest

Total unimplemented scenarios: 93
Classified: 93

| File | Scenario | Class | Rationale |
|------|----------|-------|-----------|
| specs/home/welcome-header.feature | "Displays greeting with user's first name" | UPDATE | `WelcomeHeader.tsx` renders "Good morning/afternoon/evening, John" via `getGreeting`, never the literal "Hello, John" the spec asserts |
| specs/home/welcome-header.feature | "Extracts first name from full name" | KEEP | `getGreetingName` in `langwatch/src/components/home/WelcomeHeader.tsx` returns "Jane" from "Jane Maria Smith"; covered by unit test but not @scenario-bound |
| specs/home/welcome-header.feature | "Displays friendly fallback when name unavailable" | UPDATE | Implementation falls back to "Good morning"/"Good afternoon"/"Good evening" via `getGreeting` (anonymous branch) — not the literal "Hello 👋" the spec asserts |
| specs/home/welcome-header.feature | "Displays friendly fallback when name is just email" | UPDATE | `getGreetingName` returns null for emails so the anonymous greeting "Good morning" renders, not "Hello 👋" |
| specs/home/learning-resources.feature | "Displays documentation link" | KEEP | `langwatch/src/components/home/LearningResources.tsx` renders a "View documentation" CTA in the resources list; no test bound |
| specs/home/learning-resources.feature | "Displays video link" | KEEP | `LearningResources.tsx` renders a "Watch videos" CTA pointing at YouTube; no test bound |
| specs/home/learning-resources.feature | "Documentation link points to docs site" | KEEP | `resources[0].href` in `LearningResources.tsx` is "https://docs.langwatch.ai" — contains "docs.langwatch.ai"; no test bound |
| specs/home/learning-resources.feature | "Video link points to YouTube" | KEEP | `resources[1].href` in `LearningResources.tsx` is "https://www.youtube.com/@LangWatch/videos" — contains "youtube.com/@LangWatch"; no test bound |
| specs/home/learning-resources.feature | "Documentation click is tracked" | DELETE | `LearningResources.tsx` has no analytics call; grep for "documentation_click" finds only the unused `welcome/ResourcesCard.tsx`, not the home `LearningResources` |
| specs/home/learning-resources.feature | "Video click is tracked" | DELETE | `LearningResources.tsx` does not emit any "video_click" tracking event; no analytics import or `trackEvent` call in the component |
| specs/home/learning-resources.feature | "Section has descriptive title" | KEEP | `LearningResources.tsx` renders `<Heading>Learning resources</Heading>`; no test bound |
| specs/home/learning-resources.feature | "Card has appropriate styling" | KEEP | `ResourceCardItem` in `LearningResources.tsx` applies `borderRadius`, `background`, `padding` styles per resource; no test bound |
| specs/home/quick-access-links.feature | "Displays 4 feature cards" | KEEP | `buildQuickAccessCards` in `langwatch/src/components/home/QuickAccessLinks.tsx` returns 4 entries (Observability, Agent Simulations, Prompt Management, Evaluations); no test bound |
| specs/home/quick-access-links.feature | "Observability card links to setup when not integrated" | UPDATE | `QuickAccessLinks.tsx` Observability `href` is `/${slug}/messages` in BOTH integrated and non-integrated branches (ternary collapses to same value); spec expects `/test-project/setup` which is not produced |
| specs/home/quick-access-links.feature | "Observability card links to messages when integrated" | KEEP | `QuickAccessLinks.tsx` `buildQuickAccessCards` integrated branch returns href `/${projectSlug}/messages`; no test bound |
| specs/home/quick-access-links.feature | "Observability card has docs link" | KEEP | `docsHref` for Observability is "https://langwatch.ai/docs/integration/quick-start" — contains "docs" and "integration"; no test bound |
| specs/home/quick-access-links.feature | "Agent Simulations card links to simulations" | KEEP | `buildQuickAccessCards` Agent Simulations href is `/${projectSlug}/simulations` in `QuickAccessLinks.tsx`; no test bound |
| specs/home/quick-access-links.feature | "Agent Simulations card has docs link" | KEEP | Agent Simulations `docsHref` is "https://langwatch.ai/docs/agent-simulations/getting-started" — contains "docs" and "simulations"; no test bound |
| specs/home/quick-access-links.feature | "Prompt Management card links to prompts" | KEEP | Prompt Management href is `/${projectSlug}/prompts` in `QuickAccessLinks.tsx`; no test bound |
| specs/home/quick-access-links.feature | "Prompt Management card has docs link" | KEEP | Prompt Management `docsHref` is "https://langwatch.ai/docs/prompt-management/overview" — contains "docs" and "prompt"; no test bound |
| specs/home/quick-access-links.feature | "Evaluations card links to evaluations" | KEEP | Evaluations href is `/${projectSlug}/evaluations` in `QuickAccessLinks.tsx`; no test bound |
| specs/home/quick-access-links.feature | "Evaluations card has docs link" | KEEP | Evaluations `docsHref` is "https://langwatch.ai/docs/llm-evaluation" — contains "docs" and "evaluation"; no test bound |
| specs/home/quick-access-links.feature | "Card click is tracked" | DELETE | `QuickAccessLinks.tsx` has no `useAnalytics`/`emit`/`trackEvent` call on the card — clicks are plain `NextLink` navigations; "quick_access_click" event is not emitted anywhere |
| specs/home/quick-access-links.feature | "Cards are arranged in responsive grid" | KEEP | `QuickAccessLinks.tsx` wraps cards in a Chakra `<Grid templateColumns={{base:"1fr", sm:"repeat(2,1fr)", lg:"repeat(4,1fr)"}}>`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Hides component when all steps complete" | KEEP | `OnboardingProgress.tsx` returns `null` when `completionPercentage === 100` (line 263); no test bound |
| specs/home/onboarding-progress-ui.feature | "Shows component when steps remain" | KEEP | `OnboardingProgress.tsx` renders the `HomeCard` whenever `allComplete` is false and data is loaded; no test bound |
| specs/home/onboarding-progress-ui.feature | "Shows progress percentage - 17%" | UPDATE | Implementation has 9 steps (including inviteTeamMembers, setupModelProviders, createPrompt) so 1/9 yields 11%, never 17%; the 17% figure assumes the spec's outdated 6-step list |
| specs/home/onboarding-progress-ui.feature | "Shows progress percentage - 50%" | UPDATE | With 9 steps, 50% is unreachable (4/9=44, 5/9=56); the spec's 50% target reflects the outdated 6-step model in the feature file |
| specs/home/onboarding-progress-ui.feature | "Completed steps show checkmark indicator" | KEEP | `StepItem` in `OnboardingProgress.tsx` shows `<LuCheck>` icon and green tint when `step.complete`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Current step is identifiable" | DELETE | `OnboardingProgress.tsx` has no notion of a "current" step — incomplete steps share identical styling; no `isCurrent` flag exists in `OnboardingStep` type or `StepItem` |
| specs/home/onboarding-progress-ui.feature | "Current step shows action button" | DELETE | `StepItem` renders only an icon plus title text; no per-step "Start"/action button is rendered, the whole row is the click target |
| specs/home/onboarding-progress-ui.feature | "Future steps show as pending" | KEEP | Incomplete steps render with non-strikethrough text and the step-specific icon (not a check) in `StepItem`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Each step shows title" | KEEP | `StepItem` always renders `<Text>{step.title}</Text>` from the `buildOnboardingSteps` output; no test bound |
| specs/home/onboarding-progress-ui.feature | "Clicking syncFirstMessage step navigates to messages page" | KEEP | `handleStepClick` in `OnboardingProgress.tsx` calls `router.push(step.href)` and `syncFirstMessage` href is `/${slug}/messages`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Clicking createWorkflow step navigates to workflows page" | KEEP | `createWorkflow` href is `/${projectSlug}/workflows` in `buildOnboardingSteps`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Clicking createSimulation step navigates to simulations page" | KEEP | `createSimulation` href is `/${projectSlug}/simulations`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Clicking setupEvaluation step navigates to evaluations page" | KEEP | `setupEvaluation` href is `/${projectSlug}/evaluations`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Clicking createDataset step navigates to datasets page" | KEEP | `createDataset` href is `/${projectSlug}/datasets`; no test bound |
| specs/home/onboarding-progress-ui.feature | "Can view completed steps" | DELETE | `OnboardingProgress.tsx` shows all 9 steps simultaneously in columns; there is no paginated "view completed" navigation feature in the component |
| specs/home/onboarding-progress-ui.feature | "Can view future pending steps" | DELETE | No pagination/view-toggle UI in `OnboardingProgress.tsx` — all steps render at once in a `<Grid>` so "navigate to view future steps" is not a feature that exists |
| specs/home/onboarding-progress-ui.feature | "Can navigate back to current step from completed steps view" | DELETE | Predicates a stepper navigation feature that does not exist; component renders all steps in a static grid layout |
| specs/home/onboarding-progress-ui.feature | "Displays vertical stepper layout" | UPDATE | `OnboardingProgress.tsx` lays out steps in a horizontal `<Grid templateColumns>` of columns of 3, not a single vertical stepper; the spec phrasing does not match implementation |
| specs/home/onboarding-progress-ui.feature | "Shows progress bar" | KEEP | `OnboardingProgress.tsx` renders `<Progress.Root value={completionPercentage} colorPalette="orange">` (lines 302-310); no test bound |
| specs/home/onboarding-progress-ui.feature | "Shows loading state while fetching" | UPDATE | Component returns `null` while `isLoading || !checkStatus || !project` — no skeleton or spinner is rendered for the onboarding section, so "loading indicator" is not what the implementation shows |
| specs/home/onboarding-progress-backend.feature | "Returns all steps with completion status" | UPDATE | `OnboardingChecksService.getCheckStatus` returns counts (workflows/datasets/evaluations/simulations/modelProviders/prompts/teamMembers/firstMessage/integrated), not a `steps` array; the 6-step list in the spec also omits the real inviteTeamMembers/setupModelProviders/createPrompt steps |
| specs/home/onboarding-progress-backend.feature | "Step createProject is always complete" | UPDATE | The "always complete" rule lives in `buildOnboardingSteps` (UI), not in the backend service — the API returns no `createProject` field at all; spec assigns this responsibility to the wrong layer |
| specs/home/onboarding-progress-backend.feature | "Step syncFirstMessage is incomplete when project has no firstMessage" | UPDATE | API exposes raw `firstMessage: boolean`, not a `syncFirstMessage` step status; spec's "step X should be incomplete" phrasing does not match the actual response shape |
| specs/home/onboarding-progress-backend.feature | "Step syncFirstMessage is complete when project has firstMessage" | UPDATE | Same shape mismatch — API returns `firstMessage` boolean directly via `OnboardingChecksService.getCheckStatus`; the step abstraction is UI-only |
| specs/home/onboarding-progress-backend.feature | "Step createWorkflow is incomplete when no workflows exist" | UPDATE | Service returns `workflows: number` from `prisma.project.findUnique({ include: { workflows: { take: 1 } } })`; no `createWorkflow` step is exposed |
| specs/home/onboarding-progress-backend.feature | "Step createWorkflow is complete when at least one workflow exists" | UPDATE | Same shape mismatch — service returns `workflows` count, UI maps `(data.workflows ?? 0) > 0` to step completion |
| specs/home/onboarding-progress-backend.feature | "Step createWorkflow ignores archived workflows" | KEEP | `OnboardingChecksService.getCheckStatus` includes `workflows: { where: { archivedAt: null } }`; behavior is real but the "step" framing needs the same UPDATE pass — keeping because the underlying invariant is correct and worth a test |
| specs/home/onboarding-progress-backend.feature | "Step createSimulation is incomplete when no simulations exist" | UPDATE | Service returns `simulations: number` (1 if any scenario set exists, 0 otherwise) via `getSimulationsCount`; no `createSimulation` step on the API response |
| specs/home/onboarding-progress-backend.feature | "Step createSimulation is complete when at least one simulation exists" | UPDATE | Same shape — `simulations` count from ClickHouse is returned, not a step boolean |
| specs/home/onboarding-progress-backend.feature | "Step setupEvaluation is incomplete when no evaluations exist" | UPDATE | Spec says "0 monitors" but service measures `experiments` (Prisma) via `prisma.project.findUnique({ include: { experiments: { take: 1 } } })`, exposed as `evaluations` count — not a step nor "monitors" |
| specs/home/onboarding-progress-backend.feature | "Step setupEvaluation is complete when at least one evaluation exists" | UPDATE | Same — service returns `evaluations: number` derived from `experiments`, not a `setupEvaluation` step boolean and not from the `Monitor` model |
| specs/home/onboarding-progress-backend.feature | "Step createDataset is incomplete when no datasets exist" | UPDATE | Service returns `datasets: number` (with archivedAt null filter); no `createDataset` step boolean on the API response |
| specs/home/onboarding-progress-backend.feature | "Step createDataset is complete when at least one dataset exists" | UPDATE | Same — `datasets` is a count, the step abstraction is built UI-side in `buildOnboardingSteps` |
| specs/home/onboarding-progress-backend.feature | "Returns overall completion percentage - none complete" | UPDATE | Backend never returns a percentage — `calculateCompletionPercentage` lives in `OnboardingProgress.tsx`; with 9 UI steps, 1/9 (createProject always complete) = 11%, not 17% |
| specs/home/onboarding-progress-backend.feature | "Returns overall completion percentage - all complete" | UPDATE | Backend returns no percentage; the 100% calculation is UI-only and requires all 9 steps complete (spec only checks 5 fields) |
| specs/home/onboarding-progress-backend.feature | "Returns overall completion percentage - half complete" | UPDATE | Backend returns counts not percentage; with 9 steps, exact 50% is unreachable — this scenario predicates the spec's outdated 6-step model |
| specs/home/onboarding-progress-backend.feature | "Returns allComplete false when steps remain" | UPDATE | Grep across `langwatch/src` shows no `allComplete` field returned by `integrationsChecks.getCheckStatus`; flag is computed locally in `OnboardingProgress.tsx` from the percentage |
| specs/home/onboarding-progress-backend.feature | "Returns allComplete true when all steps done" | UPDATE | Same — no `allComplete` boolean exposed by the backend; the UI derives it from `completionPercentage === 100` |
| specs/home/recent-items-ui.feature | "Displays loading skeleton while fetching" | KEEP | `RecentItemsSection.tsx` renders `<RecentItemsSkeleton />` while `isLoading` is true (line 299); no test bound |
| specs/home/recent-items-ui.feature | "Displays empty state when no recent items" | UPDATE | `RecentItemsSection.tsx` returns `null` when `recentItems?.length === 0` (line 259); the `_RecentItemsEmptyState` component is defined but unused (underscore prefix) — no empty state message is shown |
| specs/home/recent-items-ui.feature | "Displays grid of item cards" | KEEP | `RecentItemsGrid` renders one `RecentItemCard` per item in `<Grid templateColumns={{base:"1fr", md:"repeat(2,1fr)", lg:"repeat(3,1fr)"}}>`; no test bound |
| specs/home/recent-items-ui.feature | "Each card shows entity name" | KEEP | `RecentItemCard` renders `<Text lineClamp={1}>{item.name}</Text>`; no test bound |
| specs/home/recent-items-ui.feature | "Each card shows relative time" | KEEP | `RecentItemCard` calls `formatTimeAgo(item.updatedAt.getTime())` and renders the result; no test bound |
| specs/home/recent-items-ui.feature | "Clicking card navigates to entity deep link" | KEEP | `RecentItemCard` wraps content in `<NextLink href={item.href}>`, so clicking navigates to the supplied href; no test bound |
| specs/home/recent-items-ui.feature | "Recents tab is selected by default" | KEEP | `<Tabs.Root defaultValue="recents">` in `RecentItemsSection.tsx` (line 266); no test bound |
| specs/home/recent-items-ui.feature | "Recents tab shows items ordered by date" | KEEP | Items arrive ordered by `createdAt: desc` from `RecentItemsRepository.getRecentAuditLogEntries` and the UI renders them in that order; no test bound |
| specs/home/recent-items-ui.feature | "By type tab groups items by entity type" | KEEP | `<Tabs.Trigger value="by-type">` activates `GroupedItemsView` which uses `groupItemsByType`; covered by unit test on the helper but not @scenario-bound |
| specs/home/recent-items-ui.feature | "Handles API error gracefully" | KEEP | `RecentItemsSection.tsx` lines 301-321 render an error card with "Failed to load recent items" and a "Retry" button when `error` is truthy; no test bound |
| specs/home/recent-items-ui.feature | "Retry button refetches data" | KEEP | The error block's onClick calls `void refetch()` (line 315); no test bound |
| specs/home/recent-items-ui.feature | "Card click is tracked" | DELETE | `RecentItemCard` has no analytics emit on click; grep for "recent_item_click" returns zero matches across `langwatch/src` |
| specs/home/recent-items-backend.feature | "Returns items from AuditLog filtered by user and project" | KEEP | `RecentItemsRepository.getRecentAuditLogEntries` queries `prisma.auditLog.findMany({ where: { userId, projectId, OR: actionConditions } })`; no @scenario-bound test |
| specs/home/recent-items-backend.feature | "Extracts prompt IDs from prompts.update actions" | KEEP | `ACTION_TO_TYPE_MAP["prompts."]` = "prompt", `ENTITY_ID_EXTRACTORS.prompt = (args) => args.configId`; existing test "returns recent prompt from audit log" uses `prompts.update` but not @scenario-bound |
| specs/home/recent-items-backend.feature | "Extracts prompt IDs from prompts.create actions" | KEEP | Action prefix `"prompts."` covers both `prompts.update` and `prompts.create`, both routed through the same `configId` extractor; no test bound |
| specs/home/recent-items-backend.feature | "Extracts workflow IDs from workflow.update actions" | UPDATE | The hydrated `RecentItem` type has no `icon` field — `RecentItem` is `{ id, type, name, href, updatedAt }`; the spec's expected `icon: 🔄` is not returned by the service |
| specs/home/recent-items-backend.feature | "Extracts workflow IDs from workflow.create actions" | UPDATE | Same icon mismatch — `RecentItemsService.hydrateEntity` returns no `icon` from the workflow; only id/type/name/href/updatedAt are populated |
| specs/home/recent-items-backend.feature | "Extracts dataset IDs from dataset.update actions" | KEEP | `ACTION_TO_TYPE_MAP["dataset."]` and `ENTITY_ID_EXTRACTORS.dataset = (args) => args.datasetId` work as described; existing dataset test exists but is not @scenario-bound |
| specs/home/recent-items-backend.feature | "Extracts dataset IDs from dataset.create actions" | KEEP | Action prefix `"dataset."` covers both create and update via the same extractor; no test bound |
| specs/home/recent-items-backend.feature | "Extracts evaluation IDs from monitors.update actions" | KEEP | `ACTION_TO_TYPE_MAP["monitors."]` = "evaluation" and the extractor reads `args.checkId ?? args.monitorId` — matches the spec's checkId form; no test bound |
| specs/home/recent-items-backend.feature | "Extracts annotation queue IDs from annotation.createQueue actions" | UPDATE | The `annotation.createQueue` tRPC procedure does not exist — the actual path is `annotation.createQueueItem` (only one matching procedure in `annotation.ts`); the prefix `"annotation."` would catch it but the spec's exact action name is wrong |
| specs/home/recent-items-backend.feature | "Hydrates items with entity name and updatedAt" | UPDATE | Service returns the audit log's `createdAt` as the item's `updatedAt` (see `RecentItemsService.getRecentItems` storing `timestamp: log.createdAt`), not the entity's own `updatedAt`; spec's "icon" expectation also missing |
| specs/home/recent-items-backend.feature | "Excludes deleted entities from results" | KEEP | `hydrateEntity` returns null when `prompt.deletedAt` is truthy (line 96) and the integration test "excludes deleted prompts from results" exercises this but is not @scenario-bound to this scenario name |
| specs/home/recent-items-backend.feature | "Limits results to requested count" | KEEP | `getRecentItems` slices `entityMap` to `params.limit` (line 55); existing integration test "respects the limit parameter" tests behavior but is not @scenario-bound |
| specs/home/recent-items-backend.feature | "Orders by most recently touched first" | KEEP | Repository sorts by `createdAt: desc` (recent-items.repository.ts line 31) and service preserves Map insertion order, so newer entries appear first; no test bound |
| specs/home/recent-items-backend.feature | "Deduplicates same entity touched multiple times" | UPDATE | Service uses `if (!entityMap.has(key))` so it keeps the FIRST seen audit log entry — and given desc ordering that is the most recent; the spec's assertion "its timestamp should reflect the most recent touch" is correct, but the spec implies "exactly 1 item" only if both touches are within `limit*3`, otherwise behavior diverges — phrasing needs tightening |
| specs/home/recent-items-backend.feature | "Returns correct deep link URL for prompts" | KEEP | `hydrateEntity` for "prompt" returns href `/${prompt.project.slug}/prompts?prompt=${prompt.id}` — contains "/prompts" and the id; no test bound |
| specs/home/recent-items-backend.feature | "Returns correct deep link URL for workflows" | KEEP | href is `/${workflow.project.slug}/studio/${workflow.id}` — matches the literal `/project-456/studio/workflow-123` (assuming slug == id in the test); no test bound |
| specs/home/recent-items-backend.feature | "Returns correct deep link URL for datasets" | KEEP | href is `/${dataset.project.slug}/datasets/${dataset.id}` matching `/project-456/datasets/dataset-123`; no test bound |
| specs/home/recent-items-backend.feature | "Returns correct deep link URL for evaluations" | KEEP | href is `/${monitor.project.slug}/evaluations` (no slug suffix) matching the spec's `/project-456/evaluations`; no test bound |
| specs/home/recent-items-backend.feature | "Returns correct deep link URL for annotation queues" | KEEP | href is `/${queue.project.slug}/annotations/${queue.slug}` matching `/project-456/annotations/my-queue`; no test bound |
