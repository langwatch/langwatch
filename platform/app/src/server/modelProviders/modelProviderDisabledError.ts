/** Compatibility export for legacy transports; the domain error lives in the feature contract. */
export {
  MODEL_PROVIDER_DISABLED_CAUSE,
  ModelProviderDisabledError,
  type ModelProviderResolvedAlternate as ResolvedAlternate,
} from "@langwatch/model-provider-contract";
