/** Compatibility type export; provider-neutral storage is owned by Stored Objects. */
export type {
  StoredObjectStorageDriver as StorageDriver,
  StoredObjectStorageDriverFactory as StorageDriverFactory,
} from "@langwatch/stored-object-server/storage";
