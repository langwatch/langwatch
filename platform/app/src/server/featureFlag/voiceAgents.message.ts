/** The copy every voice-agent refusal shows the customer (AC29). Kept in a module with no server imports because client-bundled code (suites/errors.ts via suite-evaluators) reads it. */
export const VOICE_AGENTS_DISABLED_MESSAGE =
  "Voice agents are not enabled for this project";
