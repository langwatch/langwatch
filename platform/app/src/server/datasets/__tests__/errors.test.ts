import { describe, it } from "vitest";

describe("Dataset Errors", () => {
  describe("DatasetNotFoundError", () => {
    it.todo("has name='DatasetNotFoundError'");
    it.todo("includes message");
  });

  describe("DatasetConflictError", () => {
    it.todo("has name='DatasetConflictError'");
    it.todo("has default message about duplicate name");
  });
});
