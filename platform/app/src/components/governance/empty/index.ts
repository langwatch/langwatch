/**
 * The governance section's shared empty state.
 *
 * Import from here rather than the file, so the module path stays stable if
 * the component grows siblings:
 *
 *   import {
 *     GovernanceEmptyState,
 *     GovernanceEmptyStateAction,
 *   } from "~/components/governance/empty";
 */
export {
  GovernanceEmptyState,
  GovernanceEmptyStateAction,
  type GovernanceEmptyStateIcon,
} from "./GovernanceEmptyState";
