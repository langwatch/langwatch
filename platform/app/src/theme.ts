/**
 * Chakra UI theme system for LangWatch.
 * Extracted from _app.tsx to remove Next.js dependency (next/font/google).
 * Inter font is now loaded via CSS @import in globals.scss.
 */
export { system } from "./pages/_app";
