/**
 * The account's own sign-in addresses, on /settings/security.
 *
 * The surface D07's detach guards point at: the guard has always said "add a
 * verified email address first", and this is where somebody does it. Adding,
 * confirming and removing all run on D01's identity ceremonies.
 *
 * Spec: specs/identity/authentication-settings.feature
 */
export { EmailIdentifiersSection } from "./components/EmailIdentifiersSection";
