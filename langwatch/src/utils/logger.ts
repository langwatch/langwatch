/**
 * Server-side logger - re-exports from logger.server.ts
 *
 * For client-side logging, import from "~/utils/logger.client" instead.
 */
export { createLogger, type CreateLoggerOptions, type Logger } from "./logger.server";
