import { Box } from "@chakra-ui/react";

export function PromptBrowserHeader() {
  return (
    <Box height="full" width="full" bg="white">
      Prompt Name, version, and other metadata
    </Box>
  );
}
