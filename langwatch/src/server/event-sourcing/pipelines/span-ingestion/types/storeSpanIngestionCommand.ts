/**
 * Command data for storing a span ingestion.
 * This command creates a span ingestion event that will be stored and processed by event handlers.
 * Type is inferred from the Zod schema.
 */
export type { StoreSpanIngestionCommandData } from "../../../schemas/commands/spanIngestion.schema";
