/**
 * Re-export type identifiers from schemas/index.ts for backwards compatibility.
 * All type identifiers are now defined in their respective schema files and collected in index.ts.
 */
export {
  EVENT_TYPE_IDENTIFIERS,
  COMMAND_TYPE_IDENTIFIERS,
  AGGREGATE_TYPE_IDENTIFIERS,
} from "./index";

