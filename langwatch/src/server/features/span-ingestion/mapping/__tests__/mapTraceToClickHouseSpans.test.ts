import { describe, it } from "vitest";

describe("mapReadableSpansToClickHouseSpans", () => {
  it.todo("maps a single span to a ClickHouse span");
  it.todo("maps multiple spans while preserving order");
  it.todo("handles spans with errors and sets status fields");
  it.todo("serializes complex input/output structures to attributes");
  it.todo("clamps negative durations to zero");
});
