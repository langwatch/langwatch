/**
 * Every handled-error code the TypeScript app can raise.
 *
 * This list exists so the presentation registry can be *exhaustive*: it is the
 * `Record` key set that `presentation.ts` must satisfy, which turns "someone
 * added an error code and never wrote the copy" from a runtime shrug into a
 * `pnpm typecheck` failure.
 *
 * Kept honest by `__tests__/codes.unit.test.ts`, which scans `src/`, `ee/` and
 * the workspace `packages/` for every code a `HandledError` subclass declares
 * and fails when this list and the code drift apart in either direction. All
 * three trees raise handled errors, and a scan that covers only one is a list
 * that silently stops being exhaustive. Adding a code here without
 * throwing it anywhere is just as much a bug as the reverse — it means dead
 * copy, which is how `recipient_not_in_team` survived in the automations
 * explainer for a code nothing raises.
 *
 * Codes declared by the Go services are NOT here — they come from
 * `goErrorCodes` in `@langwatch/handled-error`, generated by `cmd/herrgen`.
 */
export const APP_ERROR_CODES = [
  "anomaly_rule_not_found",
  "api_key_already_revoked",
  "api_key_not_found",
  "api_key_not_owned",
  "api_key_permission_denied",
  "api_key_reserved_name",
  "api_key_scope_violation",
  "broadcaster_not_active",
  "cannot_impersonate_admin",
  "cannot_impersonate_deactivated_user",
  "clickhouse_unavailable",
  "codex_auth_failed",
  "dataset_name_taken",
  "dspy_step_not_found",
  "evaluation_not_found",
  "evaluator_config_error",
  "evaluator_execution_error",
  "evaluator_input_too_large",
  "evaluator_missing_field",
  "evaluator_not_found",
  "experiment_not_found",
  "filter_field_unknown",
  "filter_parse_error",
  "guardrail_attach_forbidden",
  "ingestion_source_not_found",
  "invalid_action_params",
  "invalid_email_recipient",
  "langy_agent_at_capacity",
  "langy_agent_errored",
  "langy_agent_session_lost",
  "langy_agent_unavailable",
  "langy_conversation_not_found",
  "langy_conversation_not_owned",
  "langy_credential_resolution",
  "langy_dispatch_rejected",
  "langy_egress_misconfigured",
  "langy_empty_message",
  "langy_github_not_connected",
  "langy_github_repo_not_accessible",
  "langy_idempotency_mismatch",
  "langy_insufficient_scope",
  "langy_model_not_allowed",
  "langy_model_not_configured",
  "langy_not_enabled",
  "langy_turn_in_progress",
  "langy_turn_not_stoppable",
  "langy_turn_timeout",
  "langy_worker_restarting",
  "langy_worker_spawn_failed",
  "langy_worker_stopped",
  "lite_member_restricted",
  "malformed_custom_role_permissions",
  "malformed_request",
  "missing_annotator",
  "missing_slack_bot_token",
  "missing_slack_webhook",
  "notification_delivery_error",
  "organization_not_found_for_team",
  "project_not_found",
  "query_memory_exceeded",
  "query_timeout",
  "resource_limit_exceeded",
  "rum_ingest_disabled",
  "rum_payload_invalid",
  "rum_payload_too_large",
  "rum_rate_limited",
  "scenario_set_limit_exceeded",
  "schema_failure",
  "share_link_exhausted",
  "share_link_expired",
  "share_link_forbidden",
  "share_link_not_found",
  "share_read_rate_limited",
  "span_not_found",
  "subscription_service_unavailable",
  "suite_all_scenarios_archived",
  "suite_all_targets_archived",
  "suite_invalid_scenario_references",
  "suite_invalid_target_references",
  "suite_name_taken",
  "suite_not_found",
  "system_prompt_conflict",
  "system_prompt_required",
  "template_validation_error",
  "test_fire_unavailable",
  "time_range_too_wide",
  "trace_not_evaluatable",
  "trace_not_found",
  "trace_sharing_disabled",
  // Also a Go code, with copy already written under the shared/transport
  // heading — an app-level subclass raises it too (`ee/admin/routes/admin.ts`),
  // and the scan is right to insist it be listed here as well.
  "unauthorized",
  "user_to_impersonate_not_found",
  // Raised by the shared package rather than an app-level subclass:
  // `ValidationError` (and `ValidationError.fromZodError`, which the tRPC
  // boundary uses to promote input ZodErrors onto the handled channel).
  "validation_error",
] as const;

export type AppErrorCode = (typeof APP_ERROR_CODES)[number];
