# No configuration needed!
# The tool handles everything through the --env flag:
#
#   --env local   → localhost:6379
#   --env dev     → AWS Secrets Manager (profile: lw-dev)
#   --env staging → kubectl port-forward svc/db-tunnel
#   --env prod    → kubectl port-forward svc/db-tunnel
