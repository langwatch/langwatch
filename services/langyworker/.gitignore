# Bun-compiled binaries now default to <repo root>/.bin/langy-worker/, ignored
# there. This covers the older out/ path so a stale build from before the move
# can never be committed. dist/ is ignored repo-wide.
out/
