[
  { "name": "main", "sql": "SELECT toDate(StartedAt) AS day, sum(ActiveTimeCliSec) / 60 AS working, sum(BlockedOnUserMs) / 60000 AS waiting FROM agent_sessions WHERE StartedAt >= {dashboard_context_period_start:DateTime} AND StartedAt < {dashboard_context_period_end:DateTime} GROUP BY day ORDER BY day", "parameters": [] }
]
