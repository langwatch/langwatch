[
  { "name": "main", "sql": "SELECT toStartOfMonth(Day) AS month, sum(TokensIn + TokensOut) AS tokens FROM finops_usage WHERE DepartmentId = 'engineering' AND Charge = 'usage' AND Day >= {dashboard_context_period_start:DateTime} AND Day < {dashboard_context_period_end:DateTime} GROUP BY month ORDER BY month", "parameters": [] }
]
