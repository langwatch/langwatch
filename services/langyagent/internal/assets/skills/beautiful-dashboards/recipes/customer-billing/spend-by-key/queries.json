[
  { "name": "main", "sql": "SELECT KeyId AS key, sum(Cost) AS cost FROM finops_usage WHERE KeyId != '' AND Day >= {dashboard_context_period_start:DateTime} AND Day < {dashboard_context_period_end:DateTime} GROUP BY key ORDER BY cost DESC LIMIT 8", "parameters": [] }
]
