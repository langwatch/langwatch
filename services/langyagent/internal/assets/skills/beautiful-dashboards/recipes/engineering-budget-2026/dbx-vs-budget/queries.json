[
  { "name": "main", "sql": "SELECT toStartOfMonth(Day) AS month, sum(Cost) AS cost FROM finops_usage WHERE Tool = 'databricks' AND Day >= toStartOfYear(now()) GROUP BY month ORDER BY month", "parameters": [] }
]
