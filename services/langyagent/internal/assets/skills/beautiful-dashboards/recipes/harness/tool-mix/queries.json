[
  { "name": "main", "sql": "SELECT ToolName AS tool, sum(ToolResultBytes) / 4 AS tokens FROM agent_session_events WHERE EventKind = 'tool_result' AND TimeUnixMs >= {dashboard_context_period_start:DateTime} AND TimeUnixMs < {dashboard_context_period_end:DateTime} GROUP BY tool ORDER BY tokens DESC LIMIT 8", "parameters": [] }
]
