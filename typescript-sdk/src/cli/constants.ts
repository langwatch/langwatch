/**
 * Default model written into a freshly created `.prompt.yaml`. Re-exported
 * from a generated file so it stays in lock-step with the platform's
 * registry (`langwatch/src/server/modelProviders/llmModels.json`). Run
 * `pnpm prepare` to regenerate after the registry changes.
 */
export { DEFAULT_PROMPT_MODEL } from "../internal/generated/cli/default-prompt-model.generated";
