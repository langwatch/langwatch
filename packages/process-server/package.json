{
  "name": "@langwatch/process-server",
  "version": "0.1.0",
  "private": true,
  "description": "The process boundary: the signals that end a LangWatch process, the failures that kill it, and the order the things it hosts are torn down in. It builds no telemetry and reads no config.",
  "license": "MIT",
  "files": [
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -b tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@langwatch/kernel": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
