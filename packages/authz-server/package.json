{
  "name": "@langwatch/authz-server",
  "version": "0.1.0",
  "private": true,
  "description": "The server-side runtime of the unified authorization engine (ADR-092): AuthzService, GrantsService, shadow mode, and the storage ports they run on. Storage-engine-free - the app implements the repository interfaces with Prisma.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@langwatch/authz": "workspace:*",
    "@langwatch/handled-error": "workspace:*",
    "@langwatch/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.9"
  }
}
