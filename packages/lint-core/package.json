{
  "name": "@langwatch/lint-core",
  "version": "0.1.0",
  "private": true,
  "description": "The shared core of the LangWatch oxlint plugin: the rule definition helper, the one per-file classification every rule gates on, the keyed AST walk, the pure policy grammars, and the fixture-tree test harness. Plain ESM so oxlint loads it with no build step.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.mjs",
  "files": [
    "grammar",
    "src"
  ],
  "exports": {
    ".": "./src/index.mjs",
    "./testing": "./src/testing.mjs",
    "./grammar/*": "./grammar/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "oxlint": "^1.78.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
