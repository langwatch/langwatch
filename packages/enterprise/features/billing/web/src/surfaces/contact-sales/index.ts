/**
 * The "need more?" card, for the families that hit an Enterprise wall.
 *
 * Audit logs, groups, gateway webhooks and the authorization upsell all end at
 * the same sentence and the same list of what Enterprise adds. Billing owns the
 * plan those words describe, so the card travels as one surface rather than as
 * four copies that fall out of step with the catalogue.
 */
export { ContactSalesBlock } from "./contact-sales-block";
