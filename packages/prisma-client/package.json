{
  "name": "@langwatch/prisma-client",
  "version": "0.1.0",
  "private": true,
  "description": "Explicitly configured Prisma/Postgres construction and lifecycle services, plus the canonical LangWatch schema, migrations, and generated client.",
  "license": "MIT",
  "files": [
    "prisma",
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./generated": {
      "types": "./src/generated/client.ts",
      "default": "./src/generated/client.ts"
    }
  },
  "scripts": {
    "prisma:generate": "prisma generate --config ./prisma.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@prisma/adapter-pg": "7.9.1",
    "@prisma/client": "7.9.1",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@types/pg": "^8.20.3",
    "prisma": "7.9.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  }
}
