{
  "name": "@langwatch/system-migrations",
  "version": "0.1.0",
  "private": true,
  "description": "In-place, one-time data migrations the system runs on itself: a per-tenant state machine driven by a leased runner at worker boot. Storage-engine-free - the app implements the repository interfaces with Prisma and Redis. First rider: the ADR-092 stage-B authorization backfill.",
  "license": "MIT",
  "files": [
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@langwatch/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  }
}
