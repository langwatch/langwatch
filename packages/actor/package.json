{
  "name": "@langwatch/actor",
  "version": "0.1.0",
  "private": true,
  "description": "The actor vocabulary: one closed answer to \"who caused this action\", minted at the boundary that authenticated it and carried to every durable record. No node imports and no dependencies, so any surface — frontend, backend, workers — names actors from the same vocabulary.",
  "license": "MIT",
  "files": [
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p . && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  }
}
