{
  "name": "@langwatch/model-provider-contract",
  "version": "0.1.0",
  "private": true,
  "description": "Portable Model Provider feature contracts and Zod 4 values.",
  "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": { "safe-regex2": "^5.1.1", "zod": "^4.4.3" },
  "devDependencies": { "typescript": "^7.0.2", "vitest": "^4.1.9" }
}
