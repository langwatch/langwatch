{
  "name": "@langwatch/notification-server",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical server-side notification record service, private persistence, and the process-owned outbound mail delivery capability.",
  "files": [
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.1080.0",
    "@langwatch/notification-contract": "workspace:*",
    "@langwatch/observability": "workspace:*",
    "@langwatch/prisma-client": "workspace:*",
    "@sendgrid/mail": "^8.1.6",
    "nodemailer": "^9.0.3",
    "undici": "^8.5.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  }
}
