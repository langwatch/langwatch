/**
 * The URL-addressed drawers this family owns.
 *
 * ONE PUBLIC ENTRY FOR THE WHOLE SET: the composing application spreads a map
 * of these into its drawer registry, so what it may name is one entry rather
 * than a path per component.
 */

export { PromptListDrawer, type PromptListDrawerProps } from "./prompt-list-drawer";
