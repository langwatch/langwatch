{
  "name": "@langwatch/evaluator-server",
  "version": "0.1.0",
  "private": true,
  "description": "Evaluator services, private persistence and infrastructure adapters.",
  "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run", "test:unit": "vitest run" },
  "dependencies": {
    "@langwatch/evaluator-contract": "workspace:*",
    "@langwatch/prisma-client": "workspace:*",
    "@langwatch/workflow-contract": "workspace:*"
  },
  "devDependencies": { "typescript": "^7.0.2", "vitest": "^4.1.9" }
}
