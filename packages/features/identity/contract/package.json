{
  "name": "@langwatch/identity-contract",
  "version": "0.1.0",
  "private": true,
  "description": "The isomorphic identity core (ADR-101, ADR-115): the identifier vocabulary, value normalization, the fact and command shapes, the pure reducer, the refusal errors and the backfill's parity policy. No node imports, so the frontend and the backend share it verbatim. Server-side services live in @langwatch/identity-server.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p . && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@langwatch/handled-error": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^26",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  }
}
