/**
 * Reserved for package-local test builders. The concrete service and its
 * repositories stay private; composition consumers use the root adapter and
 * the contract's AutomationService capability.
 */
export {};
