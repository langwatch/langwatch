{
  "name": "@langwatch/langy",
  "version": "0.1.0",
  "private": true,
  "description": "Langy's shared conversation-projection contract: the event payload schemas, the pure turn fold, the turn-phase machine, and the event cursor — imported by both the LangWatch server pipeline and the browser panel so a turn renders identically on both sides (ADR-059).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "zod": ">=3.25.0 <5"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^4.1.9",
    "zod": "^3.25.76"
  }
}
