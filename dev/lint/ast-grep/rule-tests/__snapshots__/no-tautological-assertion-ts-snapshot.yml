id: no-tautological-assertion-ts
snapshots:
  ? |
    expect(TIMEOUT_MS).toBe(TIMEOUT_MS);
  : labels:
    - source: expect(TIMEOUT_MS).toBe(TIMEOUT_MS)
      style: primary
      start: 0
      end: 35
  ? |
    expect(config.retries).toEqual(config.retries);
  : labels:
    - source: expect(config.retries).toEqual(config.retries)
      style: primary
      start: 0
      end: 46
  ? |
    expect(true).toBe(true);
  : labels:
    - source: expect(true).toBe(true)
      style: primary
      start: 0
      end: 23
