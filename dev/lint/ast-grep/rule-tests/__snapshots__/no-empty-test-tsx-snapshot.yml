id: no-empty-test-tsx
snapshots:
  ? |
    it("rejects a malformed cursor", () => {});
  : labels:
      - source: it("rejects a malformed cursor", () => {})
        style: primary
        start: 0
        end: 42
  ? |
    test("seeds the catalog", async () => {});
  : labels:
      - source: test("seeds the catalog", async () => {})
        style: primary
        start: 0
        end: 41
