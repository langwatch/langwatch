# LangWatch lint rules

<!-- Generated by `pnpm --filter @langwatch/architecture-enforcer docs`. Edit the rule in
     `packages/oxlint-rules/src/rules` and regenerate; do not edit this file. -->

Every rule the `langwatch` oxlint plugin registers (57 of them), rendered from the
`defineRule` declaration each one is written in. A printed message is `what` followed by
`fix`; `why` is documentation and the linter never prints it.

- [`langwatch/banned-test-model-names`](#langwatchbannedtestmodelnames)
- [`langwatch/banned-verb-prefix`](#langwatchbannedverbprefix)
- [`langwatch/clickhouse-no-version-order-limit`](#langwatchclickhousenoversionorderlimit)
- [`langwatch/clickhouse-tenant-id`](#langwatchclickhousetenantid)
- [`langwatch/cognitive-complexity`](#langwatchcognitivecomplexity)
- [`langwatch/comment-block-size`](#langwatchcommentblocksize)
- [`langwatch/condition-shape`](#langwatchconditionshape)
- [`langwatch/conditional-type-depth`](#langwatchconditionaltypedepth)
- [`langwatch/em-dash-in-copy`](#langwatchemdashincopy)
- [`langwatch/enterprise-license-header`](#langwatchenterpriselicenseheader)
- [`langwatch/environment-boundaries`](#langwatchenvironmentboundaries)
- [`langwatch/eventing-role-purity`](#langwatcheventingrolepurity)
- [`langwatch/fallible-result-naming`](#langwatchfallibleresultnaming)
- [`langwatch/feature-source-filename`](#langwatchfeaturesourcefilename)
- [`langwatch/feature-source-layout`](#langwatchfeaturesourcelayout)
- [`langwatch/feature-source-subject`](#langwatchfeaturesourcesubject)
- [`langwatch/handled-error-outside-contract`](#langwatchhandlederroroutsidecontract)
- [`langwatch/id-generation-origin`](#langwatchidgenerationorigin)
- [`langwatch/idempotency-key-is-stable`](#langwatchidempotencykeyisstable)
- [`langwatch/jsx-from-hook`](#langwatchjsxfromhook)
- [`langwatch/legacy-monolith-path`](#langwatchlegacymonolithpath)
- [`langwatch/module-classes`](#langwatchmoduleclasses)
- [`langwatch/module-layers`](#langwatchmodulelayers)
- [`langwatch/namespace-class`](#langwatchnamespaceclass)
- [`langwatch/no-alias-reexport`](#langwatchnoaliasreexport)
- [`langwatch/no-boot-hook-outside-guard`](#langwatchnoboothookoutsideguard)
- [`langwatch/no-form-watch-in-child`](#langwatchnoformwatchinchild)
- [`langwatch/no-inline-dynamic-import`](#langwatchnoinlinedynamicimport)
- [`langwatch/no-logger-spy`](#langwatchnologgerspy)
- [`langwatch/no-port-vocabulary`](#langwatchnoportvocabulary)
- [`langwatch/no-prototype-stub`](#langwatchnoprototypestub)
- [`langwatch/no-runtime-reflection`](#langwatchnoruntimereflection)
- [`langwatch/no-tautological-assertion`](#langwatchnotautologicalassertion)
- [`langwatch/overload-by-literal`](#langwatchoverloadbyliteral)
- [`langwatch/package-boundaries`](#langwatchpackageboundaries)
- [`langwatch/pass-through-class`](#langwatchpassthroughclass)
- [`langwatch/plan-literals`](#langwatchplanliterals)
- [`langwatch/prisma-count-in-list-query`](#langwatchprismacountinlistquery)
- [`langwatch/refusal-is-a-handled-error`](#langwatchrefusalisahandlederror)
- [`langwatch/require-fetch-timeout`](#langwatchrequirefetchtimeout)
- [`langwatch/rest-route`](#langwatchrestroute)
- [`langwatch/schema-outside-contract`](#langwatchschemaoutsidecontract)
- [`langwatch/service-does-not-open-a-channel`](#langwatchservicedoesnotopenachannel)
- [`langwatch/service-loads-its-own-config`](#langwatchserviceloadsitsownconfig)
- [`langwatch/shared-setup-is-a-hook`](#langwatchsharedsetupisahook)
- [`langwatch/signature-mirror`](#langwatchsignaturemirror)
- [`langwatch/stand-in-cast`](#langwatchstandincast)
- [`langwatch/store-containment`](#langwatchstorecontainment)
- [`langwatch/temporal-only`](#langwatchtemporalonly)
- [`langwatch/test-description-is-an-action`](#langwatchtestdescriptionisanaction)
- [`langwatch/transport-declares`](#langwatchtransportdeclares)
- [`langwatch/unbounded-loop`](#langwatchunboundedloop)
- [`langwatch/unit-test-does-not-render`](#langwatchunittestdoesnotrender)
- [`langwatch/unresolved-relative-import`](#langwatchunresolvedrelativeimport)
- [`langwatch/web-imports-server-shaped-value`](#langwatchwebimportsservershapedvalue)
- [`langwatch/zod-internals`](#langwatchzodinternals)
- [`langwatch/zod-object-composition`](#langwatchzodobjectcomposition)

## `langwatch/banned-test-model-names`

- Kind: problem
- Applies to: every file
- Fixable: code
- Spec: `specs/tooling/lint-banned-test-model-names.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `bannedModelName`
  - what: This literal names the retired or overpriced model `{{name}}`.
  - fix: Use `gpt-5-mini` instead.
  - why: gpt-5-mini is the cheapest and most capable model, and the whole suite should default to it.

## `langwatch/banned-verb-prefix`

- Kind: problem
- Applies to: isFallibleResultModule
- Fixable: no
- Spec: `specs/tooling/lint-banned-verb-prefix.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `requireAssertion`
  - what: `{{name}}` carries a `require` prefix, which says how it fails rather than what it checks.
  - fix: Name it `assert{{rest}}` and leave the body as it is: an `assert*` answers nothing and throws when the condition fails.
- `requirePrefix`
  - what: `{{name}}` carries a `require` prefix, which says how it fails rather than what it answers.
  - fix: Name it `get{{rest}}` and leave the body as it is: ADR-146's `get` already answers exactly one thing or throws.
- `swallowingTry`
  - what: `{{name}}` hedges: its catch turns a failure into null or undefined, so the caller cannot tell absence from breakage.
  - fix: Delete the catch that answers null or undefined so the failure reaches the caller, then drop `try` and name it for what it answers. A lookup of one thing becomes `get<Noun>` — or `getBy<Key>` when the key is what distinguishes it — and throws the domain error instead of answering null. None or many becomes `find<Noun>` returning an array, whose empty case is the absence. A derivation (`parse`, `extract`, `derive` and the other ADR-146 derivation verbs) may answer undefined when its input carried none. Never rename it to a `find*` that still answers null — `find` promises a list.
- `tryPrefix`
  - what: `{{name}}` is named for how it behaves on failure, not for what it answers.
  - fix: Drop `try` and name it for what it answers. A lookup of one thing becomes `get<Noun>` — or `getBy<Key>` when the key is what distinguishes it — and throws the domain error instead of answering null. None or many becomes `find<Noun>` returning an array, whose empty case is the absence. A derivation (`parse`, `extract`, `derive` and the other ADR-146 derivation verbs) may answer undefined when its input carried none. Never rename it to a `find*` that still answers null — `find` promises a list.
  - why: A caller reading the call site cannot tell a lookup from a hedge, and the two need different handling.

## `langwatch/clickhouse-no-version-order-limit`

- Kind: problem
- Applies to: isClickHouseRepository
- Fixable: no
- Spec: `specs/tooling/lint-clickhouse-no-version-order-limit.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `versionOrderLimit`
  - what: This ClickHouse query reads heavy columns and picks the latest version with `ORDER BY {{column}} DESC LIMIT 1`, which loads every unmerged version before discarding them.
  - fix: Select the latest version with an IN-tuple dedup: key columns and `max({{column}})` in an inner GROUP BY, heavy columns only in the outer SELECT.
  - why: The sort runs over full rows, not over the key columns.

## `langwatch/clickhouse-tenant-id`

- Kind: problem
- Applies to: isClickHouseRepository
- Fixable: no
- Spec: `specs/tooling/lint-clickhouse-tenant-id.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `missingTenantPredicate`
  - what: This ClickHouse query reads `{{table}}` without a `TenantId` predicate in that table's own WHERE, so it can return another tenant's rows.
  - fix: Add `TenantId = {tenantId:String}` (or `TenantId IN {tenantIds:Array(String)}`) to the WHERE that filters `{{table}}`.
  - why: No id but TenantId is unique across tenants.

## `langwatch/cognitive-complexity`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-cognitive-complexity.feature`
- Enforced: yes, at `error`

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `max` | integer | `15` | — |

Messages:

- `tooComplex`
  - what: `{{name}}` has cognitive complexity {{complexity}} (max {{max}}); the {{construct}} at line {{atLine}} carries {{share}} of it.
  - fix: Extract that {{construct}} into a module-level function (a nested closure still counts toward `{{name}}`) so the rest of `{{name}}` stays flat.
- `tooComplexSpread`
  - what: `{{name}}` has cognitive complexity {{complexity}} (max {{max}}), spread across {{blocks}} nested blocks with no single one carrying a third of it -- the depth is the cost, not any one branch.
  - fix: Flatten it: take the nesting down with early returns, or lift a whole stage of the work -- the {{construct}} at line {{atLine}} is the largest single block at {{share}} -- into a module-level function; a nested closure still counts toward `{{name}}`.

## `langwatch/comment-block-size`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-comment-block-size.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `commentBlockSize`
  - what: Comment block has {{lines}} lines; the maximum is {{max}}.
  - fix: Delete it when the code already says it, or move the narrative into an ADR under `dev/docs/adr/` and leave one line here linking it. See ADR-140.
  - why: A comment is for what the code cannot say. The rule makes two checks: a block of more than 5 lines, and a comment line wider than 100 columns.
- `commentColumns`
  - what: Comment line is {{width}} columns; wrap at {{max}}.
  - fix: Wrap it at {{max}} columns, keeping the sentence whole across the break. If it only restates the code beside it, delete it instead of wrapping it.
  - why: The rule's second check: a comment line is at most 100 columns wide.

## `langwatch/condition-shape`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-condition-shape.feature`
- Enforced: yes, at `error`

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `maxCalls` | integer | `1` | — |
| `maxHops` | integer | `2` | — |
| `maxOperators` | integer | `2` | — |

Messages:

- `chainTooDeep`
  - what: This test reads a chain {{hops}} properties deep and also calls or combines; `maxHops` is {{maxHops}}.
  - fix: Read the chain into a named `const` above the test; a chain alone, however deep, is fine once it stops combining with anything else.
  - why: A test nobody can read at a glance is where the wrong branch hides.
- `ternaryInTest`
  - what: This test has a ternary inside it.
  - fix: Move the ternary out of the test: decide it in the branch it belongs to, not inside this condition.
  - why: A test nobody can read at a glance is where the wrong branch hides.
- `tooManyCalls`
  - what: This test makes {{calls}} calls; `maxCalls` is {{maxCalls}}.
  - fix: Read each call into a named `const` above the test, or return, `continue` or `break` as soon as the first one fails so each remaining test makes one call. For a `switch`, read the value once above it and switch on that read.
  - why: A test nobody can read at a glance is where the wrong branch hides.
- `tooManyOperators`
  - what: This test joins {{operators}} logical operators; `maxOperators` is {{maxOperators}}.
  - fix: Split it into guard clauses — return, `continue` or `break` as soon as one part fails — or name the combined condition in a `const` that says what the branch means.
  - why: A test nobody can read at a glance is where the wrong branch hides.

## `langwatch/conditional-type-depth`

- Kind: problem
- Applies to: isOverengineeringFile
- Fixable: no
- Spec: `specs/tooling/lint-conditional-type-depth.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `stateTheShape`
  - what: {{message}}
  - fix: {{allowed}}
  - why: A type this deep re-computes what a plain interface or discriminated union already says.

## `langwatch/em-dash-in-copy`

- Kind: problem
- Applies to: isCustomerFacingTsx
- Fixable: no
- Spec: `specs/tooling/lint-em-dash-in-copy.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `emDashInCopy`
  - what: This user-facing text contains an em dash: "{{excerpt}}".
  - fix: Replace the em dash with a comma, a colon, or parentheses.
  - why: The copy guidelines ban em dashes in customer-facing prose.

## `langwatch/enterprise-license-header`

- Kind: problem
- Applies to: isOpenTreeImplementation
- Fixable: no
- Spec: `specs/tooling/lint-enterprise-license-header.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `enterpriseLicenseOutsideEnterprise`
  - what: This file carries the Enterprise SPDX directive but lives outside `enterprise/`.
  - fix: Move the implementation into its Enterprise module under `enterprise/modules/<name>/` and expose only its contract to the open tree; never delete the directive to pass.
  - why: Enterprise-licensed source belongs to an Enterprise module; the directive is how misplaced proprietary code is found.

## `langwatch/environment-boundaries`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-environment-boundaries.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `environment`
  - what: Do not read `process.env` here.
  - fix: Declare the key in the module's config schema and take the parsed value as an argument; only an app's `src/main.ts` or `src/config.ts` reads the environment.

## `langwatch/eventing-role-purity`

- Kind: problem
- Applies to: isScanned
- Fixable: no
- Spec: `specs/tooling/lint-eventing-role-purity.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `durableEvent`
  - what: {{role}} appends durable events itself with `{{name}}()`.
  - fix: Send the owning module's command instead; only a command handler appends durable events.
- `missingRedeliveryTest`
  - what: Subscriber `{{name}}` has no redelivery test.
  - fix: Add `{{expected}}` proving that handling the same event twice leaves one externally visible result; queue deduplication alone is not proof.
  - why: Delivery is at-least-once, so every subscriber must prove a redelivered event changes nothing.
- `processImpure`
  - what: Process manager {{detail}}.
  - fix: Keep its evolution synchronous and deterministic, and send the owning module's command for any external or delayed work.
  - why: A process manager's evolution replays from the stream, so it must decide the same way every time.
- `projectionImpure`
  - what: Projection {{detail}}.
  - fix: Keep the fold synchronous and deterministic: return the next read-model state and let the projection store persist it; side effects belong in a subscriber.
  - why: The worker refolds projections from the ordered stream; side effects or awaits make the read model depend on when it was folded.

## `langwatch/fallible-result-naming`

- Kind: problem
- Applies to: isFallibleResultModule
- Fixable: no
- Spec: `specs/tooling/lint-fallible-result-naming.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `nullableWithoutFind`
  - what: `{{name}}` answers null or undefined, which is not a shape new code writes.
  - fix: Decide what the absence means. If it is one thing that may not exist, name it `get<Noun>` — or `getBy<Key>` when the key is what distinguishes it — and throw the domain error, dropping null and undefined from the return type, so the caller gets the answer or the reason there is none. If it is really none or many, return an array and name it `find<Noun>`: the empty array is the absence. If it is a write whose target may normally be absent, return an explicit result union. Do not answer this by adding a nullable `find*` — `find` states cardinality, and the existing nullable ones are left as they are rather than joined by new ones.
  - why: A caller cannot tell whether absence here is an ordinary answer or a failure.
- `repositoryServiceVocabulary`
  - what: Repository method `{{name}}` uses service vocabulary; repositories answer `find*`, services answer `get*`.
  - fix: Rename it `find{{rest}}` here and in the repository interface this class implements.

## `langwatch/feature-source-filename`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-feature-source-filename.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `filename`
  - what: `{{name}}` is not `<subject>.<artifact>.ts` in lower kebab case.
  - fix: {{instruction}}

## `langwatch/feature-source-layout`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-feature-source-layout.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `contractFilename`
  - what: Rename `{{name}}` to `<subject>.<artifact>.ts` in lower kebab case, e.g. `trace-search.service.ts`.
  - fix: Take the artifact from `app`, `commands`, `errors`, `events`, `queries` or `service`, and write the subject and the artifact in lower kebab case with a single dot between them.
- `contractMissingSubject`
  - what: Rename `{{name}}` to `<subject>.{{artifact}}.ts`, e.g. `agent.commands.ts`.
  - fix: Add the subject to the filename.
- `contractProcessArtifact`
  - what: `{{name}}` is a process artifact: contract source holds none of `.api`, `.channel`, `.composition`, `.intent`, `.mapper`, `.migration`, `.process`, `.projection`, `.repository`, `.rules`, `.store`, `.subscriber`, `.task`.
  - fix: Move it into `modules/{{feature}}/process/src/`: `.repository`, `.store` and `.mapper` under `repositories/`, `.channel` under `channels/`, `.projection`, `.subscriber`, `.process` and `.intent` under `eventing/`, `.rules` under `rules/`, `.task` under `tasks/`, `.migration` under `migrations/`, and any `.api` other than `{{feature}}.api.ts` as `transport/{{feature}}.<rest|trpc|ws>.ts`.
- `processManagerService`
  - what: `{{path}}` is a process manager named as a service.
  - fix: Move it to `eventing/{{subject}}.process.ts`, beside the pipeline that names it.
- `processPath`
  - what: `{{path}}` has no home in layout v0. Only this shape is allowed: index.ts, <feature>.server.ts, app/<feature>.app.ts, app/<feature>.members.ts, transport/<feature>.<rest|trpc|ws>.ts, services/<name>.service.ts, repositories/ (interfaces, the bundle, the registry, and a backend folder beside them), channels/ (the interface, the bundle, the registry, and a tier folder beside them), eventing/<feature>.pipeline.ts and what it names, rules/<name>.rules.ts, tasks/<name>.task.ts, migrations/, app/<feature>-composition.build.ts (the ported process composition a converted module still carries; it only shrinks).
  - fix: Move `{{path}}` onto one of those paths: a service flattens to `services/<name>.service.ts`, with no subdirectory under `services/` and no qualifier before `.service`; owned state becomes `repositories/<backend>/<backend>.<subject>.repository.ts`, a message to anything the module does not own becomes `channels/<tier>/<tier>.<subject>.channel.ts`; a projection, subscriber, process manager or intent becomes `eventing/<feature>.<projection|subscriber|process|intent>.ts`; a transport becomes `transport/<feature>.<rest|trpc|ws>.ts`; a file with no artifact suffix moves into the module that already uses it.
- `rulesImpurity`
  - what: Rules module `{{path}}` may only export functions and constants (found {{found}}).
  - fix: Move the class or the `new` into `services/<name>.service.ts` and pass the constructed value into `{{path}}` as a function parameter.

## `langwatch/feature-source-subject`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-feature-source-subject.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `foreignSubject`
  - what: Source module {{path}} claims {{subject}}, which the catalogue gives to the {{owner}} module.
  - fix: Move the file to `{{ownerRoot}}/{{role}}/src/{{path}}`, or rename the subject if it is genuinely different.

## `langwatch/handled-error-outside-contract`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-handled-error-outside-contract.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `handledError`
  - what: `{{name}}` is a `HandledError` subclass (it extends `{{parent}}`) declared in `{{path}}`.
  - fix: Move it to `{{errorsFile}}`.
  - why: A service throws it and a client reads its code, so both sides need the class and neither may import a process package.

## `langwatch/id-generation-origin`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-id-generation-origin.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `foreignIdModule`
  - what: `{{name}}` mints ids outside the house scheme.
  - fix: Import `generate` from `@langwatch/ksuid` and mint it with its kind: `generate("{{kindPrefix}}").toString()`.
  - why: A second id scheme neither sorts by time nor names its kind, and two schemes in one table are a migration.
- `randomUuid`
  - what: `randomUUID()` mints an id outside the house scheme.
  - fix: Import `generate` from `@langwatch/ksuid` and mint it with its kind: `generate("{{kindPrefix}}").toString()`.
  - why: A UUID neither sorts by time nor names its kind, and two schemes in one table are a migration.

## `langwatch/idempotency-key-is-stable`

- Kind: problem
- Applies to: isProductionSource
- Fixable: no
- Spec: `specs/tooling/lint-idempotency-key-is-stable.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `mintedAtCallSite`
  - what: `{{name}}` is minted here by `{{source}}`, so every retry sends a different key and nothing is deduplicated.
  - fix: Derive it from the request's own content, or bind it once for the operation it identifies — `useState(() => crypto.randomUUID())` for a form, a key threaded from the caller for a mutation — and pass that binding here.
  - why: A key that changes per attempt is a field on the wire, not a guarantee: the server sees each retry as a new operation.

## `langwatch/jsx-from-hook`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-jsx-from-hook.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `hookReturnsJsx`
  - what: Hook `{{name}}` returns JSX.
  - fix: Return the state and callbacks, and move the JSX into the component that calls this hook.
  - why: Hooks return state and callbacks; a hook that renders is a component wearing the wrong name.

## `langwatch/legacy-monolith-path`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-legacy-monolith-path.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `legacyMonolithPath`
  - what: `{{name}}` names the deleted monolith.
  - fix: Find where the code lives now by searching its basename under `modules/*/*/src`, `enterprise/modules/*/*/src`, `packages/*/src` and `apps/*/src`, then import it by that package's name.
  - why: Nothing maps `~/*` or `@app/*` and `platform/` is gone, so the path resolves to nothing until something reaches the file.

## `langwatch/module-classes`

- Kind: problem
- Applies to: moduleClassKind
- Fixable: no
- Spec: `specs/tooling/lint-module-classes.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `create`
  - what: `{{name}}` has no static create, so nothing builds it the one way a process installs it.
  - fix: Add {{create}}.
- `missingConcrete`
  - what: `{{path}}` exports no concrete class named `*{{suffix}}`.
  - fix: Export one `class <Name>{{suffix}}` here; an interface for it belongs in the interface file beside it.
- `missingDeclared`
  - what: `{{path}}` declares a {{suffix}} and exports no interface or abstract class named `*{{suffix}}`.
  - fix: Export `interface <Name>{{suffix}}` here; the concrete class that implements it lives in its own file.
- `publicConstructor`
  - what: `{{name}}` has `static create`, but its constructor is public, so a caller can bypass create with `new {{name}}(...)`.
  - fix: Declare the constructor `private` — write `private constructor() {}` when the class has none.
- `standalone`
  - what: `{{name}}` is a function exported from the {{suffix}} module `{{path}}`; behaviour lives on the module's class.
  - fix: If it is a pure function of its arguments, move it to the module's `rules/` folder, or drop `export` and keep it a private helper here. Otherwise make it a method {{destination}}.

## `langwatch/module-layers`

- Kind: problem
- Applies to: isLayeredSource
- Fixable: no
- Spec: `specs/tooling/lint-module-layers.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `channelCrossing`
  - what: A channel names {{crossed}} (`{{specifier}}`).
  - fix: Move the call into the service that owns the decision and have that service invoke this channel.
  - why: A channel takes the client it speaks to; state is the repository's and behaviour the service's.
- `repositoryCrossing`
  - what: A repository names {{crossed}} (`{{specifier}}`).
  - fix: Move the decision into the service that calls this repository and keep the repository over its store alone.
  - why: A repository takes the store it reads; behaviour lives in the services above it.
- `serviceNamesABackend`
  - what: A service names {{crossed}} (`{{specifier}}`).
  - fix: Import the `repositories/<subject>.repository.ts` interface instead and take the implementation from the module's repository registry.
  - why: A service works over repository interfaces; the registry chooses the backend.
- `serviceNamesAChannelImplementation`
  - what: A service names {{crossed}} (`{{specifier}}`).
  - fix: Import the `channels/<subject>.channel.ts` interface instead and take the implementation from the module's channel registry.
  - why: A service works over channel interfaces; the channel registry chooses the tier.
- `transportCrossing`
  - what: A transport names {{crossed}} (`{{specifier}}`).
  - fix: Call the operation on the `app` the handler receives.
  - why: A transport declares; the module behind `app` owns the state, the guards and the tenancy filter.

## `langwatch/namespace-class`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-namespace-class.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `namespaceClass`
  - what: `{{name}}` has only static members ({{count}}), so it is a module wearing a class.
  - fix: Export the functions to `{{target}}` and delete the class.
  - why: A class earns its name by holding state; a bag of statics hides plain functions behind a namespace and a `create` nobody calls.

## `langwatch/no-alias-reexport`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-no-alias-reexport.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `aliasReexport`
  - what: `{{local}}` is re-exported here as `{{exported}}`.
  - fix: Rename `{{local}}` to `{{exported}}` with tslsp-cli and export it under one name.

## `langwatch/no-boot-hook-outside-guard`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-no-boot-hook-outside-guard.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `bootHookOutsideGuard`
  - what: `process.{{method}}("{{event}}", ...)` is registered outside the boot guard.
  - fix: Delete this listener and boot through the guard: a long-running process through the `Server` from `@langwatch/process-server`, a one-shot executable through `bootNodeExecutable` from `@langwatch/observability`.

## `langwatch/no-form-watch-in-child`

- Kind: problem
- Applies to: isBrowserSource
- Fixable: no
- Spec: `specs/tooling/lint-no-form-watch-in-child.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `watchOnReceivedForm`
  - what: `{{form}}.watch()` runs on a form this component received as a prop, so the whole form tree re-renders on every keystroke.
  - fix: Read the value with `useWatch({ control: {{form}}.control, name })` instead.

## `langwatch/no-inline-dynamic-import`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-no-inline-dynamic-import.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `inlineDynamicImport`
  - what: `import(...)` is used inline here.
  - fix: Use a top-level `import` / `import type` statement instead; a code-split component loads through `lazy(() => import(...))`.
  - why: A dynamic import hides a dependency the reader expects to find as a top-level import.

## `langwatch/no-logger-spy`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-no-logger-spy.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `spyOnLogger`
  - what: `vi.spyOn` patches a real logger here.
  - fix: Inject the `logger` from `createTestLogger()` (`@langwatch/test-harness`) and assert with `lines.findLine(level, text)` instead.

## `langwatch/no-port-vocabulary`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-no-port-vocabulary.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `portFile`
  - what: `{{name}}` is a port file.
  - fix: Move it to `repositories/` if it owns state, `channels/` if it exchanges messages with something the module does not own, `services/` if it's behaviour, or the module's Infrastructure if it's a client the process supplies — then rename the file for that role, dropping `port`.
- `portVocabulary`
  - what: `{{name}}` names a port.
  - fix: Name the role instead. Owned state is a repository, messages to something the module does not own are a channel, a client the process supplies is a member of the module's Infrastructure, behaviour is a service.

## `langwatch/no-prototype-stub`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-no-prototype-stub.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `prototypeStub`
  - what: `Object.create({{name}}.prototype)` builds a test double from a class prototype.
  - fix: Write it as an object literal typed `satisfies {{name}}`, or import the module's exported fixture if one already exists for this double.

## `langwatch/no-runtime-reflection`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-no-runtime-reflection.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `defineProperty`
  - what: `Object.{{method}}` patches `{{target}}`, which is not a class prototype.
  - fix: Declare the property directly where `{{target}}` is defined — in its class body or its object literal — instead of patching it in afterward.
- `proxy`
  - what: `new Proxy(...)` stands in for a class here.
  - fix: Write the class or app the module already declares instead of intercepting it at runtime.
- `reflect`
  - what: `Reflect.{{member}}` reaches around a method call.
  - fix: Call the method directly; add it to the interface if it is missing.
- `setPrototypeOf`
  - what: `Object.setPrototypeOf` rewires the prototype of `{{target}}` at runtime.
  - fix: Construct `{{target}}` as the class it should be — `new X(...)`, or `class X extends Y` — instead of swapping its prototype afterward.

## `langwatch/no-tautological-assertion`

- Kind: problem
- Applies to: isTestFile
- Fixable: no
- Spec: `specs/tooling/lint-no-tautological-assertion.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `assertsItself`
  - what: `expect({{actual}}).{{matcher}}({{actual}})` compares a value with itself and cannot fail.
  - fix: Assert the value the code produced against one derived independently of it.

## `langwatch/overload-by-literal`

- Kind: problem
- Applies to: isOverengineeringFile
- Fixable: no
- Spec: `specs/tooling/lint-overload-by-literal.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `splitTheOverloads`
  - what: {{message}}
  - fix: {{allowed}}
  - why: An overload set that only flips a flag makes the reader diff two signatures to learn one thing.

## `langwatch/package-boundaries`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-package-boundaries.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `browserImportsProcess`
  - what: `{{specifier}}` is process-only, and this is a browser package.
  - fix: Call the procedure through this module's derived tRPC client, and import any shared type from the owning module's contract.
- `browserSideDoor`
  - what: `{{specifier}}` reaches past `{{module}}`'s browser declaration, the only door a browser package has.
  - fix: Import `@langwatch/{{module}}-browser/declaration` and read the capability from its `withCapabilities` slot, or move a shared component into `{{kit}}`.
- `compositionRoot`
  - what: `{{specifier}}` is `{{module}}`'s process package, and an application composes modules without naming one.
  - fix: Take `{{module}}` from the generated `@langwatch/installed-server-modules` list (catalogue-driven, `pnpm generate:modules`), and move whatever this root builds from `{{specifier}}` behind the module's own declaration so the module constructs it.
- `contractRuntime`
  - what: A contract package is runtime-neutral: `{{specifier}}` is a node, browser or process runtime.
  - fix: Keep only schemas, types, errors and the `*Api` token here; move the code that needs `{{specifier}}` into this module's process package, or into its browser package when it is a browser import.
- `coreImportsEnterprise`
  - what: `{{specifier}}` is an enterprise module's implementation, and this is core code.
  - fix: Depend on that module's peer `*Api` from its contract instead; the enterprise module installs like any other and the process resolves the peer (ARCHITECTURE.md §11).
- `crossModuleBrowser`
  - what: `{{specifier}}` is `{{module}}`'s browser package, which is closed to every other module.
  - fix: Move what this needs into `{{kit}}` and import it from there; where fewer than two modules share it, inline it here instead (the kit law, ARCHITECTURE.md §3.4).
- `crossModuleProcess`
  - what: `{{specifier}}` is another module's process package.
  - fix: Call `{{api}}` from `{{contract}}` instead; if the operation is not there, it is a new `{{api}}` operation to propose to the module's owner.
- `kitFetches`
  - what: `{{specifier}}` fetches, and a browser kit fetches nothing.
  - fix: Take the data as a prop (`options`, `value`, `onChange`) and let each consumer run its own query.
- `kitLeaf`
  - what: `{{specifier}}` is a browser package, and a browser kit is a leaf.
  - fix: Import only contracts, `@langwatch/design-system` and `@langwatch/browser-host` here; take what `{{specifier}}` provides as a prop from the consumer.
- `packageEscape`
  - what: `{{specifier}}` resolves outside `{{packageRoot}}`, so this package depends on a file it does not own.
  - fix: Replace `{{specifier}}` with the target's package name — `@langwatch/<module>-<contract|process|browser|browser-kit>` for a module package, `@langwatch/<name>` for any other workspace package. Move the file into `{{packageRoot}}` instead only when nothing outside `{{packageRoot}}` imports it.
- `processImportsBrowser`
  - what: `{{specifier}}` is browser-only, and this is a process package.
  - fix: Move the browser-only code into this module's browser package; share a type through the contract.
- `processOutsideModule`
  - what: `{{specifier}}` is `{{module}}`'s process package, and only `{{module}}` itself may import it.
  - fix: Call `{{api}}` from `{{contract}}` instead; if the operation is not there, it is a new `{{api}}` operation to propose to the module's owner.
- `retiredPackageRuntime`
  - what: `{{specifier}}` is a retired runtime or package entry point.
  - fix: Import {{replacement}} instead.
- `schemaBoundary`
  - what: `{{specifier}}` binds the module to Hono.
  - fix: Export a plain Zod schema from the contract and let the transport adapt it.
- `sealedExports`
  - what: `{{subpath}}` is not in `{{package}}`'s `exports`.
  - fix: Import from `{{package}}` itself when its entry already re-exports the symbol. A browser package exports only `./declaration` and a kit only `.`, so there the symbol is private; for a contract or process package, add `"{{subpath}}"` to its `exports` and re-export the symbol from that entry.
- `unownedEscape`
  - what: `{{specifier}}` resolves outside `{{packageRoot}}` into a directory no package owns, so nothing records that this package depends on it.
  - fix: Give the target directory a `package.json` and add it to `pnpm-workspace.yaml`, then import it by that name — the way `dev/scripts` became `@langwatch/dev-scripts`. Move the file into `{{packageRoot}}` instead when only this package reads it.

## `langwatch/pass-through-class`

- Kind: problem
- Applies to: isOverengineeringFile
- Fixable: no
- Spec: `specs/tooling/lint-pass-through-class.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `passThrough`
  - what: {{message}}
  - fix: {{allowed}}
  - why: A pass-through class costs every reader a hop and every caller an indirection, and hides nothing.

## `langwatch/plan-literals`

- Kind: problem
- Applies to: isPlanFactSource
- Fixable: no
- Spec: `specs/tooling/lint-plan-literals.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `statesPlanFacts`
  - what: {{name}} states {{fields}} itself.
  - fix: Read them from the catalogue: `planCatalogue.plan("<TYPE>").limits` from @langwatch/plans. If this is a fixture, build it from `planCatalogue.plan(...)` and override the one field the test is about.
  - why: An object giving two or more limit fields literal values is a plan definition, and there is one catalogue of those.

## `langwatch/prisma-count-in-list-query`

- Kind: problem
- Applies to: isPrismaSeamFile
- Fixable: no
- Spec: `specs/tooling/lint-prisma-count-in-list-query.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `countInsideFindMany`
  - what: `_count` rides this `findMany`, and the planner can re-run its aggregate once per listed row.
  - fix: Drop `_count` from the query and run a second `groupBy` count restricted to the listed row ids.
  - why: Prisma builds `_count` as an uncorrelated join over the whole related table.

## `langwatch/refusal-is-a-handled-error`

- Kind: problem
- Applies to: isServerTransport
- Fixable: no
- Spec: `specs/tooling/lint-refusal-is-a-handled-error.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `handWrittenRefusal`
  - what: A refusal is written as a status and a body ({{shape}}).
  - fix: Throw a `HandledError` with a stable `code` — set `fault: "platform"` or `"provider"` explicitly if the status is 5xx — and let the boundary render it; register the code in `packages/handled-error/src/app-codes.ts` (sorted) and give it a customer-safe entry in `packages/handled-error/src/presentation.ts`.
  - why: Nothing hand-rendered reaches the error boundary, so the answer carries no code: the client presentation registry has nothing to key the customer's words on, and a test can only assert prose that will change.

## `langwatch/require-fetch-timeout`

- Kind: problem
- Applies to: isChannelSource
- Fixable: no
- Spec: `specs/tooling/lint-require-fetch-timeout.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `fetchWithoutSignal`
  - what: This `fetch` has no abort signal, so a peer that never answers hangs the caller.
  - fix: Pass `signal: AbortSignal.timeout(ms)` (or a controller's signal) in the init object.

## `langwatch/rest-route`

- Kind: problem
- Applies to: isRestTransport
- Fixable: no
- Spec: `specs/tooling/lint-rest-route.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `foreignContract`
  - what: REST route `{{operation}}` declares its wire with `{{name}}` from `{{source}}`, another module's contract.
  - fix: Declare the shape in this module's own contract and import it from there, or move the route to the module that owns `{{source}}`.
  - why: every wire schema imports from the module's own contract (ARCHITECTURE.md §8).
- `manualAnswer`
  - what: The handler for `{{operation}}` builds its own answer with `{{symbol}}`.
  - fix: Return the plain value the declared output describes, and throw a HandledError with a stable code for a refusal (a plain Error for anything else).
  - why: a hand-built answer skips the framework's serialisation, and a hand-built refusal carries no code for the client to key on.
- `missingInput`
  - what: REST route `{{operation}}` is a body-carrying method but declares no input.
  - fix: Add `.withInput(<schema>)` from the module's own contract; an action that takes no body declares an empty input schema there. A body nothing should parse uses `.withRawBody(...)` or `.withMultipart(...)` instead.
  - why: input is mandatory on every route, and the framework parses and validates only what the route declares (ARCHITECTURE.md §8).
- `missingOutput`
  - what: REST route `{{operation}}` declares no answer.
  - fix: Add `.withOutput(<schema>)` from the module's own contract (`z.void()` for a route that answers with nothing), `.responds({...})` for several statuses, or `.withResponse(<kind>)` for a non-JSON answer.
  - why: the framework serialises and documents only what the route declares (ARCHITECTURE.md §8).
- `pathParam`
  - what: REST route `{{operation}}` takes a path parameter named `{{name}}`.
  - fix: Name it for what it identifies - `:{{suggestion}}` - and rename the matching field in the route's withParams() schema.
  - why: the parameter name is published in the OpenAPI document and becomes the argument name in every generated client.
- `rawResponse`
  - what: REST route `{{operation}}` answers through `.withRawResponse`, the retired hatch.
  - fix: Replace it with `.withResponse("bytes" | "sse" | "redirect" | "protocol" | "forwarded", {...})` and produce the answer through the handler's `response` argument; a JSON answer declares `.withOutput(<schema>)` and returns a plain value.
  - why: a non-JSON protocol declares the kind it answers with, and the handler is handed the one producer for that kind (ARCHITECTURE.md §8).

## `langwatch/schema-outside-contract`

- Kind: problem
- Applies to: isSchemaSource
- Fixable: no
- Spec: `specs/tooling/lint-schema-outside-contract.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `schema`
  - what: `{{name}}` is a Zod schema declared in `{{path}}`.
  - fix: Move it to `{{contractPath}}` and import it here.
  - why: A transport file declares routes and imports its shapes; the vocabulary belongs to every side of the wire.

## `langwatch/service-does-not-open-a-channel`

- Kind: problem
- Applies to: isService
- Fixable: no
- Spec: `specs/tooling/lint-service-does-not-open-a-channel.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `serviceOpensAChannel`
  - what: A service opens {{conduit}} directly (`{{specifier}}`).
  - fix: Move the conduit to `channels/{{tier}}/{{tier}}.<subject>.channel.ts` and inject the channel interface.
  - why: Messages to or from something the module does not own are a channel: an interface the module names, a live implementation per tier and a memory twin a test asserts against.

## `langwatch/service-loads-its-own-config`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-service-loads-its-own-config.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `configFunction`
  - what: `{{name}}` in `{{path}}` resolves its own configuration.
  - fix: Delete it; add a named `config` member to the argument object `create` takes and resolve it once at the composition root.
  - why: A caller that already validated its config forces every callee to re-validate what it was handed.

## `langwatch/shared-setup-is-a-hook`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-shared-setup-is-a-hook.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `siblingTestsRepeatSetup`
  - what: These {{count}} sibling tests each open with the same {{shared}} statements.
  - fix: Move the repeated statements into a `beforeEach(() => { ... })` at the top of this `describe` and delete them from each test.
  - why: Setup pasted into every sibling drifts one body at a time.

## `langwatch/signature-mirror`

- Kind: problem
- Applies to: isBoundarySource
- Fixable: no
- Spec: `specs/tooling/lint-signature-mirror.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `mirroredSignature`
  - what: This boundary type mirrors another signature through `{{name}}<…>`.
  - fix: Declare the input and output as named contract types (Zod schema plus `z.infer`) and type the `*Api` operation with them explicitly.
  - why: A mirrored type changes whenever the mirrored function does, so the contract no longer states what the boundary accepts or returns.

## `langwatch/stand-in-cast`

- Kind: problem
- Applies to: isGovernedSource
- Fixable: no
- Spec: `specs/tooling/lint-stand-in-cast.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `doubleCast`
  - what: `as {{through}} as {{target}}` casts through {{through}} to reach {{target}}.
  - fix: If this value crossed a trust boundary (network, database row, user input), parse it with the contract's Zod schema (`Schema.parse(value)`) instead of casting; otherwise fix the type of whatever produced it so the cast is unnecessary.
  - why: A cast through `unknown` or `any` removes the only check that stood between the two types.
- `doubleCastInTest`
  - what: `as {{through}} as {{target}}` forces this test value to {{target}} without checking it.
  - fix: Build the stub to {{target}}'s real shape instead of casting: give each mocked member its real signature so the object type-checks without the cast.
  - why: A test value never crossed a trust boundary, so there is nothing to parse — the fix is a typed stub, not a schema.

## `langwatch/store-containment`

- Kind: problem
- Applies to: isGoverned
- Fixable: no
- Spec: `specs/tooling/lint-store-containment.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `storeClientValue`
  - what: `{{specifier}}` is the {{store}} client, value-imported outside `repositories/{{folder}}/`.
  - fix: Move the query into `repositories/{{folder}}/{{folder}}.<subject>.repository.ts` behind the `repositories/<subject>.repository.ts` interface and call that from the service; a file that only needs a type writes `import type`.
  - why: A service holding its own client has a second, unswappable path to the module's data.
- `storeInApplication`
  - what: `{{specifier}}` is a {{store}} client named in an application.
  - fix: Delete the import: the `Server` chain opens every store, and only a module's repositories and channels hold a client.
- `storeNamed`
  - what: `{{specifier}}` names {{store}} outside `repositories/{{folder}}/`.
  - fix: Move the query into `repositories/{{folder}}/{{folder}}.<subject>.repository.ts` behind the `repositories/<subject>.repository.ts` interface and call that from the service; only that folder names {{store}}, even as a type.

## `langwatch/temporal-only`

- Kind: problem
- Applies to: isTemporalOnlySource
- Fixable: no
- Spec: `specs/tooling/lint-temporal-only.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `constructInstant`
  - what: {{name}} builds a Date out of a value.
  - fix: Use `Temporal.Instant.from(iso)` for an ISO string, `Temporal.Instant.fromEpochMilliseconds(ms)` for a millisecond count, or `fromDate(date)` from @langwatch/time when a boundary handed you a Date.
- `dateType`
  - what: {{name}} is typed `Date`.
  - fix: Declare it `Temporal.Instant` from @langwatch/time, or `string` when the value only ever comes off the wire, and convert with `toDate()` at the Prisma or SDK boundary.
  - why: A Date on a declared type invites one to be minted to satisfy it.
- `mintNow`
  - what: {{name}} mints a Date for the current moment.
  - fix: Call `nowInstant()` from @langwatch/time.
  - why: One clock: every moment in the product is a Temporal.Instant.
- `mintNowMilliseconds`
  - what: {{name}} reads the current moment as Date epoch milliseconds.
  - fix: Call `nowInstant().epochMilliseconds` from @langwatch/time.
- `parseInstant`
  - what: {{name}} parses a moment through Date.
  - fix: Use `Temporal.Instant.from(iso).epochMilliseconds` from @langwatch/time.
- `utcInstant`
  - what: {{name}} builds an epoch count out of calendar parts through Date.
  - fix: Build the moment with `Temporal.PlainDateTime.from({ year, month, day }).toZonedDateTime("UTC").epochMilliseconds` from @langwatch/time.

## `langwatch/test-description-is-an-action`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-test-description-is-an-action.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `nestedDescribeMissingGivenWhen`
  - what: This nested `describe` title does not start with "given ", "when " or "and ", and does not name the unit under test.
  - fix: Rename it to a condition: "given <precondition>" if it sets up state, "when <action>" if it performs the behaviour under test.
  - why: Nested `describe` blocks read as BDD structure: an outer `given <precondition>`, an inner `when <action>`.

## `langwatch/transport-declares`

- Kind: problem
- Applies to: isProcessSource
- Fixable: no
- Spec: `specs/tooling/lint-transport-declares.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `bareRouter`
  - what: This tRPC transport builds a bare `router({ … })`.
  - fix: Declare each procedure with `defineTrpcRouter`, which applies its permission after the parser.
- `compositionImport`
  - what: This source imports `@langwatch/api/composition`, which only a process imports.
  - fix: Declare the route with `defineRestRouter` or the procedure with `defineTrpcRouter`; the process builds and mounts the application.
- `credentialFromContext`
  - what: This transport reads the credential `{{key}}` off the request context.
  - fix: Read the caller from the handler's `actor` and `scope`; the framework resolves the credential, class and all, before the handler runs.
- `handlerBindingCall`
  - what: This source calls `createTrpcHandlerBinding`.
  - fix: Delete the call and export the `defineTrpcRouter` declaration; the process binds handlers when it mounts the router.
- `handlerConstructs`
  - what: The handler constructs `{{name}}`.
  - fix: Call the operation on the handler's `app`; the module constructs its services once, when the process boots.
- `handlerControlFlow`
  - what: The handler branches, loops or catches.
  - fix: Move the decision into the module behind `app`; the handler calls one operation and returns its result or throws.
- `handlerNotInline`
  - what: This route's handler is not written inline.
  - fix: Write the handler as an inline function inside `.handle(...)`, beside the route's input, output and permission.
- `handlerTooLong`
  - what: The handler has {{count}} top-level statements; the ceiling is {{max}}.
  - fix: Keep it to one operation call on `app` and a pure mapping of the result; declare permission and limits on the route.
- `legacyRbacName`
  - what: This transport names the legacy RBAC identifier `{{name}}`.
  - fix: Declare access with `.withPermission(...)` on the route or procedure; AuthZ decides it.
- `legacyRegisterRoute`
  - what: This route registers its handler through `registerRoute`.
  - fix: Declare it with `defineRestRouter`'s chain so the verb, input, output, permission and inline handler form one declaration.
- `multipleOperationCalls`
  - what: The handler makes {{count}} calls on `app`.
  - fix: Call exactly one API operation and move the orchestration into the module; a pure mapping of its result may stay.
- `nestedOperationCall`
  - what: The handler calls `app` from inside a callback.
  - fix: Add one operation to the module that does the whole batch, and call it once.
- `noContentSentinel`
  - what: The handler returns the `NO_CONTENT` sentinel.
  - fix: Return nothing: a handler that returns `undefined` answers with an empty response.
- `openapiDoorImport`
  - what: This REST transport imports `{{names}}` from `{{specifier}}`.
  - fix: Declare the route with `defineRestRouter`: `.withInput(...)` validates the request and `.withDocs(...)` documents it.
- `outputUnchecked`
  - what: This transport switches output validation off with `{{how}}`.
  - fix: Declare the response with `.withOutput(schema)` and leave validation on; the framework checks every response against it.
- `rawApp`
  - what: This source constructs `{{name}}` itself.
  - fix: Declare the route with `defineRestRouter` or the procedure with `defineTrpcRouter`; the process builds and mounts the application.
- `rawContextAccess`
  - what: The handler reaches the raw request through `{{text}}`.
  - fix: Take only `{ input, app, actor, scope, signal }`; the framework resolves the request, the session and the headers before the handler runs.
- `rawContextField`
  - what: The handler takes `{{field}}` from its context.
  - fix: Take only `{ input, app, actor, scope, signal }`; the framework resolves the request, the session and the headers before the handler runs.
- `rawContextSpread`
  - what: The handler collects the rest of its context into `{{name}}`.
  - fix: Take only `{ input, app, actor, scope, signal }`; the framework resolves the request, the session and the headers before the handler runs.
- `rawHonoRoute`
  - what: This transport registers the raw Hono route `{{method}}()`.
  - fix: Declare the route with `defineRestRouter` or the procedure with `defineTrpcRouter`; the process builds and mounts the application.
- `rawResponse`
  - what: The handler constructs a raw `Response`.
  - fix: Return the plain value `.withOutput()` declares, or nothing for an empty response, and throw a `HandledError` to refuse; the framework serialises both.
- `rbacImport`
  - what: This transport imports the legacy RBAC module `{{specifier}}`.
  - fix: Declare access with `.withPermission(...)` on the route or procedure; AuthZ decides it.
- `responseMethod`
  - what: The handler calls the response method `{{name}}()`.
  - fix: Return the plain value `.withOutput()` declares, or nothing for an empty response, and throw a `HandledError` to refuse; the framework serialises both.
- `responseMutation`
  - what: The handler sets response state through `{{text}}`.
  - fix: Declare the status on the route with `.withStatus(...)`; the handler only returns a value or throws.
- `stringDispatch`
  - what: This transport exposes a generic `{{name}}(path: string, …)` dispatcher.
  - fix: Call the operation on the handler's `app`, or the typed client derived from the contract, so the compiler checks every procedure path.
- `stringPathCall`
  - what: This transport dispatches through the string path `{{path}}`.
  - fix: Call the operation on the handler's `app`, or the typed client derived from the contract, so the compiler checks every procedure path.
- `transportHeaders`
  - what: The handler reads transport headers through `{{text}}`.
  - fix: Declare what the handler needs with `.withInput(...)`, or read the caller from `actor`; headers are the framework's to resolve.
- `trpcInput`
  - what: This tRPC transport calls `.input(…)` outside the chain.
  - fix: Declare the procedure's input with `.withInput(schema)` so its permission applies after the input is parsed.
- `trpcRoot`
  - what: This source creates a tRPC root with `{{call}}`.
  - fix: Declare the procedures with `defineTrpcRouter`; the process owns the one tRPC root and applies each permission after the parser.
- `zodValidatorImport`
  - what: This REST transport imports `@hono/zod-validator`.
  - fix: Declare the request shape with `.withInput(schema)` on the route.

## `langwatch/unbounded-loop`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-unbounded-loop.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `unboundedLoop`
  - what: This `{{form}}` loop states no exit condition in its header.
  - fix: State whichever bound this loop already tracks directly in its header — a deadline (`while (now() < deadline)`) or an attempt counter (`for (let attempt = 0; attempt < MAX_ATTEMPTS; attempt++)`) — or, if it tracks no such bound yet, move it into a function whose signature takes one as a parameter.
  - why: When the exit lives in the body the reader has to find every return, break and throw to know when the loop ends.

## `langwatch/unit-test-does-not-render`

- Kind: problem
- Applies to: isGovernedTestFile
- Fixable: no
- Spec: `specs/tooling/lint-unit-test-does-not-render.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `unitTestImportsRenderer`
  - what: `{{specifier}}` renders components, and `{{name}}` is not named as an integration test.
  - fix: Rename the file to `{{target}}` and leave the content unchanged.
  - why: A test that renders a component and mocks its boundaries is an integration test.

## `langwatch/unresolved-relative-import`

- Kind: problem
- Applies to: every file
- Fixable: no
- Spec: `specs/tooling/lint-unresolved-relative-import.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `unresolved`
  - what: `{{specifier}}` names no file on disk.
  - fix: Point it at the file's new path, or delete the line if the file is gone; a move leaves these in batches, so resolve the whole file in one pass.
  - why: A stale `dist/*.d.ts` keeps diagnostics green, so only a test run finds it.

## `langwatch/web-imports-server-shaped-value`

- Kind: problem
- Applies to: applies
- Fixable: no
- Spec: `specs/tooling/lint-web-imports-server-shaped-value.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `serverShaped`
  - what: A browser module value-imports `{{name}}`, whose declarations are the server's and pull a database graph into the browser program.
  - fix: Import it as a type when only the type is needed; otherwise read the data through the owning module's tRPC client, derived from its contract.
  - why: Loading declaration files is the largest bucket in a type-check, and this class of import is invisible in the bundle.

## `langwatch/zod-internals`

- Kind: problem
- Applies to: isGoverned
- Fixable: no
- Spec: `specs/tooling/lint-zod-internals.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `defAccess`
  - what: `{{object}}._def` reads a Zod schema's internals, which differ between the two installed Zod majors.
  - fix: Export the un-refined schema object from the contract and build from it instead of unwrapping this one.
- `zodErrorInstanceOf`
  - what: `instanceof {{right}}` tests an error against one Zod major's class, and the other major's errors fail it.
  - fix: Test the error structurally instead: check `Array.isArray(error.issues)`.

## `langwatch/zod-object-composition`

- Kind: problem
- Applies to: isSchemaSource
- Fixable: no
- Spec: `specs/tooling/lint-zod-object-composition.feature`
- Enforced: yes, at `error`

Options: none.

Messages:

- `keepRefinements`
  - what: `.{{method}}()` is called on a Zod object that carries refinements.
  - fix: Write `.safeExtend({ ...fields })` here — a `z.object({ ...base.shape })` spread would drop those refinements without a type error.
  - why: Spreading its shape alone would discard those checks.
- `spreadShape`
  - what: `.{{method}}()` builds this schema's type through Zod's mapped `Extend` generic.
  - fix: Choose by what the base schema carries. When it is a plain `z.object()` with no `.strict()` or `z.strictObject()`, no `.catchall()` and no `.refine()` or `.superRefine()`, write `z.object({ ...base.shape, ...fields })` — spreading `...other.shape` for `.merge(other)` — since that is the only form that avoids the generic. Otherwise write `.safeExtend({ ...fields })`, which keeps the strictness, catchall and refinements that the shape spread drops silently.
  - why: Only the shape spread produces a fresh object type; `.safeExtend()` preserves behaviour but instantiates the same generic.
