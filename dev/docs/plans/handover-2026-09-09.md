# Handover, 2026-09-09 02:00: where the shape drive stands

**TL;DR.** The target shape is fixed and enforced by lint; the runtime that every module mounts on is complete bar the
tRPC round; 17 of 41 modules are fully on the shape, 7 partly, 17 not started. The tooling, plan and skills are
consolidated. The branch does not boot tonight because about 150 files still import the deleted legacy transport
builders; a lane is cutting those out of the process graphs so the stack comes up with the unconverted families
unmounted. About 1,150 uncommitted paths sit in the tree, most of them one stopped lane's agent conversion; a
review lane is classifying them. Full state, counters and decisions: `strict-feature-layout.md` (the one plan).

## The final shape (what "done" looks like)

```
modules/<module>/            (folder still named features until D-a lands; the word is "module")
  contract/src/  <m>.api.ts (interface <M>Api + featureApi token)  <m>.trpc.ts (defineTrpcContract)
                 schemas, errors (HandledError subclasses with codes), no abstract service
  server/src/    <m>.server.ts = defineFeature(<m>).withRepositories(registry).withApp(App).withTransports(...).build()
                 app/<m>.app.ts (one class implements <M>Api; static dependencies name peer APIs)
                 services/ (private), rules/, ports/ (genuinely external: eventing, metrics, HTTP, flags)
                 repositories/{<x>.repository.ts, prisma/, memory/, <m>-repositories.registry.ts}  (twins under one contract test)
                 transport/<m>.trpc.ts, <m>.rest.ts (flat declarations on @langwatch/api; never mounted here)
  web/src/       model/ behavior/ ui/{elements,blocks,sections}  flat entry files src/<id>.ts
apps/api/src/features/<m>/   <m>.composition.ts (installApi<M> boots createApp...withFeature), <m>-{trpc,rest}.mount.ts
apps/worker, apps/tasks      boot the same installer with their own infrastructure
packages/api/                the one runtime: rest/{declaration?,runtime,addressing?,request,idempotency?,credential,response,openapi,security}, trpc/{runtime,policy,audit}
```

Rules that hold everywhere: one runtime per transport; annotation is the reference module; no refusing twins (a process
installs a module or does not); no `try*` (nullable only on `find*`); config schema-driven, `process.env` only at boot;
ksuid ids; `{ ok, error }` results banned; folders of at most 12 files, no fragments under 20 lines; baselines only shrink
and share one shape; every scenario bound by `@scenario`.

## Good (landed, keep)

- `@langwatch/api` runtime: rounds one, two, three A and B. Doors (project, organization, internal secret, SCIM, session,
  instance admin), access kinds (permission, route-scoped, authenticated, public, optional, deferred), addressing (dated,
  v1-only, v1-in-path with generation, literal), raw bytes in and out, HEAD/any-method, 405 guards, multipart, declared
  answers, rate limit, cache, route cross-check. 357 unit tests. Legacy builders deleted.
- Modules fully converted: annotation, dashboard, data-privacy, data-retention, entitlement, feature-flag, notification,
  platform-health, secret, share, sso, suite, topic, dataset, monitor, api-key (Kimi), log and metric bar a registry row.
- Lint rebuild: one report, one baseline shape (`{version, policy, entries[{key, measured, expires?}]}`), one workspace
  snapshot, 12 s a run. `feature-shape` (13 kinds) and `source-folder-shape` are the ratchets that define done.
- Process wiring waves 1 to 4; the plan reduced to one document; skills reduced to `module`, `module-review`,
  `architecture-guide` in one root with module vocabulary.

## Bad or half-done (know before you touch)

- **Boot.** Every process graph imports dead legacy transports through module indexes; the boot-graph lane is removing the
  exports and mounts (unconverted families become unmounted, one log line each). Until it lands, `haven up` fails at the
  tasks catalogue and the api. `apps/api` has ~1,600 type errors, `apps/worker` ~760, all in unconverted families and
  the pile.
- **`packages/api/src/rest/runtime.ts` is 3,736 lines.** D-o (ten-file split, `declaration.ts` first) is overdue.
- **Partly converted:** evaluation 4/7 (legacy family now declarable with `literal`), stored-object 6/8 (`/api/files`
  now declarable), evaluator 6/9 (15 packages type against its abstract service), authz 3/8 (rename across ~150 files
  queued for a quiet tree), user 3/8 (tRPC round C), audit-log/presence/role wait on a test database URL (D-e).
- **Not started (17):** project, organization, agent, prompt, model-provider, workflow, github, langy, analytics,
  automation, billing, governance, licensing, identity, navigation, onboarding, plus hosted-mcp, webhook, gateway, scim,
  experiment, trace, ops, auth, coding-agent now unblocked by round three.
- **The uncommitted tree (~1,150 paths).** Agent conversion (216, 09-07, stopped lane), enterprise webhook (73), trace
  (63), scenario `ScenarioApi`/`PromptApp` rework (55), langy/workflow/governance/eventing/thuishaven, twelve untracked
  worker compositions, api-key (Kimi, live). A Sonnet review classifies each as land / hand back / drop; report at
  `$CLAUDE_JOB_DIR/tmp/tree-review/report.md` when it finishes. Nothing there is committed; nothing there is deleted.
- Wave 5 leftovers: monitor's production install (replication peer stranded on the evaluator graph), scenario
  composition (needs the uncommitted rework), evaluation-read composing a partial, dataset backfill task, OpenAPI
  regeneration once `apps/api` compiles. Main has 47 commits not merged.

## Bin, refactor, change

- **Bin:** every `transport/api-{rest,trpc,ws}/*.api.ts` written against the deleted builders, once its flat declaration
  exists (they are lift sources until then); `refusing*Feature()` twins and `testing.ts` entries wherever they remain;
  `packages/api/README.md`'s description of the deleted builders; stale composed-exports rows as they go stale.
- **Refactor:** `runtime.ts` split (D-o); `request.ts` loses idempotency; `EvaluatorService`/`EvaluationService`
  consumers onto the APIs; the worker composes modules by booting installers instead of hand-building Prisma adapters.
- **Change (decisions for Alex):** D-a modules tree rename (after Kimi), D-b the pile's owner, D-e test database URL,
  D-i repository backends beyond Postgres (ClickHouse, Redis), D-o, D-p Langy kept as-is bar seams and persistence,
  D-q handler-validated credential access kind, lint D1 to D7.

## How to continue

Lanes: Opus for conversions and runtime design, Sonnet for mechanical work; brief the shape, never the number; lanes never
run git or root typecheck; the root session commits path-scoped slices (`$CLAUDE_JOB_DIR/tmp/commit-slice.sh`,
`commit-slice2.sh` with HEAD-variant overrides for files carrying others' hunks) and owns the baselines
(`drop-rows.mjs`). Conversion procedure: `.claude/skills/module/references/convert.md`. Board: plan section 4.

## The giants, and the install system (Alex, 02:2x)

The final shape has no room for them, and the ratchets exist for two of the three:

- **Folders of 50 files.** `source-folder-shape` refuses more than 12 files per folder and fragments under 20 lines;
  352 baseline rows are the burn-down list and only shrink. A conversion lane pays its module's rows as it goes.
- **`apps/api/src/app/api-production.composition.ts` (about 4,400 lines).** The install system is not missing: wave 4 put
  nine modules behind `await installApi<Module>` in `apps/api/src/features/<m>/<m>.composition.ts`, and the
  boot-graph lane then removed the legacy tRPC record and about 800 lines of process-side port builders that fed it.
  What is missing tonight is the **tRPC surface for the converted modules**: their `*-trpc.mount.ts` files are
  off-graph because the record that used to mount everything was legacy. First wave-5 item: a `TrpcRuntime` record in
  `apps/api/src/app-trpc/` that mounts only converted declarations. api-production itself shrinks one module at a time:
  the end state is an ordered list of installs and nothing else, a few hundred lines; each `compose<X>` method leaves as
  its module lands. Nothing in the shape lets a composition hold logic.
- **`packages/api/src/rest/runtime.ts` (3,736 lines).** No file-size ratchet exists yet. Two actions: approve D-o and run
  the split; add a `file-budget` policy to the lint rebuild (refuse files over a fixed line count, one baseline shape,
  shrink-only) so no file grows past it again. Add it as lane L9 in `architecture-lint-review-2026-09-08.md`.

## Boot graph, remaining edges at 02:2x

Tasks loads. The api graph stops at `apps/api/src/app-rest/app-rest.packaged-families.ts` importing
`createDatasetRestApp` from `@langwatch/dataset-server` (deleted; the dataset family is mounted through
`composed.dataset.rest` since `9697edd1f5`, so the import and its `mount("dataset", …)` block go). Expect a few more
of the same shape in that file and in `apps/api/src/index.ts`; the oracle is
`node --experimental-transform-types --input-type=module -e "await import('./apps/api/src/app/api-production.composition.ts')"`.
The worker stops at the untracked `apps/worker/src/app/worker-evaluation-app.composition.ts:20` (`PostgresDatasetAdapter`),
which belongs to the 09-07 pile (D-b). The boot-graph lane's own record is
`dev/docs/plans/boot-graph-after-legacy-transport-delete.md`.

## Boot graph at 14:2x: every process boots, the api answers

Commits `e6e9202953` (worker healthz 200, UI served, every graph loads) and `ffd77bba33` (api listens). The api boots
with `node --experimental-transform-types --env-file-if-exists=../../.env src/api.entrypoint.ts` from `apps/api` and
logs `API HTTP listener started`; `/api/health` answers 204, the five probe routes 401 without a key and 405 on POST.
The UI sits on its loading screen until that answers, as built.

What "boots" hides: **66 REST families were declared absent** rather than served at 14:2x (61 after `f8503032ea` and the root-discovery commit brought gateway-openapi, api-discovery, root-discovery, rum and image-proxy back), one WARN line each at boot
(`API process serves no <family> REST family: its transport is still written against the deleted REST builders`).
22 in `app-rest.packaged-families.ts` (survivors `dashboards`, `evaluators`, `monitors`), 37 in
`app-rest.process-features.ts` (survivors health, platform-health, annotations, stored-objects), 7 in
`composeDoors` (gateway-platform, gateway-spend, gateway-internal, elevenlabs-webhook, api-keys, billing-webhook,
sse-subscriptions). Each family comes back when its module converts to `defineRestRouter` and mounts through
`createRestRuntime`; the red tests under `apps/api/src/app-rest/__tests__` are each conversion's checklist
(`expected 404 to be 200` is the unmounted family, `createAppRestSecurity is not a function` is the deleted builder
still named by `apps/api/src/app-rest/index.ts`, `api.application.ts` and `support/rest-family.harness.ts`).

Two details worth knowing. Cron (`/api/cron/old_lambdas_cleanup`) was left absent on purpose: its gate is a
builder-level shared-secret middleware on an `internalSecret` door, and a destructive door with a subtly wrong gate is
worse than no door. And `featureApi<HealthProbeApi>("platform-health")` is the first `featureApi` call outside a
contract package; if the feature-layout lint objects, the token moves to `@langwatch/platform-health-contract`.

The one runtime fix: a fatal composition failure used to hang for 120 s while the ops snapshot reader polled a closed
Redis. The reader is now owned by the graph's resource scope and re-checks `stopped` after its first read, pinned by a
regression test in `modules/ops/server`.

## State at 16:3x, before the design refinement (Alex: "refine it with you, then clean up and migrate in one go")

Every lane is landed and the tree is committed bar Kimi's api-key work and nine untracked scratch paths (a 30 MB `haven`
binary, `file`, `w3b-server-tail.oxlintrc.json.tmp`, `apps/ui/.tsprobe/probe.ts`, and five `dev/scripts/commit-*` /
`annotation-cleanup.*` / `patches/` files from an earlier session). The standing loop is stopped. No lane runs.

| Counter | Now |
| --- | --- |
| Feature-shape baseline | 242 rows, 38 modules, 24 on legacy transport |
| Source-folder-shape rows | 351 |
| REST families declared absent at the api's three mount sites | 59 (of 66 at 14:2x) |
| Non-test files still calling a deleted REST or tRPC builder | 166 |
| `api-production.composition.ts` | 4,836 lines |
| Commits on the branch since the merge-base `105613d379` | 2,067 |

Landed this afternoon, in order: the api boots (`ffd77bba33`), D-o split (`bc2a49abdf`), Part C tRPC (`913e0feaec`,
wired `83a286af30`), five process doors (`f8503032ea`, `0192b0273e`), hosted-mcp (`20b38fcbbb`), stored-object files
(`15df613d74`), licensing tRPC (`2c840d6c31`), twenty dead files deleted (`099d7aaf3f`), the 09-07/08 pile committed
as work in progress (`9015d8420f` to `a2a89a1977`).

What the afternoon's lanes kept tripping on, which is the input to the refinement:

- **Three absence sites and a family-name union each.** `app-rest.process-features.ts`, `app-rest.packaged-families.ts`
  and `composeDoors` each keep their own `mount(name, null)` list and `*FamilyName` union; a family returning edits
  two of them. One registry of doors, declared by the module, is the shape.
- **Ports are re-plumbed per mount.** Every `*-rest.mount.ts` and `*-trpc.mount.ts` in `apps/api/src/features` builds a
  `createRestRuntime` with its own `identity`, `facts`, `onError`; the process should build the runtime once and a
  module should only bind facts and hand over its application.
- **`featureApi` owner labels for doors that are not modules** (D-r).
- **`installer-not-booted` rows.** Modules define `defineFeature(...).build()` and the api still composes their
  applications by hand in `api-production.composition.ts`; the install system (`installApi<M>`, `createApp(...).boot`)
  is driven for nine modules and bypassed for the rest.
- **33 mount and transport files imported by nothing** (13 `apps/api/src/features/*-rest.mount.ts` and
  `*-trpc.mount.ts`, 3 enterprise tRPC compositions, 17 module `*.api.ts`), kept as the port-wiring record for the
  one-go migration.

## The door registry landed (17:4x, `5fbde5a46b`)

`apps/api/src/app-rest/api-rest.runtime.ts` builds the api's REST runtime once (which of the seven doors the process
opens; the projectKey door's credential resolution, refusal, scope, actor and facts). `api-rest.doors.ts` is the one
list of 77 families with their paths: a converted family is one entry, `{ family, owner, paths, mount: ({ runtime,
services }) => services.x ? [runtime.mount(xRest.router(), services.x)] : null }`; `null` means "composed no
service", no `mount` means "transport still on the deleted builders", and one `LoggedApiRestAbsence` names both at
boot. The two mount-list files are now `api-rest.services.ts` and `api-rest.packaged-services.ts` (types only), six
mount files folded into entries, every other mount shrank. Idempotency has no seam on the declared runtime yet
(`RestRuntimePorts` wants a port beside `rateLimiter` and `cache`); `apps/api/src/app-rest/index.ts` still re-exports
the deleted security names, which is what the 30 red `app-rest/__tests__` files sit on.

Conversions now run module-side only (contract, server transport, tests, `withTransports`) and report their mount
lines; a wiring wave applies them to the registry. Landed module-side so far: billing (`47d8b896d4`), github
(`e7e7daf5e3`), api-key REST (`47b850f1ea`); their notes are under `$CLAUDE_JOB_DIR/tmp/wiring/`. Wiring wave 1 is
running; the api does not boot between the two (billing's deleted `createStripeWebhookRestApp` import).

## The uncommitted tree, reviewed and landed (02:5x)

The Sonnet review (`$CLAUDE_JOB_DIR/tmp/tree-review/report.md`, 47 clusters) found one coherent sweep, not a pile: a
repo-wide `*Service` → `*Api` rename, the agent module conversion, webhook and audit-log leaving enterprise, a worker
composition split, and a long tail of independent clusters. About 40 clusters were LAND and are now committed one slice
each ("Land the <cluster> work left uncommitted on 09-07/08"). Fixes they still need, in their own modules:

- webhook: `webhook.server.ts`'s `defineFeature(...).withApp(WebhookApp)` does not typecheck;
  coding-agent: `coding-agent.server.ts` calls `createRestRouter`/`createTrpcRouter`, which do not exist (breaks the
  worker cluster transitively); simulations: the `AgentCodeEditorDrawer` mount. Done 14:4x in `48f66f751b`: the
  managed-provider `listPaths` stub and the installed UI features list; the agent `super()` call was already gone
  (the classes now `implements` rather than extend) and `table-catalogue.ts` was already current. The agent server's
  22 red unit tests are the deleted REST builders, not the cluster.
- HAND BACK (not landed): experiment (15 of 17 tests fail, a dependency-shape refactor left half-finished), audit-log
  (catalogue names a server package that does not exist yet), docs-site (asserts a migration that has not happened),
  the CI workflow (two owners: the webhook rename is ready, the `@langwatch/platform-api-client` rename is not), the
  `apps/api/src/app*` composition root (still moving).
- DROP (untracked scratch, left on disk for Alex to delete): a 30 MB `haven` binary, a stray `file`, an oxlint scratch
  config, `apps/ui/.tsprobe/probe.ts`; `dev/scripts/annotation-cleanup.*` is provisionally stale.
- Six missing names at HEAD explain most remaining red: `createTrpcService`, `AppRestSecurity` and friends,
  `EntitlementService`, `SecretService`, `PostgresDatasetAdapter`, `DatasetService`. All are the runtime and contract
  work of this drive, not the clusters'.

## State at 21:1x, before the compaction (Alex: "refresh current state, update the plan doc, compress context before the next tranche")

Landed since 18:4x, all on `feat/strict-feature-layout-v0`, HEAD `9c7cb5ff5e` plus the tool and doc commits after it: auth `f93dbe497d`, user `05f9ece62f`, wiring wave 2 `1ee7bc0204`, Part E `a8e843e6ba`, automation `d153b25f42`, prompt `afcd43d2b9`, workflow `805e80d019`, ops `f63cf4341e`, wiring 3a `86062317fe` (api boots), worker user record `7f8e31ed2a`, the naming lint widened and the remap ladder `e4a879b90c`, nine web tRPC maps derived from contracts (Haiku), three `try*` renames (scim, webhook, coding-agent), the entitlement repoint, `prompt_handle_taken` registered, dead api re-exports pruned.

The counters and the live lanes are in `strict-feature-layout.md` sections 2 and 8. What the compaction must carry:

- **The app is not usable yet.** The api listens, but the tRPC record is still `undefined` in `api-production.composition.ts`, so `/` flickers "could not load workspace" and `/auth/signin` renders blank waiting on the `publicEnv` query. Auto-mount flips the record live per namespace; the `publicEnv` brief removes the procedure. Both before the test pass.
- **Alex runs his dev stack on the working tree**, which always carries in-flight lanes' edits; every restart on a lane's save shows a half-applied tree. A detached worktree at committed HEAD (`git worktree add --detach ../langwatch-run <sha>`, `make haven up` there) shows committed state only. Each tick names the sha.
- **The order from here:** auto-mount lands, then `publicEnv`, then wiring 3b as install lines, then the test pass (curl every mounted family, drive sign-in, home, traces, prompts, settings in the browser, log failures by module), fixes, then the persistence wave and the remaining eight conversions.
- **Model policy** (`tmp/model-compare.md`): Opus chooses shapes, Sonnet does cross-file renames and repoints, Haiku does single-file remaps; every remap lane runs `references/remap.md`; the naming lint now reads every module source file.
- **Machine hygiene:** vitest forks orphaned by lanes' test runs were the memory sprawl; briefs say never background or time-limit a vitest run; the root sweeps pid-1 workers between rounds.
- **apidiff** runs main against committed HEAD from `$CLAUDE_JOB_DIR/tmp/apidiff/out`; the first run flushed Redis DB 0 on the dev server (the tool derived 0 from the work-root name; fixed so it never can). A Haiku lane reads the report; the root session decides actions.
- **Security items still open:** the SCIM Enterprise plan gate (`.withEntitlement("enterprise")` exists since Part D, not attached), the organization door asking by-id permissions at both tiers, three `UserService` members the passkey and SCIM flows call that `UserApi` does not carry.

## State at 23:5x (head 81ca1564af)

Landed tonight, in order: auto-mount `8303615a73`; Prisma client un-committed `a054de0589`; tree rename
`3eb6c56657`; references generator `6edf217226` and its write `5f9cf03ae9`; publicEnv close-out
`b296291289`; structured logs `e28c2e6cb2`; vocabulary rename `ade8e7c115`; three derived web maps.
The tree is `modules/<m>/{contract,server,web}` and `enterprise/{modules,packages}`; the vocabulary is
`defineModule`/`withModule`/`runtime.module`/`moduleApi`. Baselines were re-keyed and re-sorted.

Running: catalogs (Sonnet); api-key, project, user persistence lanes (Opus); ops and workflow web maps
(Haiku). Queued in order: wiring 3b install lines, then the test pass, then more Tier B, Tier A, the
main fold (a porting wave, 1,088 conflicts), apidiff fixtures, public config per module.

Rules learned tonight: a commit slice taken while lanes edit sweeps their in-flight hunks (the rename
commit carried three lanes' partial edits; harmless, but say so in each lane's report); commit through
`tmp/commit-slice-fast.sh` (one scoped `git add` into a temp index, seconds for 12k paths, never the
per-file loop); apidiff instance A is this branch and B is main; Haiku is clean on one-file remaps and
not on anything wider.
