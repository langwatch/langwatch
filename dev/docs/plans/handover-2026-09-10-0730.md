# Handover at 07:3x on 2026-09-10

**Method first.** A fresh session reads `dev/docs/plans/coordinator-prompt.md`
section 1 before this file and follows it as its instructions: the coordinator
spawns lanes, inspects every 15 minutes, reviews and commits by slice, and
reports the counter line every 30 minutes with a closer/same/further verdict.
It does not read module code, lane transcripts or whole-app typecheck output
into its own context, and never runs a whole-tree typecheck. Lanes run on
Sonnet and use tslsp-cli for every identifier. Every conversion targets the end
shape directly, never an interim one (Alex, 08:0x).

**Counters that are finished, do not hunt for them:** platform/app lines left
is 0 (directory deleted 2026-09-03); UI loader keys left in apps/ui is 0 (all
49 moved 2026-09-02); the `API_UNAVAILABLE_PRODUCT_ADAPTERS` list no longer
exists. The live counters are the one line `bash dev/scripts/shape-counters.sh`
prints.

**08:0x:** Codex was killed; its claims are released and its uncommitted
gateway, doors and api-production edits sit in the tree for the coordinator to
land by slice. Lanes running: wire-identity, door-query, door-organization-2,
trace-transport (briefs in the job scratch directory, `prompt-*.md`).

Branch `feat/strict-feature-layout-v0`, HEAD a5102f30b6. Counters at HEAD
(`bash dev/scripts/shape-counters.sh | tail -1`): feature-shape rows 79,
families absent 37, ports/adapters files 551, api-side files 176, modules on
the legacy transport 6.

## The goal, unchanged

Every module on the annotated runtime: `defineModule(...).withRepositories(registry)
.withApp(App).withTransports(...)`, persistence as `repositories/{prisma,clickhouse,
redis,memory}` behind `defineRepositories` (no `ports/` or `adapters/` for
persistence), one install line per module in the process composition, and
`apps/api/src/app/api-production.composition.ts` (4,300 lines) shrinking to those
install lines. The wire is pinned to origin/main.

## Where the remaining work is

1. **Eleven REST doors without a `mount:`** in `apps/api/src/app-rest/api-rest.doors.ts`
   are exactly `families_absent`. Every module side is already a `defineRestRouter`
   family; only the api mounts were dead (they imported `AppRestSecurity`,
   `createAppRestSecurity`, `SecuredApp`, `createTrpcService` - names that no
   longer exist and are not to be recreated). Mounts landed for langwatch-ql,
   evaluations-legacy, model-providers, workflow-run and workflow-studio; their
   door entries and api-production lines are queued (below) because Codex holds
   both files (`.claims.tsv`, `codex-shared-wiring`). Lanes still running for
   organization (organization-management, organizations) and trace (otlp-ingest).
   langy, governance-ingest and gateway-spend sit inside other claims. The
   `query` door is dead too (`query-rest.mount.ts`) and has no lane yet.
2. **The legacy REST seam in apps/api** goes once all doors are declared:
   `apps/api/src/api-rest.security.ts`, `apps/api/src/app-rest/index.ts`,
   `apps/api/src/app-trpc/app-trpc.sse.ts`, the `app-rest/__tests__/support/*`
   worlds, and every test that imports `createAppRestSecurity`
   (`$T/missing-importers-present.txt` lists the 51 files as of 07:1x).
3. **Persistence still under ports/adapters** (551 files): trace (executor
   running from `$T/plan-trace.md`), identity (executor running steps 5-11 from
   `$T/plan-identity.md`), governance (running), automation step 7-9
   (`$T/plan-automation.md` lines 419-613, blocked on worker-production lines),
   billing items 1 and 3, stored-object item 3, ops usage-stats rows not yet in
   `OpsRepositories` (ClickHouse-requirement ruling pending).
4. **Optional-store rows** (Redis/ClickHouse the api may lack) cannot join a tier
   that `requires` them: github, langy analytics, billing, ops, topic. Needs one
   ruling: optional store in `defineRepositories`, a third tier, or a null-object
   connection. Trace took option (a) for editOverlay: resolve the tier, overwrite
   one row.

## Queued handover lines (apply when Codex releases the files)

`/Users/afr/.claude/jobs/eeb488e6/tmp/handovers-queue.md` holds the verbatim
text. Summary:
- api-rest.doors.ts: `mount:` entries for langwatch-ql, evaluations-legacy,
  model-providers, workflow-studio, workflow-run (imports listed with each).
- api-production.composition.ts: `...(this.composedEvaluation ? { evaluations:
  this.composedEvaluation } : {})` in the composeApiPackagedRest call (required,
  TS2741 otherwise); `workflowRun = workflowService ? { workflows: () =>
  this.composedWorkflow.app } : undefined` at ~2076; billing L63/L4223
  `PostgresBillingAdapter` -> `PostgresBillingRepositories.create({ prisma })`;
  langy tokenBuffer L2885/L3895 -> `repositories.tokenBuffer.open({ redis })`;
  organization.composition membership/settings lines.
- api-authoring-rest.composition.ts ~108: `composition.workflowStudio = {
  session, workflows: () => workflows }`.
- Codex's dirty tree copies of `api-rest.services.ts`,
  `api-rest.packaged-services.ts` and `api-packaged-rest.composition.ts` also
  carry the evaluation door's hunks (dropped dead collaborator types, added
  `evaluations`); they land with Codex's next commit of those files.

## Rulings made by the coordinator this window

Identity Q1 = IdentityApi token in the contract; Q3(c) = ssoConnectionLedger
nullable like mail, app refuses by name when absent, composeBackoffice joins the
repoint list. Trace Q1 (a), Q2 hand-built legacy read, Q3 refused (step 3c
skipped), Q4 tail repair authorised. Baseline rows move only on detector evidence
(`$T/shape-findings.mts`, `$T/folder-baseline.mts` dry-run then `apply`).

## Open for Alex

Governance Q1 (booting governance in the api is a behaviour change); six
ClickHouse table-ownership questions; the optional-store ruling above; trace tier
key naming; whether `persistence` becomes name-only at runtime.

## Lanes running at 07:3x (their edits are uncommitted in the tree)

door-organization, door-trace-otlp, exec-trace, exec-identity-2, exec-governance.
Each writes a report with handover lines; land by pathspec after
`$T/check-imports.mjs` and a HEAD import check, never `git add -A`. Claims in
`.claims.tsv`; release with `bash dev/scripts/claim.sh done <name>`.

## Operating rules that bit this window

Slice lists subtract other claims' paths (a lane's vitest.config.ts was swept
once). Drop a baseline row only when the detector lists it STALE at HEAD. Lanes
run on Sonnet (execution) and Haiku (single-file jobs, inventories); Opus only for
plans. Identifier work through `npx --no-install @0xdeafcafe/tslsp-cli` from the
package directory; `rtk ` prefix on pnpm, vitest, git and grep.
