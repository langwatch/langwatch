# Handover, 2026-09-11

At `4397738ef8`. Thirteen commits this session (`442b11c726~1..4397738ef8`).
Nothing pushed, no PR.

This is a snapshot, not a log. Under 200 lines on purpose - the drive that
preceded it cost $28.7k partly on handovers nobody could act on. It was 150
until the classification lane landed; the cap was raised deliberately rather
than met by cutting findings, which is the failure mode it exists to prevent.

## Counters

```
dirty=43  feature_shape_rows=0  ports_adapters_files=35  port_names=30
port_word_files=86  families_declared=80  families_unbound=47
main_unfolded=85  api_boot=no-stack
boot_shape_undefined=26          <- added 2026-09-11, see the correction below
```

`boot_shape_undefined` counts boot-shape call sites naming an `ApplicationBuilder`
method that does not exist. It derives the defined-method list from
`application.ts` rather than hardcoding it, so a name retires itself the moment its
seam lands. It exists because **every other counter here is a static grep, and all
of them read green while 16 of 21 module installation tests failed.**

`feature_shape_rows=0` - **that drive is finished.** Any brief describing module
conversion to the annotation shape as the work is stale.

`main_unfolded=85` is commits behind `origin/main`, growing ~17/day. Not a task,
a clock.

## What landed

- **The agent workflow protocol**, tracked: `.claude/coordinator/` (COORDINATOR,
  LANE, manifest + handoff templates), `.claude/skills/core/` (repository,
  testing, handoff rules), `.claude/agents/lane.md` (a lane runs with no `Agent`
  tool, so "no subagents" is a constraint). `.claude/manifests/` and
  `.claude/handoffs/` are gitignored runtime state, as is
  `.claude/coordinator/LANES.md` - the live-lane roster, added `7963972aba`,
  which is what makes "can this session be replaced?" checkable.
- **Enforcement**: `packages/architecture-lint/tests/agent-workflow-protocol.unit.test.ts`
  binds 5 structural scenarios (7 tests, 276ms, CI-safe).
  `skills/_tests/agent-workflow-protocol.scenario.test.ts` binds 4 behavioural
  ones (real Claude Code session, `skipIf(isCI)`, **never run - costs money**).
  Spec: `specs/claude/agent-workflow-protocol.feature`, 9/9 bound.
- **Drive to zero**: 188 → 43 dirty.
- **Docs**: `composition-by-size.md` (the root at four sizes, verbatim from the
  tree), `main-merge-2026-09-11/` (the merge ledger), `worker-composition-proposal.md`.

## The blocker - CLEARED at `0d17f1a7de`

`packages/runtime-composition` typechecks; 131 tests / 14 files green.

It was two false positives and one decision, not three of a kind. The two were
`ModuleConfigGuard` refusing a module whose name had been widened to `string`,
including a **valid** call; `ModulesMissingConfig` now skips a name it cannot
identify, on the rule that a guard that cannot know must not refuse. The third
was real: `@ts-expect-error` at `define-feature.unit.test.ts:93`, because config
from JSON or the environment was never type-checked and the runtime parse stays
load-bearing. TypeScript errors on an *unused* `@ts-expect-error`, so that line
is now the guard's only test that it still refuses.

## The proposal, and the one thing that would falsify it

`dev/docs/plans/worker-composition-proposal.md`. In one line: the api root is
**183 lines** and the worker's is **13,725 across 64 files**, and the 184 bespoke
collaborator fields in fourteen modules are the mechanism keeping it that way -
so ratchet them down with a shrink-only baseline, the play that already took
`feature_shape_rows` 392 → 0.

**Its unproven claim was tested on 2026-09-11 and survived.** All 200
constructions in `worker-production.composition.ts` classified: (a)+(b) beats
(c) **78% to 22% on count, 70% to 30% on lines**. My "153 constructions" was
wrong - it missed 51 delegate calls. Full numbers in the proposal.

The stronger finding the proposal now leads with: **476 lines of that file are
class *implementations*, not constructions.** The root does not merely build its
modules' collaborators, it defines them.

Open question it raised: 32 constructions / 449 lines are one shape - a
per-module absence reporter x13, ambiguous between module-owned and process
observability. The ratchet needs it as its own baseline row, or the first lane
there decides it by accident.

## Exact next action - SUPERSEDED 2026-09-11 ~14:40, read the correction

The action below was **wrong on its premise** and is kept only so the correction
makes sense. It said `cv2-boot-shape-stragglers` was blocked on the guard decision
and that 7 installation tests were what stood between the tree and zero:

> Spawn `cv2-boot-shape-stragglers` (sonnet). It was blocked on the guard decision
> and is not any more: 7 installation tests still call `.withInfrastructure(`.

Neither half held. That lane was blocked on the **member seam**, not the guard, and
the guard clearing changed nothing for it. And the scope was not 7 files.

**What was measured instead** (at `6436e4668c`, by running every one of them):

- **16 of 21 module installation tests fail.** Only annotation, data-privacy,
  dataset, notification and role pass.
- `ApplicationBuilder` defines four `with*` methods. **Three more are called and
  implemented nowhere**, across 26 files: `withInfrastructure` (20, deleted),
  `withModule` (16), `withPersistence` (13). The last two are documented as the
  canonical boot shape by `packages/architecture-lint/src/policies/feature-shape.ts:83`,
  which is why they read as existing API.
- The two spellings of the same bag disagree: 4 tests pass `members:`, 6 worker
  roots pass `infrastructure:`.
- `application.ts:443`'s `members: membersFor(...) as Members` is why none of this
  refused. `membersFor` returns only the `reads()` names, `reads()` is bounded to
  the 14 canonical `ProcessMembers`, so a module whose Members slot is its own
  bespoke bag gets a frozen `{}` **cast** to that bag: compiles clean, undefined at
  runtime.
- "Converted" never meant "runs". `user` and `data-retention` import their
  `create*TestInfrastructure` fixture and never call it; `topic`'s helper
  references an undeclared `role`. All three were counted as done.

**The premise the blocked lane stopped on was wrong and must not be revived.** It
concluded the six bespoke-bag modules each needed a per-field design call mapping
their collaborators onto the 14 canonical members. They do not. A module's own
collaborators are the second type parameter of `FeatureSetup<Dependencies, Members,
Config, Repositories>` - per-module, not process-wide - and `withModule(module, {
members })` is how a process supplies them. `monitor` and `feature-flag` already
spell the call out.

**Two lanes were spawned on this**, both recorded in `.claude/coordinator/LANES.md`:

| Lane | Model | Scope |
| --- | --- | --- |
| `cv2-with-module-seam` | opus | implement `withModule` + the three decisions (the key name, refusal for an unsupplied bag, the cast). Keystone - 10 tests and 6 worker roots wait on it |
| `cv2-installation-mechanicals` | sonnet | the 6 failures independent of the seam: secret, share, suite (no member source), metric (no memory tier), topic (`role`), presence (registry provider shape) |

`withPersistence` (13 files) is **not** covered by either and needs its own lane
once the seam's semantics are settled - it is the same shape of hole.

Do not start the merge first. It is 85 commits and growing, but it needs a tree
at zero.

## Then, in order

1. ~~Decide the `ModuleConfigGuard` question.~~ Done, `0d17f1a7de`.
2. Build the ratchet: `bespoke-members-baseline.json`, a lint rule refusing a new
   bag field, `bespoke_member_fields` in `shape-counters.sh`.
3. The 7 installation tests, on their bags. Tree reaches zero.
4. The merge - `dev/docs/plans/main-merge-2026-09-11/README.md`. **Regenerate the
   ledger first**; its 1,282 conflicts were measured at `a08cbd27b8`, 13 commits
   ago.
5. Worker onto `createProcess` + `serverModules`; pay down the bags largest
   first (trace 29, automation 26, langy 21, gateway 14).

## Three stale claims found, do not trust the 09-10 documents

| Claim | Reality |
| --- | --- |
| 551 files under ports/adapters | **35** |
| `withProvided` was removed, 193 call sites broken | it is at `application.ts:355`, all 112 sites resolve |
| 7 installation tests are what stands between the tree and zero | **16 of 21 fail**, across three missing builder methods in 26 files; and the 6 with bespoke bags need no per-field design call |

Measure before designing from any of them. The third row is this document's own
`Exact next action` from earlier the same day, which is the point: a next action
written from a lane's stated blocker rather than from a measurement inherits the
lane's wrong premise. The lane said "architecture decision needed"; running the
21 tests took four minutes and said something different.

## Open lane work

`.claude/manifests/`, with handoffs beside them in `.claude/handoffs/` - both
gitignored, both still on disk:

| Manifest | Model | State |
| --- | --- | --- |
| `cv2-boot-shape-stragglers` | sonnet | **superseded** - its premise was wrong; replaced by `cv2-with-module-seam` + `cv2-installation-mechanicals` |
| `cv2-with-module-seam` | opus | spawned 2026-09-11 14:38 |
| `cv2-installation-mechanicals` | sonnet | spawned 2026-09-11 14:38 |
| `cv2-port-word` | sonnet | ready, independent of everything |
| `cv2-application-member-record` | opus | T1 |
| `cv2-rest-credential-object` | opus | T3 |
| `cv2-peer-seam` | opus | **premise is stale, verify before designing** |
| `cv2-worker-root-classification` | sonnet | collected, complete |

`.claude/coordinator/LANES.md` says which are running. Empty means this session
can be replaced; a row means it cannot.

## Risks

- **Four peer sessions were live** in this checkout all session. They had not
  written in 17h when I started, which is why committing was safe. That may not
  hold - check before committing anything wide.
- A lane created **four backwards-compatibility re-export aliases** and reported
  them under "wire differences: none" as evidence of safety. I caught it reading
  the diff and repointed 13 occurrences. The structural test tier cannot catch
  this; a lint rule could.
- `api_boot=no-stack` all session, so no lane verified a boot log. Every brief's
  "read `haven logs backend`" step has been a no-op.

## Starting the next session

Type `/coordinator` in a fresh session here. Nothing to open, nothing to paste.

It reads the state off disk rather than carrying it in prose - the lane roster,
this handover's next action, the shape counters - so it cannot quote a number
that has gone stale, which the paste it replaced could and did.

A session in this checkout also reports the same state on its own at startup,
through the `SessionStart` hook in `.claude/settings.json`
(`dev/scripts/coordinator-state.sh`). It prints nothing when no drive is in
progress, so it costs the other sessions here nothing.
