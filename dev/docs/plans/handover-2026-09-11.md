# Handover, 2026-09-11

At `4397738ef8`. Thirteen commits this session (`442b11c726~1..4397738ef8`).
Nothing pushed, no PR.

This is a snapshot, not a log. Under 150 lines on purpose - the drive that
preceded it cost $28.7k partly on handovers nobody could act on.

## Counters

```
dirty=43  feature_shape_rows=0  ports_adapters_files=35  port_names=30
port_word_files=86  families_declared=80  families_unbound=47
main_unfolded=85  api_boot=no-stack
```

`feature_shape_rows=0` - **that drive is finished.** Any brief describing module
conversion to the annotation shape as the work is stale.

`main_unfolded=85` is commits behind `origin/main`, growing ~17/day. Not a task,
a clock.

## What landed

- **The agent workflow protocol**, tracked: `.claude/coordinator/` (COORDINATOR,
  LANE, manifest + handoff templates), `.claude/skills/core/` (repository,
  testing, handoff rules), `.claude/agents/lane.md` (a lane runs with no `Agent`
  tool, so "no subagents" is a constraint). `.claude/manifests/` and
  `.claude/handoffs/` are gitignored runtime state.
- **Enforcement**: `packages/architecture-lint/tests/agent-workflow-protocol.unit.test.ts`
  binds 5 structural scenarios (7 tests, 276ms, CI-safe).
  `skills/_tests/agent-workflow-protocol.scenario.test.ts` binds 4 behavioural
  ones (real Claude Code session, `skipIf(isCI)`, **never run - costs money**).
  Spec: `specs/claude/agent-workflow-protocol.feature`, 9/9 bound.
- **Drive to zero**: 188 → 43 dirty.
- **Docs**: `composition-by-size.md` (the root at four sizes, verbatim from the
  tree), `main-merge-2026-09-11/` (the merge ledger), `worker-composition-proposal.md`.

## The blocker, and it is one thing

**`packages/runtime-composition` does not typecheck.** I fixed 5 of 8 errors
(duplicate `Name` generic, three missing `extends string`, the legacy
`serverFeature` return type). The last 3 are a real decision:

> `tests/define-feature.unit.test.ts:93` constructs deliberately-bad config to
> prove *runtime* rejection. The new `ModuleConfigGuard` refuses it at *compile*
> time. Cast, `@ts-expect-error`, or loosen the guard.

Downstream of it: 7 installation tests fail on `.withInfrastructure(` (gone, no
replacement), `modules/workflow` and `apps/worker` stay dirty, the tree cannot
reach zero, and the merge cannot start.

## The proposal, and the one thing that would falsify it

`dev/docs/plans/worker-composition-proposal.md`. In one line: the api root is
**183 lines** and the worker's is **13,725 across 64 files**, and the 184 bespoke
collaborator fields in fourteen modules are the mechanism keeping it that way -
so ratchet them down with a shrink-only baseline, the play that already took
`feature_shape_rows` 392 → 0.

**It rests on one unproven claim.** I asserted the worker's 13,725 lines are
*mostly* bag construction, from 153 constructions and 120 local bindings in
`worker-production.composition.ts`. That is suggestive, not proven. See next
action.

## Exact next action

Sample `apps/worker/src/app/worker-production.composition.ts` (2,530 lines) and
classify its 153 constructions into: (a) building a module's
`<Module>Infrastructure` bag, (b) building a repository or channel the module
should own, (c) genuine process wiring that belongs in a root. Report the three
counts.

If (a) plus (b) is most of it, the proposal stands and step 4 of its sequencing
is justified. If (c) dominates, the headline is wrong and the proposal needs
rewriting before anyone acts on it.

Do not start the merge first. It is 85 commits and growing, but it needs a tree
at zero, and the tree needs the blocker above.

## Then, in order

1. Decide the `ModuleConfigGuard` question (above). Unblocks the typecheck.
2. Build the ratchet: `bespoke-members-baseline.json`, a lint rule refusing a new
   bag field, `bespoke_member_fields` in `shape-counters.sh`.
3. The 7 installation tests, on their bags. Tree reaches zero.
4. The merge - `dev/docs/plans/main-merge-2026-09-11/README.md`. **Regenerate the
   ledger first**; its 1,282 conflicts were measured at `a08cbd27b8`, 13 commits
   ago.
5. Worker onto `createProcess` + `serverModules`; pay down the bags largest
   first (trace 29, automation 26, langy 21, gateway 14).

## Three stale claims found, do not trust the 09-10 documents

| Claim | Reality |
| --- | --- |
| 551 files under ports/adapters | **35** |
| `withProvided` was removed, 193 call sites broken | it is at `application.ts:355`, all 112 sites resolve |
| `.withInfrastructure` fully migrated | **8 stragglers**, one a real worker composition root |

Measure before designing from any of them.

## Open lane work

`.claude/manifests/` holds `cv2-port-word` (gateway Batch B - the pure functions,
DTO mappers and the eventing channel), `cv2-boot-shape-stragglers` (the 7 tests,
**blocked** on the decision above), `cv2-application-member-record` (T1, opus),
`cv2-rest-credential-object` (T3), `cv2-peer-seam` (T2 - **premise is stale,
verify before designing**).

Their handoffs are in `.claude/handoffs/`, gitignored, still on disk.

## Risks

- **Four peer sessions were live** in this checkout all session. They had not
  written in 17h when I started, which is why committing was safe. That may not
  hold - check before committing anything wide.
- A lane created **four backwards-compatibility re-export aliases** and reported
  them under "wire differences: none" as evidence of safety. I caught it reading
  the diff and repointed 13 occurrences. The structural test tier cannot catch
  this; a lint rule could.
- `api_boot=no-stack` all session, so no lane verified a boot log. Every brief's
  "read `haven logs backend`" step has been a no-op.

## Starting the next session

Paste this into a fresh session here. Everything else is in the files it names.

```
You are the coordinator, working in
/Users/afr/Source/github.com/langwatch/langwatch on feat/strict-feature-layout-v0.
Work in this checkout. Do not create a worktree, switch branch, push or open a
PR. Never read .env or any secret-bearing file.

Read these in order and treat them as your instructions:
  dev/docs/plans/handover-2026-09-11.md      the state, the blocker, what is next
  .claude/coordinator/COORDINATOR.md         how you coordinate
  .claude/skills/core/repository-rules.md    what binds you and every lane
  .claude/skills/core/handoff-rules.md       the seven statuses, the handoff contract

Do the handover's "Exact next action" yourself, before spawning anything. It is
a counting task on one file, and it decides whether
dev/docs/plans/worker-composition-proposal.md stands. Report the three counts.

Then work "Then, in order", spawning lanes with .claude/coordinator/LANE.md and
the manifests in .claude/manifests/, naming model, effort and context size at
every spawn. Before committing: run `bash dev/scripts/shape-counters.sh` and
check whether the four peer sessions here have written since 2026-09-11.
```
