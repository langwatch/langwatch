# Main functionality lost in the merges — port ledger (2026-09-25)

Alex: nothing on main may be missing here. Source: `.claude/handoffs/audit-main-functionality-gaps.md` §5 (evidence, file:line).
Each row is ported by a lane and then deleted from this file. Rulings: port ops are approved as ports of main (merge takes main);
image proxy → stored-object (Alex); wire = main's paths.

| # | gap | lane | state |
|---|---|---|---|
| 1 | sharedTrace.get unserved (public shared traces) | port-trace-gaps | PORTED b4b00fc656 |
| 2 | analytics.savedWorkbenchCharts path mismatch | port-namespace-alignment | PORTED b7379069f0 |
| 3 | dashboardWidgets tRPC unserved (REST only) | port-namespace-alignment | PORTED 5d129d6c07 |
| 4 | suites.testSuites path mismatch, update missing | port-namespace-alignment | PORTED b7379069f0 (create fields: run 2) |
| 5 | GET /api/image-proxy gone | port-rest-gaps (stored-object) | PORTED 8c4ac0cbcb; run 3 parity uncommitted: any upstream status echoed, TLS verified only when isSaas (main's rule) |
| 6 | /api/evaluations/v3/* alias gone (Python SDK) | port-rest-gaps (experiment) | PORTED uncommitted: 10 routes, hidden from OpenAPI, alias==canonical pinned |
| 7 | usage-limit warning emails never sent (cron trace_analytics), seed_demo | port-usage-emails-health-canaries | BLOCKED on decisions (handoff §11): entitlement composes no usage counter (every month reads USAGE_UNKNOWN), no SaaS subscription source (every org free baseline) and an always-refusing UsageWarning; billing's UsageWarningService is unconstructed with a memory-only mail channel; seed_demo is a raw-Prisma dogfood script |
| 8 | traces.codingAgentSession / codingAgentTranscript | port-trace-gaps | PORTED uncommitted: TraceApi.readCodingAgentTranscript (port op) + existing readCodingAgentSession; outputs z.unknown() (coding-agent-contract depends on trace-contract) |
| 9 | traces.getSampleTraces | canaries-and-trace-reads | PORTED (uncommitted) — `EvaluatorApi.findTraceIdsPassingPreconditions` (main's precondition functions ported to evaluator-process rules/) and `TraceApi.readPreconditionSampleTraces` (main traces.ts:393-480 shuffle and top-up, in `TracePreconditionSampleService`); procedure keeps main's name, input fields and `traces:view`. Input is loose (`evaluatorType: string`, `preconditions: unknown[]`) and evaluator refuses bad values with `validation_error` and `meta.fieldErrors`, evaluatorType checked exactly as main (generated `evaluatorsSchema.keyof()` or `custom/*`); 7 bound @unit scenarios. Wire differences in .claude/handoffs/canaries-and-trace-reads.md §11 |
| 10 | export.onExportProgress / onScenarioRunExportProgress | port-trace-gaps | PORTED uncommitted: trace serves the whole `export` namespace (one namespace = one module), TraceApi.streamExportProgress (port op); placement to confirm |
| 11 | governance admin ~70 procedures | after discovery | open; wire dates on governance tRPC answers are the branch's epoch-ms `*Ms` fields, not main's Dates (ruled 2026-09-25: tRPC is plain JSON, the ported screens read *Ms, no external caller) |
| 12 | personal surface (sessions, virtual keys, ingestion key, budgetOverview, personalUsage, resolveHome, recordWorkspaceView) | after discovery | open; wire dates on governance tRPC answers are the branch's epoch-ms `*Ms` fields, not main's Dates (ruled 2026-09-25: tRPC is plain JSON, the ported screens read *Ms, no external caller) |
| 13 | procedures with no successor + no caller (identity email ids, auth priorSession/signUpEnrollment/myAddressConfirmation, ops.getSignUpHealth, tracesV2.changeMetadata/facets) | port-misc-gaps (verified) | NOT an intentional redesign — every one is a #7631 (c5999477f1) port gap, see `.claude/handoffs/merge-gap-7631.md`. auth.priorSession (main caller IdentifierFirstSignIn.tsx:113; branch identifier-first-sign-in.tsx has no expired-session recovery; gap §1.1), auth.signUpEnrollment (VerificationFirstSignUp.tsx:82; branch verification-first-sign-up.tsx is the base-era frontDoor.completeSignUpVerification flow; gap §2.2), auth.myAddressConfirmation (SignInMethodsSummary.tsx:52, EmailIdentifiersSection.tsx:75; gap §3), identity.myIdentifiers/myMethodsLastUsed/addEmailIdentifier/resendIdentifierConfirmation/removeIdentifier (features/account-identifiers/**; no branch successor; gap §3; lives in modules/user, governance lane holds it), ops.getSignUpHealth (no UI caller on main; reads Organization+OrganizationUser so needs an OrganizationApi op; gap §4.2). Namespace: main serves `auth.*`, branch `frontDoor.*` — whole-namespace wire difference. tracesV2.facets: UI need served by `traces.discover` facets (use-filtered-trace-facets.ts:32) — feature kept, wire name differs; tracesV2.changeMetadata: no UI caller on main, REST PATCH successor 36dad30c19; tRPC name unserved | open: needs the #7631 identity drive |
| — | governance discovery #8043 | port-governance-discovery (5 lanes) | in progress |
| — | langy tRPC router (18 procs), langy route families | port-langy-routes | in progress |
| 0 | WORKER PIPELINES NOT REGISTERED: trace, evaluation, experiment, coding-agent, gateway, governance, topic, automation, langy conversation, simulation, billing reporting, blob/process-retention maintenance never call .withEventing (deleted composition f6ae3e5426; WP drive stalled at WP-3) — also webhook delivery PM, spend-spike evaluator, storage-stats collection (WP-6), scenario processor + execution pool, VOICE_WORKER_ONLY (WP-5) | worker-pipelines WP-3..WP-7 | REGISTERED — every module pipeline incl. trace_processing (7b6e61b106); worker installation test green. Remaining: peer wiring in trace (WP-7c), governance reconcile/erasure/rollups, voice worker mode, live trace broadcasts, PostHog first_trace_integrated |
| 0b | ~~EVALUATION RUNTIME IS A CLOSED STUB~~ CLOSED 9db083c5cc: EvaluationApp.create always builds createUnavailableEvaluationInfrastructure (ClickHouse resolver, evaluator executor, trace rescore all refuse) — evaluations don't run in production | port-evaluation-runtime | open |
| 14 | customer.io evaluation / simulation first-use sync | — | CLOSED: main never registers them (pipelineRegistry.ts:620 TODO) |
| 15 | /api/health/{collector,evaluations,processor,triggers,workflows,scenarios,langy} (only /api/health served) | port-rest-gaps | 15a PORTED uncommitted: platform-health serves the five project-keyed probes at main's bodies, keys resolved through ApiKeyApi.findResolvedToken (needs `pnpm install` for the api-key-contract dep). 15b /api/health/scenarios PORTED uncommitted (platform-health, ScenarioApi+SuiteApi peers, main's 400/401/429/503/200 bodies, no-store); /api/health/langy BLOCKED: needs a LangyApi turn-settlement op (handoff port-usage-emails-health-canaries §10) |
| 16 | trace_ingestion_spans_total metric (#7932) — confirm on a running stack | verify | open |
| 17 | governance cost-rollup mismatch/lag metrics (#7650) | WP-6 | open |
| 18 | server-side refusal of disallowed timeseries aggregations (#8013) | port-misc-gaps | PORTED uncommitted: AnalyticsService.getTimeseries refuses before cache/read with validation_error (unknown metric → fieldErrors `series.N.metric`; disallowed aggregation → main's meta metric/aggregation/allowedAggregations + fieldErrors `series.N.aggregation`); table in analytics-contract `analyticsMetricAggregations`, browser-kit registry pinned to it by a parity test; 3 bound scenarios |
| 19 | one-shot tasks backfillAnnotationsToClickhouse, groupQueueMigrationAudit | port-misc-gaps | groupQueueMigrationAudit CLOSED: not a task on main, the object-storage migration's cutover gate — ported as stored-object GroupQueueObjectStorageMigrationAdapter, called at object-storage-migrate.task.ts:250. backfillAnnotationsToClickhouse BLOCKED on decision: apps/tasks boots no createApp, and the backfill needs annotation reads + a TraceApi bulk-sync op (TraceApi has record/removeAnnotation only). Also: apps/tasks/src/main.ts registers 6 tasks; the module *.task.ts (dataset-content-backfill, object-storage-migrate, stalled-runs-backfill, …) are not runnable from it |
| — | MCP tools / rpc.discover / openapi / llms.txt surface | audit run 3 | auditing |
| 20 | ENTITLEMENT RUNS ON PLACEHOLDERS: monthly usage always unknown, SaaS has no subscription source (every org, paying ones included, resolves the free plan), limits.checkAndSendUsageLimitNotification always refuses | entitlement-live | open — URGENT |
| 21 | GET /api/traces/{traceId}/transcript unregistered (tracesRest passes no transcript reader) | canaries-and-trace-reads | PORTED, uncommitted: TraceApi.readTraceTranscript, route registered unconditionally, 3 bound scenarios |
| 22 | PATCH /api/traces/:traceId/metadata | canaries-and-trace-reads | PORTED (uncommitted) — `TraceApi.updateTraceMetadata` moves main's handler one-to-one over the existing `TraceMetadataWriteService` (synthetic span through the ingress command; refuses by name without a recorder); the route registers unconditionally with `traces:update`, 200 `{ traceId }` pinned. Invalid body answers the framework's 422 `validation_error` where main's zValidator answered 400 |
| 23 | [security] #7631 §1.6 residual: `user.personalContext` returns `workspace.project.apiKey` unredacted (user.app.ts:757-781; schema personal-workspace.ts:36 demands a non-empty key) where main blanks it without `project:manage` (main routers/user.ts:921-931); `user.register` takes no `addressProof` (user.schemas.ts:16-27 vs main user.ts:258,325; ride with row 30). §1.6 items 1-4 CLOSED (identity-gap-audit handoff) | identity-audit lane S1 | open |
| 24 | [security] #7631 §1.7: SSO ID tokens not verified — `requireIdTokenVerification` never set for auth0/okta (0 hits; better-auth 1.7.1 supports it) and `accountIssuer` pinning absent from better-auth-sso-adapter.rules.ts. Recover post-#8143 `git show origin/main:platform/app/ee/sso/providers.ts` (~L598-607) | identity-audit lane S1 | open |
| 25 | [security] #7631 §2.1 privacy probe: `SignInAccountLookup` is now implemented (identity/process/src/services/signin-account-lookup.service.ts, used by signin-router.service.ts:99) but credential-sign-in-form.tsx:96,113 still asks "does this address exist?" by mailing `frontDoor.requestSignUpVerification`; swap to the router. Post-#8143 bridge-subject routing unverified | identity-audit lane S2 | open |
| 26 | [security] #7631 §1.1 residual: list + end one session served as `user.browserSessions`/`endBrowserSession` (e7bd61ecdd); main's wire `personalSessions.listWebSessions`/`revokeWebSession` unserved (governance serves only list/revoke/revokeAll, CLI) and `revokeWebSessionsForIdentifier` (end what one sign-in method minted) absent. Recover main ee/governance/routers/personalSessions.ts:101-165 | identity-audit lane S2 | open |
| 27 | [security] #7631 §1.3: `twoFactor()` mounts only behind `mfaEnrollmentOpen` (http.better-auth.channel.ts:812); main's two-step ceremonies, organization MFA policy/gate and enrolment UI (TwoFactorSetup*, totpUri, OrganizationMfaGate) have 0 hits. Recover main better-auth/two-step-ceremonies.ts, app-layer/identity/organization-mfa.service.ts, components/me/twoFactor/* | identity-audit lane S3 | open (partial) |
| 28 | [security] #7631 §1.4: `PasskeyRemoval.deleteIfAnotherWayInRemains` declared (better-auth-identity-storage.service.ts:120-131); implementation not verified by the audit; last-way-in warning UI (useLastWayInWarning, RemoveSignInMethodDialog) 0 hits. Recover main app-layer/identity/last-way-in.service.ts + repositories/{last-way-in,passkey-removal}.prisma.repository.ts | identity-audit lane S3 | open (partial) |
| 29 | [functional] row 13 namespace: main serves `auth.*` (signUpEnrollment, route, requestSignUpVerification, myAddressConfirmation, sendMyAddressConfirmation, inviteLanding, requestFreshInvite, priorSession); branch declares `frontDoor` (auth/contract/src/front-door.trpc.ts:23) and the browser calls `api.frontDoor.*` (credential-sign-in-form.tsx:96). Whole-namespace wire difference against "wire = main's paths"; ruling needed before port | coordinator ruling, then F1 | open |
| 30 | [functional] #7631 §2.2 spent-link grace / account-before-confirmation / address proof: 0 hits for SPENT_LINK_GRACE, findSpent, keepSpentUntil, SignUpAddressState, signUpEnrollment. Recover main app-layer/identity/signup-verification.service.ts + sign-up-identifier.ts, address-confirmation-link.ts, better-auth/sign-up-confirmation.ts | identity-audit lane F1 | open |
| 31 | [functional] #7631 §2.3 reset password opens a session: 0 hits for password-reset-session. Recover main pages/auth/reset-password.tsx, better-auth/{password-reset-session,session-minter}.ts | identity-audit lane F1 | open |
| 32 | [functional] #7631 §2.4 link proposals cannot be decided: 0 hits for CONFIRM_LINK/REJECT_LINK/link-proposal. Recover main app-layer/identity/link-proposal.service.ts, link-proposal-guards.ts, link-proposal.repository.ts | identity-audit lane F2 | open |
| 33 | [cosmetic] #7631 §2.5: four of five codes registered and raised (auth.errors.ts, admin.errors.ts:47); `auth_direct_registration_unavailable` absent from error-presentation app-codes, branch raises `registration_not_available` (user.errors.ts:132-141) where main raised the former (main routers/user.ts:296,303) | identity-audit lane F1 | open |
| 34 | [functional] #7631 §4.1 identity lookup screen: contract + prisma/memory repositories exist (identity/contract/src/identity-lookup.ts); no browser or route consumer found. Recover main ee/admin/backoffice/resources/IdentityLookup*.tsx | identity-audit lane F2 | open (partial) |
| 35 | [functional] #7631 §4.2: address-lock reap present (`reservations().reapOrphans()`, identity.app.ts:108-112); sign-up health (service, repository, `ops.getSignUpHealth`) not found, public-egress/staged-ledger-writer unverified | identity-audit lane F2 | open (partial) |
| 36 | [functional] #7631 §5.1 codex helper threads: span-filter admission landed (coding-agent-span-filter.ts:21-22); stamping (`stampCodexHelperThread`, `codexAuxiliarySessionFacts`), the `Auxiliary` write and projection field absent — column exists (clickhouse-migrations 00096) and only lwql-view-catalog.rules.ts:1717 names it. Recover main app-layer/traces/codex-auxiliary-thread.ts + trace-request-collection.service.ts + codingAgentSpanFactsDispatch.subscriber.ts | trace/coding-agent lane F3 | open |
| 37 | NO SIGN-IN PROVIDER IS MOUNTED: auth-composition.build.ts passes socialProviders {} and genericOAuthConfigs [] — Google, GitHub, GitLab, Azure, Auth0, Okta, Cognito, OneLogin, OIDC sign-in never reach better-auth | sso-mount | open — CRITICAL |
| 38 | Identity's four pipelines (identity, join-request, scim-sync, sso-connection) are registered producer-only; no process builds their consumer side (projections/reactors), so their read models may never update — verify against main's worker | worker-pipelines | landed 24197b6cef (four consumers on the worker; scim move by command) |
| 39 | Automatic-join mail omits seat counts: the join-request notifier gets plans from EntitlementApi but no memberships (main showed seats) | row38 follow-up | open |
| 40 | Ops queue payload decoding: nothing supplies `queuePayloads` in the production composition and ops-operations.ts:131 throws when Redis is configured without it | ops | open — verify against main |
| 41 | Nothing writes `governance_cost_rollup_1d`: main's `governanceCostRollup.foldProjection` was never ported, so the governance cost reads (35c746eb08) answer empty | governance-cost | open — lane running |
