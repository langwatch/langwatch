# Main functionality lost in the merges — port ledger (2026-09-25)

Alex: nothing on main may be missing here. Source: `.claude/handoffs/audit-main-functionality-gaps.md` §5 (evidence, file:line).
Each row is ported by a lane and then deleted from this file. Rulings: port ops are approved as ports of main (merge takes main);
image proxy → stored-object (Alex); wire = main's paths.

| # | gap | lane | state |
|---|---|---|---|
| 1 | sharedTrace.get unserved (public shared traces) | port-trace-gaps | PORTED b4b00fc656 |
| 2 | analytics.savedWorkbenchCharts path mismatch | port-namespace-alignment | PORTED b7379069f0 |
| 3 | dashboardWidgets tRPC unserved (REST only) | port-namespace-alignment | PORTED 5d129d6c07 |
| 4 | suites.testSuites path mismatch, update missing | port-namespace-alignment | PORTED b7379069f0 (create fields: run 2) |
| 5 | GET /api/image-proxy gone | port-rest-gaps (stored-object) | PORTED 8c4ac0cbcb; run 3 parity uncommitted: any upstream status echoed, TLS verified only when isSaas (main's rule) |
| 6 | /api/evaluations/v3/* alias gone (Python SDK) | port-rest-gaps (experiment) | PORTED uncommitted: 10 routes, hidden from OpenAPI, alias==canonical pinned |
| 7 | usage-limit warning emails never sent (cron trace_analytics), seed_demo | port-usage-emails-health-canaries | BLOCKED on decisions (handoff §11): entitlement composes no usage counter (every month reads USAGE_UNKNOWN), no SaaS subscription source (every org free baseline) and an always-refusing UsageWarning; billing's UsageWarningService is unconstructed with a memory-only mail channel; seed_demo is a raw-Prisma dogfood script |
| 8 | traces.codingAgentSession / codingAgentTranscript | port-trace-gaps | PORTED uncommitted: TraceApi.readCodingAgentTranscript (port op) + existing readCodingAgentSession; outputs z.unknown() (coding-agent-contract depends on trace-contract) |
| 9 | traces.getSampleTraces | port-trace-gaps | BLOCKED on a decision: precondition matching lives only in evaluator-browser; evaluator-contract depends on trace-contract, so the input schema cannot be declared in trace — see .claude/handoffs/port-trace-gaps.md §11 |
| 10 | export.onExportProgress / onScenarioRunExportProgress | port-trace-gaps | PORTED uncommitted: trace serves the whole `export` namespace (one namespace = one module), TraceApi.streamExportProgress (port op); placement to confirm |
| 11 | governance admin ~70 procedures | after discovery | open |
| 12 | personal surface (sessions, virtual keys, ingestion key, budgetOverview, personalUsage, resolveHome, recordWorkspaceView) | after discovery | open |
| 13 | procedures with no successor + no caller (identity email ids, auth priorSession/signUpEnrollment/myAddressConfirmation, ops.getSignUpHealth, tracesV2.changeMetadata/facets) | verify UI loss | open |
| — | governance discovery #8043 | port-governance-discovery (5 lanes) | in progress |
| — | langy tRPC router (18 procs), langy route families | port-langy-routes | in progress |
| 0 | WORKER PIPELINES NOT REGISTERED: trace, evaluation, experiment, coding-agent, gateway, governance, topic, automation, langy conversation, simulation, billing reporting, blob/process-retention maintenance never call .withEventing (deleted composition f6ae3e5426; WP drive stalled at WP-3) — also webhook delivery PM, spend-spike evaluator, storage-stats collection (WP-6), scenario processor + execution pool, VOICE_WORKER_ONLY (WP-5) | worker-pipelines WP-3..WP-7 | REGISTERED — every module pipeline incl. trace_processing (7b6e61b106); worker installation test green. Remaining: peer wiring in trace (WP-7c), governance reconcile/erasure/rollups, voice worker mode, live trace broadcasts, PostHog first_trace_integrated |
| 0b | ~~EVALUATION RUNTIME IS A CLOSED STUB~~ CLOSED 9db083c5cc: EvaluationApp.create always builds createUnavailableEvaluationInfrastructure (ClickHouse resolver, evaluator executor, trace rescore all refuse) — evaluations don't run in production | port-evaluation-runtime | open |
| 14 | customer.io evaluation / simulation first-use sync | — | CLOSED: main never registers them (pipelineRegistry.ts:620 TODO) |
| 15 | /api/health/{collector,evaluations,processor,triggers,workflows,scenarios,langy} (only /api/health served) | port-rest-gaps | 15a PORTED uncommitted: platform-health serves the five project-keyed probes at main's bodies, keys resolved through ApiKeyApi.findResolvedToken (needs `pnpm install` for the api-key-contract dep). 15b /api/health/scenarios PORTED uncommitted (platform-health, ScenarioApi+SuiteApi peers, main's 400/401/429/503/200 bodies, no-store); /api/health/langy BLOCKED: needs a LangyApi turn-settlement op (handoff port-usage-emails-health-canaries §10) |
| 16 | trace_ingestion_spans_total metric (#7932) — confirm on a running stack | verify | open |
| 17 | governance cost-rollup mismatch/lag metrics (#7650) | WP-6 | open |
| 18 | server-side refusal of disallowed timeseries aggregations (#8013) | port lane (analytics) | open |
| 19 | one-shot tasks backfillAnnotationsToClickhouse, groupQueueMigrationAudit | low | open |
| — | MCP tools / rpc.discover / openapi / llms.txt surface | audit run 3 | auditing |
| 20 | ENTITLEMENT RUNS ON PLACEHOLDERS: monthly usage always unknown, SaaS has no subscription source (every org, paying ones included, resolves the free plan), limits.checkAndSendUsageLimitNotification always refuses | entitlement-live | open — URGENT |
| 21 | GET /api/traces/{traceId}/transcript unregistered (tracesRest passes no transcript reader) | port-trace-gaps | open |
