# Seams: a mistake here is systemic, not local, so a human owner reviews every
# change. Everything else is held by the machines (oxlint, architecture-lint,
# the parity gate, per-package tests). See dev/docs/plans/strict-feature-layout.md
# section 6, "The seam review method".

# The pipeline every REST route and tRPC procedure goes through.
/packages/api/src/                                   @0xdeafcafe

# Who gets which port, which process migrates, what installs AuthZ.
/apps/api/src/app/*.composition.ts                   @0xdeafcafe
/apps/worker/src/app/*.composition.ts                @0xdeafcafe
/apps/tasks/src/platform/*.composition.ts            @0xdeafcafe
/enterprise/packages/composition/                    @0xdeafcafe

# Authorisation and the request security chain.
/apps/api/src/api-rest.security.ts                   @0xdeafcafe
/modules/authz/server/                     @0xdeafcafe
/modules/identity/server/                  @0xdeafcafe

# The rules encode the design; a wrong rule blesses a wrong pattern everywhere.
/packages/architecture-lint/                         @0xdeafcafe
/.oxlintrc.architecture.json                         @0xdeafcafe
/tsconfig.base.json                                  @0xdeafcafe

# Schema is immutable history once deployed.
/packages/prisma-client/prisma/migrations/           @0xdeafcafe
/packages/clickhouse-client/migrations/              @0xdeafcafe
