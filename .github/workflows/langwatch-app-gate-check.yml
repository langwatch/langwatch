name: langwatch-app-gate-check

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  checks: read

jobs:
  mcp-server-gate:
    uses: ./.github/reusable-workflows/gated-requirement-checks.yml
    with:
      workflow-name: 'langwatch-ci'
      path: 'langwatch'
