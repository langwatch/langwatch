/**
 * How a plan limit reads in a sentence, for the card Langy shows when a turn is refused
 * for one.
 */

export const LIMIT_TYPE_LABELS: Record<string, string> = {
  members: "team members",
  membersLite: "lite members",
};
