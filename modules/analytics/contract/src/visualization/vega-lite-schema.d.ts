/**
 * Bundled Vega-Lite v6 JSON Schema declared (not inferred) to avoid TypeScript
 * parsing 1.4MB on every typecheck. Exported as vega-lite/vega-lite-schema.json.
 */
declare module "vega-lite/vega-lite-schema.json" {
  const schema: Record<string, unknown>;
  export default schema;
}
