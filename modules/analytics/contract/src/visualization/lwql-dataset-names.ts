/**
 * The names a LangWatchQL specification is allowed to read data from. Shared by
 * chart mode (browser) and save path (server) that validates specs without rows.
 */

/** The dataset name the workbench registers its query result under. */
export const LWQL_QUERY_RESULT_DATASET = "query_result";
