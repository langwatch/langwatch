{
  "name": "@langwatch/identity-contract",
  "version": "0.1.0",
  "private": true,
  "description": "The isomorphic identity core (ADR-101, ADR-115): the identifier vocabulary, value normalization, the fact and command shapes, the pure reducer, the refusal errors and the backfill's parity policy. No node imports, so the frontend and the backend share it verbatim. Server-side services live in @langwatch/identity-process.",
  "license": "MIT",
  "files": [
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -b tsconfig.json tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@langwatch/config": "workspace:*",
    "@langwatch/handled-error": "workspace:*",
    "@langwatch/kernel": "workspace:*",
    "@langwatch/system-migrations": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@langwatch/error-presentation": "workspace:*",
    "@langwatch/vitest-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
