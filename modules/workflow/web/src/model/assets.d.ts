/**
 * Stylesheets imported for their side effect. `@xyflow/react/dist/style.css`
 * is the canvas's own stylesheet, imported as the library documents.
 * TypeScript has no notion of a CSS module, so this stands in for one.
 */
declare module "*.css";
