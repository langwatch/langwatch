import { describe, it } from "vitest";

describe("MainContentEmptyState", () => {
  describe("when routing to correct empty state", () => {
    it.todo("renders NoPromptsOnboardingState when no published prompts exist");
    it.todo("renders NoTabsOpenState when prompts exist but none are open");
  });
});
