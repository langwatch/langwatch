/**
 * The URL-addressed drawers this family owns. ONE PUBLIC ENTRY FOR THE
 * WHOLE SET: the composing application spreads a map of these into its
 * drawer registry, so it names one entry rather than a path per component.
 */

export { SelectDatasetDrawer, type SelectDatasetDrawerProps } from "./select-dataset-drawer.tsx";
