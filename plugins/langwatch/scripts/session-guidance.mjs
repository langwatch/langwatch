var o="LangWatch attributes this session's work and cost to the repository and branch it reports. When you start work in a different repository, branch or worktree (you cd into another checkout, run git checkout or git switch, or create a worktree), run `langwatch ingest context` from inside that directory. It prints one line, never interrupts the session, and is what attributes your work to the correct pull request.";var s=["CLAUDECODE","CLAUDE_CODE_SESSION_ID","CLAUDE_PROJECT_DIR"],i=s.some(r=>{var t,e;return((e=(t=process.env[r])==null?void 0:t.trim())!=null?e:"")!==""});i&&process.stdout.write(`${JSON.stringify({hookSpecificOutput:{hookEventName:"SessionStart",additionalContext:o}})}
`);process.exit(0);
