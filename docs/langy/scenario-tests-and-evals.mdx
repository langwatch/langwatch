---
title: Scenario tests and evaluations
description: "What Langy produces when it tests your agent: Scenario multi-turn simulation tests and evaluations, written and run through the langwatch CLI."
sidebarTitle: Tests and evals
keywords:
  - ai agent simulation testing
  - scenario tests
  - agent evaluations
---

Langy writes and runs Scenario simulation tests and evaluations through the `langwatch` CLI, so they live in your project like any test or evaluator you wrote by hand. Which one it picks depends on what the question needs to prove; see the table below.

<Info>**Also check:** [Ask Langy](/langy/chat) (how you request a test), [Pull requests](/langy/pull-requests) (the tests it wrote run in your CI), [Agent Simulations](/agent-simulations/introduction) and [Evaluations](/evaluations/overview) (the product surfaces these land in).</Info>

## Scenario simulation tests

A Scenario test is a multi-turn simulation. A simulated user drives your agent across a full conversation, probing it the way a real user would, and a judge scores the transcript against the criteria you set.

This is how Langy catches behavior that a single-turn check misses: an agent that holds the line for one message but drifts by turn 15.

Langy writes and runs these with:

```bash
langwatch scenario create
langwatch scenario run
```

The runs land in the Simulations surface, where you can filter, group, and open any run.

<Frame caption="The Simulations overview filtered to red-team runs. Each row is a scenario run with its outcome.">
  ![Simulations overview filtered to red-team runs](/images/langy/testing/redteam-overview.png)
</Frame>

Open a run to see the judge's verdict and its reasoning against your criteria.

<Frame caption="A scenario run detail: the judge verdict with the reasoning it used to reach it.">
  ![Scenario verdict and judge reasoning](/images/langy/testing/scenario-verdict.png)
</Frame>

When a run fails, the detail includes the bug log so you can see exactly where the agent broke.

<Frame caption="A failed-run detail with the bug log showing where the agent's behavior diverged from the criteria.">
  ![Failed run detail with bug log](/images/langy/testing/failed-run-buglog.png)
</Frame>

Read more in the Agent Simulations docs: [Introduction](/agent-simulations/introduction), [Getting started](/agent-simulations/getting-started), [Red teaming](/agent-simulations/red-teaming), and [Voice agents](/agent-simulations/voice-agents).

## Evaluations

An evaluation scores a run against a criterion: answer correctness, a custom rule, an LLM judge, a pairwise comparison. Langy writes evaluators and runs experiments and monitors with the CLI:

```bash
langwatch evaluator create
langwatch experiment run
langwatch monitor create
```

Use each for a different job:

- `langwatch experiment run` scores an offline or batch run, for comparing two models or two prompts. See [Experiments](/evaluations/experiments/overview) and [running evals in CI/CD](/evaluations/experiments/ci-cd).
- `langwatch monitor create` sets up an online evaluation that scores live production traffic. See [Online evaluation](/evaluations/online-evaluation/overview).
- `langwatch evaluator create` defines the evaluator both of the above use. See [Evaluators](/evaluations/evaluators/overview).

## Which one Langy picks

Langy picks based on what your question needs to prove:

| You want to check | Langy writes |
|---|---|
| The agent handles a full conversation correctly | A Scenario simulation test |
| One run scores well on a criterion | An evaluation |
| Live traffic stays within a criterion | An online monitor |
| Two models or prompts, head to head | An experiment |

You can also name the one you want in the [chat](/langy/chat): "write a Scenario test that..." or "set up an evaluator for...".
