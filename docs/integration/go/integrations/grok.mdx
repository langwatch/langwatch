---
title: Grok (xAI) Integration
sidebarTitle: Grok (xAI)
description: Instrument Grok (xAI) API calls in Go using LangWatch to capture high-speed traces and improve AI agent evaluations.
keywords: go, golang, grok, xai, instrumentation, langwatch, openai-compatible
---

LangWatch supports tracing Grok (xAI) API calls through xAI's OpenAI-compatible endpoint. Reuse the [OpenAI](/integration/go/integrations/open-ai) `otelopenai` middleware: point the client at the xAI base URL and label the spans with `WithGenAIProvider`.

## Installation

```bash
go get github.com/langwatch/langwatch/sdk-go
go get github.com/langwatch/langwatch/sdk-go/instrumentation/openai
go get github.com/openai/openai-go/v3
```

## Usage

<Info>
Set `LANGWATCH_API_KEY` and `XAI_API_KEY` environment variables before running.
</Info>

```go
package main

import (
	"context"
	"log"
	"os"

	langwatch "github.com/langwatch/langwatch/sdk-go"
	otelopenai "github.com/langwatch/langwatch/sdk-go/instrumentation/openai"

	"github.com/openai/openai-go/v3"
	"github.com/openai/openai-go/v3/option"
	"go.opentelemetry.io/otel"
	semconv "go.opentelemetry.io/otel/semconv/v1.40.0"
	sdktrace "go.opentelemetry.io/otel/sdk/trace"
)

func main() {
	ctx := context.Background()

	// Set up the LangWatch exporter (reads LANGWATCH_API_KEY from env).
	exporter, err := langwatch.NewExporter(ctx)
	if err != nil {
		log.Fatalf("failed to create exporter: %v", err)
	}
	tp := sdktrace.NewTracerProvider(sdktrace.WithBatcher(exporter))
	otel.SetTracerProvider(tp)
	defer tp.Shutdown(ctx) // Critical: ensures traces are flushed.

	// Create a Grok client via the OpenAI-compatible API.
	client := openai.NewClient(
		option.WithBaseURL("https://api.x.ai/v1"),
		option.WithAPIKey(os.Getenv("XAI_API_KEY")),
		option.WithMiddleware(otelopenai.Middleware("my-app",
			otelopenai.WithGenAIProvider(semconv.GenAIProviderNameKey.String("x_ai")),
		)),
	)

	response, err := client.Chat.Completions.New(ctx, openai.ChatCompletionNewParams{
		Model: "grok-4-latest",
		Messages: []openai.ChatCompletionMessageParamUnion{
			openai.SystemMessage("You are a helpful assistant."),
			openai.UserMessage("Hello, Grok!"),
		},
	})
	if err != nil {
		log.Fatalf("Chat completion failed: %v", err)
	}

	log.Printf("Response: %s", response.Choices[0].Message.Content)
}
```

<Note>
The middleware captures input and output content **by default**. Pass `otelopenai.WithDataCapture(langwatch.DataCaptureNone)` to opt out.
</Note>

<Warning>
The `defer tp.Shutdown(ctx)` call is essential. Without it, traces buffered in memory will be lost when your application exits.
</Warning>

## Related

- [Go Integration Guide](/integration/go/guide#openai-compatible-providers)
- [Go SDK API Reference](/integration/go/reference#openai-middleware)
- [Capturing Evaluations & Guardrails](/integration/python/tutorials/capturing-evaluations-guardrails)
