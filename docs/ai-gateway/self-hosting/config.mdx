---
title: Config
description: Environment variables and configuration for self-hosted LangWatch AI Gateway deployments.
sidebarTitle: Config
---

The gateway is configured entirely through environment variables — no YAML or config files. Defaults are safe for production; every value below is overridable via Helm values or plain env vars.

## Secrets

These MUST be provided via a Kubernetes Secret (or the equivalent on non-Kubernetes deployments) — never baked into an image.

| Var | Purpose | Shared with control plane? |
|---|---|---|
| `LW_GATEWAY_INTERNAL_SECRET` | HMAC secret used for `/api/internal/gateway/*` calls between gateway and LangWatch control plane | Yes — same value in both |
| `LW_GATEWAY_JWT_SECRET` | HS256 secret for signing the short-lived JWT returned by `/resolve-key` | Yes |
| `LW_VIRTUAL_KEY_PEPPER` | Peppered HMAC secret used to hash virtual-key secrets at rest | Control plane only; gateway never sees it |
| `REDIS_URL` | Optional L2 cache for resolved VKs, shared across gateway replicas. On HPA scale-out, new pods warm from L2 (~2 ms) instead of round-tripping `/resolve-key` (~30 ms); shipped in Lane A iter 15 (`6ebeecf`). TTL floored at 30 s; poison entries (decode errors) are `DEL`'d. See [Scaling → Redis L2](/ai-gateway/self-hosting/scaling#redis-l2-optional) | Gateway only |

Rotate `LW_GATEWAY_INTERNAL_SECRET` and `LW_GATEWAY_JWT_SECRET` together — the gateway supports a 10-minute overlap window (`LW_GATEWAY_JWT_SECRET_PREVIOUS`) so rolling restart doesn't evict every cached JWT at once.

## Routing

| Var | Default | Purpose |
|---|---|---|
| `LW_GATEWAY_LISTEN_ADDR` | `:8080` | Listen addr. Binds `0.0.0.0:8080` in-cluster |
| `LW_GATEWAY_CONTROL_PLANE_URL` | `https://app.langwatch.ai` | Base URL for `/api/internal/gateway/*` calls |
| `LW_GATEWAY_NODE_ID` | auto-generated ULID | Unique id for this replica, surfaced in HMAC `X-LangWatch-Gateway-Node` and in traces |
| `LW_GATEWAY_READ_TIMEOUT_MS` | `30000` | HTTP server read timeout |
| `LW_GATEWAY_WRITE_TIMEOUT_MS` | `120000` | HTTP server write timeout (streaming-safe) |
| `LW_GATEWAY_MAX_BODY_BYTES` | `10485760` (10 MB) | Max request body size — Aider and large-prompt agents need headroom |

## Auth cache

| Var | Default | Purpose |
|---|---|---|
| `LW_GATEWAY_AUTH_CACHE_L1_SIZE` | `10000` | In-process LRU capacity (per replica) |
| `LW_GATEWAY_AUTH_CACHE_TTL_SECONDS` | `900` | JWT TTL / L1 expiry. Matches `/resolve-key` JWT `exp` |
| `LW_GATEWAY_AUTH_CACHE_CHANGES_POLL_SECONDS` | `25` | Long-poll interval against `/api/internal/gateway/changes`. Per-org scoped — one goroutine per organization observed on this pod (Lane A iter 17, `5119261`). See [Scaling → Per-org /changes](/ai-gateway/self-hosting/scaling#per-org-changes-long-poll-iter-17-5119261) |
| `LW_GATEWAY_AUTH_CACHE_BOOTSTRAP` | `true` | On startup, pre-warm L1 via `/bootstrap` call |

See [Auth Cache Architecture](/ai-gateway/observability#auth-cache-architecture) for the two-tier L1/L2 design.

## Providers

Provider credentials are managed from the UI (Gateway → Providers), not env vars. The gateway fetches them via `/config/:vk_id` per-request and caches them in the L1 JWT alongside the VK config. Upstream-specific tuning:

| Var | Default | Purpose |
|---|---|---|
| `LW_GATEWAY_UPSTREAM_TIMEOUT_MS` | `60000` | Per-upstream call timeout. Longer than reasonable for most calls; streaming requests extend via write-deadline reset on chunk |
| `LW_GATEWAY_UPSTREAM_MAX_CONN_PER_HOST` | `256` | Connection pool size per (provider, region) |
| `LW_GATEWAY_UPSTREAM_IDLE_TIMEOUT_S` | `90` | Keep-alive idle |
| `LW_GATEWAY_CIRCUIT_WINDOW_S` | `30` | Breaker sliding window |
| `LW_GATEWAY_CIRCUIT_THRESHOLD` | `10` | Failures inside the window that open the circuit |
| `LW_GATEWAY_CIRCUIT_COOLDOWN_S` | `60` | How long the circuit stays open before a half-open probe |

Circuit breaker state is per-replica — no shared Redis dependency. This is intentional: under a large-scale outage, having N replicas rediscover independently is resilient; having them all depend on Redis for breaker state is not.

## Tracing

| Var | Default | Purpose |
|---|---|---|
| `LW_GATEWAY_OTEL_EXPORTER` | `otlp-http` | One of `otlp-http`, `otlp-grpc`, `none` |
| `LW_GATEWAY_OTEL_SAMPLING_RATIO` | `1.0` | Head sampler. Gateway-internal spans only — customer traces are passed through untouched |
| `LW_GATEWAY_OTEL_TENANT_ROUTER` | `enabled` | Per-tenant trace routing — reads the VK bundle's `observability_endpoint` (populated from the control-plane `Project.observabilityEndpoint`) and routes each span to the owning project's OTel pipeline. See [Observability](/ai-gateway/observability) |
| `GATEWAY_OTEL_DEFAULT_ENDPOINT` | `https://app.langwatch.ai/otel/v1/traces` | Fallback endpoint when `observability_endpoint` is null for a given project. Self-hosted: point this at your in-cluster OTel collector |

### Per-project routing (no customer config)

When `LW_GATEWAY_OTEL_TENANT_ROUTER=enabled` (default), the gateway:

1. Reads each VK bundle's `observability_endpoint` on bundle-resolve and on every config-refresh.
2. Writes `project_id → endpoint` into an in-memory `ProjectEndpointRegistry`.
3. On every span export, the `RouterExporter.Resolver` looks up the project id and routes to that endpoint; null/empty values fall back to `GATEWAY_OTEL_DEFAULT_ENDPOINT`.

Result: customer spans land in the customer's own LangWatch project without any env-var or client-side config. When an operator changes `Project.observabilityEndpoint` in the control plane, the next `/changes` long-poll propagates it ≤ 30 s.

Shipped in langwatch-saas `5fe8486` (@sergey Lane A iter 4 pt4) wired to `observability_endpoint` on the GatewayConfig bundle (@alexis Lane B iter 6 `6cda472ec`).

## Guardrails + budgets

| Var | Default | Purpose |
|---|---|---|
| `LW_GATEWAY_GUARDRAIL_TIMEOUT_MS` | `1500` | Request-level guardrail timeout. Fails closed (blocks request) |
| `LW_GATEWAY_GUARDRAIL_STREAM_CHUNK_TIMEOUT_MS` | `50` | Per-chunk guardrail timeout during streaming. Fails OPEN (lets chunk through) |
| `LW_GATEWAY_BUDGET_DEBIT_RETRY_MAX` | `8` | Outbox retries before shelving. Exponential backoff |
| `LW_GATEWAY_BUDGET_DEBIT_SHELF_BYTES` | `134217728` (128 MB) | On-disk shelf size for unprocessable debits (4xx from control plane) |

## Startup gate

| Var | Default | Purpose |
|---|---|---|
| `GATEWAY_STARTUP_NETCHECK_HOSTS` | *(empty)* | Comma-separated `host:port` list. On startup, the gateway resolves DNS + dials TCP for each entry; any failure causes `/startupz` to stay red, which makes k8s recycle the pod **before** `MarkStarted` fires. Designed to catch `NetworkPolicy` misconfig at deploy time, not first customer request. DNS-class and TCP-class failures are distinguished in the error string so operators can pinpoint which egress rule is wrong. Iter 20 (`f7901b3`). See [Helm → Startup network check](/ai-gateway/self-hosting/helm#startup-network-check-iter-20-f7901b3) |
| `GATEWAY_STARTUP_NETCHECK_TIMEOUT` | `2s` | Per-host timeout (DNS + TCP combined). Stop-on-first-failure — the probe exits as soon as one host is unreachable |

## Admin / operator endpoints

| Var | Default | Purpose |
|---|---|---|
| `GATEWAY_ADMIN_ADDR` | `127.0.0.1:6060` | Listener exposing Go `net/http/pprof` for heap / goroutine / profile / allocs / mutex / block dumps. Default is loopback-only — reachable exclusively via `kubectl port-forward`. Set to empty string to disable the listener entirely. Set to `0.0.0.0:6060` (or similar) to listen non-loopback; **in that case a bearer token is required** (iter 22) |
| `GATEWAY_ADMIN_AUTH_TOKEN` | *(empty)* | Bearer token guarding the admin listener (iter 22, `af796b8`). Compared via `crypto/subtle.ConstantTimeCompare`. Required whenever `GATEWAY_ADMIN_ADDR` is non-loopback and `GATEWAY_ALLOW_INSECURE` is unset; the gateway refuses to start otherwise with a clear error naming both env vars |
| `GATEWAY_ALLOW_INSECURE` | `false` | Escape hatch to run a non-loopback admin listener without a bearer token. Exists for test harnesses — never set this in production |

Three deployment postures for the admin listener:

**1. Disabled.** Set `GATEWAY_ADMIN_ADDR=""`. No pprof at all. Pick this for compliance-regulated deployments where live profiling is not permitted.

**2. Loopback-only (default).** `GATEWAY_ADMIN_ADDR=127.0.0.1:6060`, no token. Operator access is via `kubectl port-forward` through the Kubernetes API server, which is auditable in cluster audit logs. Bearer token is optional and works as a second defence layer.

**3. Bearer-gated non-loopback.** `GATEWAY_ADMIN_ADDR=0.0.0.0:6060` + `GATEWAY_ADMIN_AUTH_TOKEN` in a Kubernetes Secret. Pick this for non-k8s deployments (systemd / Nomad / plain Docker) where port-forward isn't available, or when you want direct access through your cluster's internal LB.

See [Helm → Admin listener](/ai-gateway/self-hosting/helm#admin-listener-iter-22-af796b8) for the Helm values shape.

Operator workflow (loopback posture, safe-by-default because the port isn't in any Service):

```bash
# Forward the admin port from a specific pod
kubectl port-forward -n langwatch deploy/langwatch-gateway 6060:6060

# Live goroutine dump when p99 spikes
go tool pprof -svg http://localhost:6060/debug/pprof/goroutine > /tmp/goroutines.svg

# 30-second CPU profile under production load
go tool pprof http://localhost:6060/debug/pprof/profile?seconds=30
```

See [Cookbook: Production runbook](/ai-gateway/cookbooks/production-runbook) for full diagnostic recipes.

## Operational defaults worth knowing

- The gateway preloads every VK's config on startup (via `/bootstrap`) if `LW_GATEWAY_AUTH_CACHE_BOOTSTRAP=true`. This adds ~1–3 s to startup for a 50K-key tenant. Disable it for horizontally-sharded deployments where each replica only serves a subset.
- `SIGTERM` handling: 30 s drain, refuse new requests with 503, finish in-flight streams. Matches Kubernetes `preStop` + `terminationGracePeriodSeconds: 35`.
- `PROTECTED_ENDPOINTS` (internal): the `/healthz` / `/readyz` / `/metrics` endpoints never require auth and are not rate-limited — give your scraper direct access.

See [Health Checks](/ai-gateway/self-hosting/health-checks) for what each probe validates and [Scaling](/ai-gateway/self-hosting/scaling) for HPA and capacity planning.
