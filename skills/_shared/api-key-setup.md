# LangWatch API Key Setup

1. Get your API key from https://app.langwatch.ai/authorize
2. Add to your project's `.env` file:
```
LANGWATCH_API_KEY=your-api-key-here
```
3. The MCP server also needs the API key — see mcp-setup.md
