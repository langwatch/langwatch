[
  { "name": "main", "sql": "SELECT toDate(TimeUnixMs) AS day, round(PreTokens / 1000) AS at_k, CompactionTrigger AS trigger FROM agent_session_events WHERE EventKind = 'compaction' AND TimeUnixMs >= {dashboard_context_period_start:DateTime} AND TimeUnixMs < {dashboard_context_period_end:DateTime} ORDER BY day", "parameters": [] }
]
