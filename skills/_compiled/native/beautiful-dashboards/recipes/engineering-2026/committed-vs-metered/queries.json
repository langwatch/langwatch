[
  { "name": "main", "sql": "SELECT toStartOfMonth(Day) AS month, Charge AS charge, sum(Cost) AS cost FROM finops_usage WHERE DepartmentId = 'engineering' AND Day >= {dashboard_context_period_start:DateTime} AND Day < {dashboard_context_period_end:DateTime} GROUP BY month, charge ORDER BY month", "parameters": [] }
]
