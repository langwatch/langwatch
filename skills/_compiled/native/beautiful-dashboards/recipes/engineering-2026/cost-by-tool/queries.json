[
  { "name": "main", "sql": "SELECT Tool AS label, sum(Cost) AS cost FROM finops_usage WHERE DepartmentId = 'engineering' AND Day >= {dashboard_context_period_start:DateTime} AND Day < {dashboard_context_period_end:DateTime} GROUP BY label ORDER BY cost DESC LIMIT 8", "parameters": [] }
]
