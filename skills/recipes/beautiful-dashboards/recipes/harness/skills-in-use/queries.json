[
  { "name": "main", "sql": "SELECT skill AS name, count() AS invocations FROM (SELECT arrayJoin(Skills) AS skill FROM agent_sessions WHERE StartedAt >= {dashboard_context_period_start:DateTime} AND StartedAt < {dashboard_context_period_end:DateTime}) GROUP BY name ORDER BY invocations DESC LIMIT 8", "parameters": [] }
]
