[
  { "name": "main", "sql": "SELECT Title AS title, RepositoryName AS repo, toDate(StartedAt) AS day, formatDateTime(StartedAt, '%H:%M') AS window_start, Commits AS commits, CostUsd AS cost FROM agent_sessions WHERE StartedAt >= subtractDays(now(), 7) AND toHour(StartedAt) < 7 ORDER BY StartedAt DESC LIMIT 8", "parameters": [] }
]
