[
  { "name": "main", "sql": "SELECT toStartOfMonth(Day) AS month, Tool AS tool, sum(Cost) AS cost FROM finops_usage WHERE DepartmentId = 'engineering' AND Day >= toStartOfYear(now()) GROUP BY month, tool ORDER BY month", "parameters": [] }
]
