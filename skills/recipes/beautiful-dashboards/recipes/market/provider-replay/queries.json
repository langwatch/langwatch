[
  { "name": "main", "sql": "SELECT sum(CacheReadTokens) AS cache_read, sum(CacheCreationTokens) AS cache_write, sum(InputTokens - CacheReadTokens - CacheCreationTokens) AS fresh, sum(OutputTokens) AS output FROM agent_sessions WHERE StartedAt >= subtractDays(now(), 30)", "parameters": [] }
]
