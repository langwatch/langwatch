[
  { "name": "main", "sql": "SELECT toStartOfMonth(Day) AS month, sum(Units) AS questions FROM finops_usage WHERE Charge = 'activity' AND Day >= {dashboard_context_period_start:DateTime} AND Day < {dashboard_context_period_end:DateTime} GROUP BY month ORDER BY month", "parameters": [] }
]
