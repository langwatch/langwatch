[
  { "name": "main", "sql": "SELECT toStartOfMonth(Day) AS month, Tool AS tool, sum(Cost) AS cost FROM finops_usage WHERE DepartmentId = 'engineering' AND Day >= {dashboard_context_period_start:DateTime} AND Day < {dashboard_context_period_end:DateTime} GROUP BY month, tool ORDER BY month", "parameters": [] }
]
