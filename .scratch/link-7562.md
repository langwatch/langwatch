Guiding issue filed: #7608.

The ruling that governs both failure modes here: **confirm before delete unless the user can restore it themselves.** We enumerated Langy's 15 delete commands and none has a user-visible restore path, so all 15 confirm. The two restorable resources (prompts, experiments) have no delete command.

Both failure modes tracked here — deletes-then-confirms (ordering), and the self-authored bypass passphrase — are why the fix is a **structural gate**, not stronger prompt wording: a rule Langy can reason around is not a gate. Detail and scope in #7608.
